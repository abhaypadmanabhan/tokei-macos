# Tokei — Release Candidate v0.4.0 (dev → main) · 2026-07-09

**Status:** Prepared, **NOT shipped / NOT pushed.** `origin` is the PUBLIC repo — pushing needs a
full-history secret sweep + Abhay's explicit go-ahead. Doubles as the PR body once the push clears.
Audit trail: `tasks/patch-bibles/2026-07-09*.md` (rounds 1–3) + this session's polish worktrees.

Base: `main` @ v0.3.0. `dev` = **35 commits** ahead. Manual QA passed (Abhay).

## Highlights

### Visual redesign (headline)
- Analytics dashboard matching the approved mockups: rounded surface cards, Swift-Charts line/area
  trends, provider **donut**, activity **heatmap**, circular **gauges**, streak/best-day stats.
- **Overview** + **Provider Detail** rebuilt; real data via a frozen `DashboardViewModel` analytics
  surface; honest empty states (no fake numbers).
- **Settings** 6-card grid + custom **`PadzyToggle`** switch style + strict column alignment.
- **Menu-bar** popover restyle + **dynamic Tokei status icon** (bars fill with burn, ink→accent,
  subtle flame near/at limit, syncing spinner). Popover now dismisses on Open Dashboard / Settings / Quit.
- **Monochrome** provider marks (one consistent family).
- Plan-only providers (Antigravity / Cursor-offline) use the redesign card language — brand header,
  credits gauge, honest "token usage unmeasured" (never a fake 0 hero).

### Token-Maxxer (epic #20)
- Menu-bar space-efficient + tightest-% mode (#40/#38); pace notch + linear-burn verdict (#36);
  route-here chip (#37).

### Core / data
- Pure **`UsageAnalytics`** (streak, best/least day, delta, provider split, rolling, peak day/hour).
- **Hourly buckets** (`ProviderSnapshot.hourlyTotals`, append-only) → activity heatmap + peak-hour.
- **Quota snapshot sampler** + append-only `QuotaSeriesStore` (#32); bounded, atomic writes.
- **Cursor quota resilience** (#35) — 429 cooldown/backoff mirroring the Claude client.

## Closed issues
#32 #35 #36 #37 #38 #40 (this batch). #39 #43 closed (shipped 0.3.0).

## Antigravity data-integrity decision
Verified local transcripts (`~/.gemini/antigravity*/…/transcript.jsonl`) carry planner/command events +
a static context budget, **not** per-message token counts; conversation DBs are opaque protobuf. Decision:
**keep Antigravity honest (credits/plan/model-quota only)** — no estimated-as-real tokens. TokenTracker's
Antigravity tokens are tokenizer estimates, not provider-reported.

## Verification
- `run-all.sh full` (BASE_REF=main): **ALL PASS**. 214 core tests, 0 failures.
- `run-all.sh release` (strict): PASS except `format`/`lint` → **FAIL only because swiftlint/swiftformat
  are not installed** (strict treats absent tool as fail). **Accepted risk**, identical to v0.3.0/v0.2.1
  precedent. `brew install swiftlint swiftformat` for a clean strict pass.
- **Security review** (`main…dev`, 7692-line diff): **0 findings.** Cursor cookie stays header-only over
  TLS (never logged/persisted); cooldown/series files store no secrets; all paths hardcoded (no traversal);
  Codable deserialization is memory-safe; no shell/subprocess with untrusted args; zero new log calls.
- **Simplify:** covered at per-package merge review; no blanket pass (fresh, reviewed code).

## Blockers before shipping
1. **Version** — `main` is 0.3.0. **Proposed: 0.4.0** (major redesign + features). Awaiting Abhay's confirm;
   then bump `AIUsageDashboard/project.yml MARKETING_VERSION`.
2. **Public-repo push gate** — `origin` = public `abhaypadmanabhan/tokei-macos`. Run full-history secret
   sweep (per [[secret-hygiene]]), then Abhay's explicit go-ahead for `git push origin dev` → `gh pr create`.

## Release loop once blockers clear (per prior releases)
1. Bump `MARKETING_VERSION` → 0.4.0.
2. Secret sweep → `git push origin dev` → `gh pr create --base main --head dev`.
3. User-approved merge: `git checkout main && git merge --no-ff dev` → tag `v0.4.0`.
4. `AIUsageDashboard/scripts/release.sh` (Developer ID A47GG9FV27, hardened runtime, notary profile
   `voxi-notary`, Sparkle key on keychain `tokei`) → `cp dist/appcast.xml docs/` →
   `gh release create v0.4.0 dist/Tokei-0.4.0-arm64.dmg` → push main.
5. Update website (see `website-todo-2026-07-09.md`).

## Notarization / signing
Configured (Release: Developer ID `A47GG9FV27`, notary profile `voxi-notary`, Sparkle keychain `tokei`).
Notarization is an outward Apple submission → **left as an explicit manual step** via `scripts/release.sh`,
not auto-run in this flow.

## Rollback
Each package merged `--no-ff` → `git revert -m 1 <merge-sha>`. `main` (v0.3.0) is the safety net; nothing
has reached it. Worktrees + `patch/2026-07-09/*` branches retained until ship.
