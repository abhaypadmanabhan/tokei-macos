# Release candidate — dev → main (2026-07-07)

Prepared via `/dev-approved`. On `dev` @ `5cc1624`, **not merged to `main`, not tagged.**
Suggested version: **0.2.0** (feature release over the 0.1.0 MVP) — bump `MARKETING_VERSION`
in `AIUsageDashboard/project.yml` at merge if adopted (currently `0.1.0`).

Audit trail: `tasks/patch-bibles/2026-07-07.md` (today) + `tasks/patch-bibles/2026-07-06.md`
(prior connector round). Scout findings: `tasks/research/2026-07-06-connector-endpoints-findings.md`.

## What's in this RC (highlights)
- **Antigravity — live Model Quota (opt-in).** Real per-model-group weekly + 5-hour limits
  (Gemini, Claude&GPT) with reset countdowns, fetched from the **local** Antigravity
  `language_server` connect-rpc (`RetrieveUserQuotaSummary`, csrf-authed, 127.0.0.1-scoped
  TLS). The Google `ya29` OAuth token is never read, sent, or stored. Behind
  `@AppStorage("antigravityOnlineQuotaEnabled")` (default OFF); degrades silently when the
  Antigravity app is closed. `.providerReported` confidence.
- **Cursor — accurate plan label** via opt-in `full_stripe_profile` (same Bearer JWT); no
  fabricated gauge for uncapped accounts.
- **Codex estimated USD cost**, **Cursor local connector**, **honest connection UX**,
  **Padzy "aitracker" design compliance** (from the 2026-07-06 round).
- **Critical fix (`5cc1624`):** subprocess pipe-buffer **deadlock** in the Antigravity
  discovery that hung the entire sync (all providers UNAVAILABLE / "SYNCED NEVER") whenever
  the online toggle was on. Root-caused, fixed (drain stdout before `waitUntilExit` + watchdog
  timeout), and covered by 2 regression tests.

## Verification
- **Full gates vs `main` (full): GREEN** — build (App) + 91 Core tests + no-secret + no-large-artifact + no-uncommitted.
- **Live end-to-end (real app):** sync completes; Claude Code / Codex / Cline populate; Antigravity
  renders Gemini 8%/12% used weekly/5h + Claude&GPT 0%/0% with countdowns. Store-verified against
  an independent live capture. 0 stuck subprocesses (deadlock gone).
- **Security review of the new code: no findings ≥8 confidence** — no command injection (fixed
  paths, Int32 pid, no shell), TLS trust scoped to 127.0.0.1 only, tokens/csrf never logged/
  persisted/leaked (JWT only Bearer-over-TLS; endpoint description excludes csrf), protobuf/SQLite
  parsing bounds-checked with static queries.

## Known issues / blockers before shipping
1. **Strict release gates: lint + format FAIL** — `swiftlint`/`swiftformat` not installed in
   the build environment (STRICT mode hard-fails missing tools). Install both to complete the
   strict gate; build/test/secrets are green.
2. **Adhoc/unsigned archive** — `DEVELOPMENT_TEAM=""`, `CODE_SIGN_IDENTITY="-"`. The
   `build/Tokei.xcarchive` is a **local test artifact, not distributable**. Notarization skipped;
   configure a Developer ID + team to sign + `notarytool submit --wait` + `stapler staple` (issue #8).
3. **Pre-existing public-history secret (#11, NOT from this PR):** a partial `sk-ant-oat01-`
   Anthropic OAuth token fragment is already in **public `main`** history (since `2b24785`;
   redacted forward in `7ef4272`). **Owner action required before treating the repo as clean:**
   rotate that credential and scrub history (`git filter-repo`) + force-push. This dev→main PR
   does not add any new secret (verified: tree + history sweep clean; fixtures use the public
   jwt.io example token and public OAuth client IDs only).
4. **First public push:** `dev` has never been pushed; `origin` has only `main`. Opening the PR
   exposes all `dev` history on a PUBLIC repo — gate on #11 above.

## Minor follow-ups (non-blocking, post-release polish)
- Antigravity sidebar row still labels "PLAN ONLY / ENABLE ONLINE" while the detail view shows
  full live quota — stale capability-tier label; recompute when online quota is active.
- `SyncEngine.snapshotAll` awaits every provider in one TaskGroup; a slow provider delays the
  whole sync. Consider per-provider isolation/timeout (the deadlock fix's watchdog mitigates the
  worst case).
- Dead UI files `UI/ProviderDetail/ProviderDetailView.swift` + `UI/Components/QuotaWindowRow.swift`
  are unreferenced (real render path is `DashboardView.swift`) — verify-then-remove.

## Rollback
- Each package merged to `dev` as one `--no-ff` commit → `git revert -m 1 <merge-sha>`
  (`cc58bed` core, `f94a84d` ui). The deadlock fix `5cc1624` is a standalone revert.
- `main` is untouched by this workflow; nothing ships without an explicit merge + tag.

## Remaining manual steps before shipping
1. Rotate the #11 Anthropic credential + scrub public history (owner).
2. `brew install swiftlint swiftformat`, re-run `run-all.sh release`, fix any findings.
3. Configure signing (Developer ID + DEVELOPMENT_TEAM), notarize + staple, verify with
   `codesign --verify --deep --strict` and `spctl -a -vvv`.
4. Bump `MARKETING_VERSION` to `0.2.0` (if adopted).
5. Open the dev→main PR (held pending owner approval — see push decision).
