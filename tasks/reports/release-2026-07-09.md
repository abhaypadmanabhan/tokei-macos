# Release Candidate — 2026-07-09 (dev → main)

**Status:** Prepared, **NOT shipped.** Two hard gates remain (see §Blockers). Do not push/merge
until resolved. This doc doubles as the PR body when the push gate clears.

Audit trail: [`tasks/patch-bibles/2026-07-09.md`](../patch-bibles/2026-07-09.md).

## What's in this RC (dev @ `main…dev`, 13 commits)

Three work packages, planned via `/morning-patch`, integrated via `/agents-done`:

1. **opencode provider connector** (new provider) — local usage for opencode. Dual-source
   `OpencodeStoreParser`: SQLite `~/.local/share/opencode/opencode.db` (`message.data` JSON)
   primary + `storage/message/**/*.json` fallback. Maps assistant `tokens{input,output,
   reasoning,cache{read,write}}` + provider-reported `cost` onto the existing snapshot shape.
   `ProviderID.opencode` added; registered in `ProviderRegistry`.
2. **#43 [P0] Keychain no-prompt** — replaced app-side `SecItemCopyMatching` on the CLI-owned
   `"Claude Code-credentials"` item with file-first + a spawned `/usr/bin/security … -w` read
   (injected `KeychainPasswordSpawning`, stderr discarded, 2 s timeout). Dialog-free, survives
   Claude Code recreating the item on token rotation. Live-verified on this machine (exit 0,
   1125 B OAuth JSON, no GUI prompt). **Pending:** confirm the same read inside the notarized,
   hardened-runtime build (entitlement to spawn `security`).
3. **UX overhaul** — removed the `NN /` numbered-kicker aesthetic (`EditorialKicker`→
   `SectionLabel`); Settings revamp (#39); responsive window (`.windowResizability(.contentMinSize)`
   + `ViewThatFits` status strip; no clip 640×480→full-screen); `+` add-agent flow
   (disk-detected + manual, add = un-hide); one-accent restored (decorative amber/green removed);
   opencode UI surface (brand mark + read-path).

## Verification

- Full gate set vs `main`: **ALL GREEN** (`preflight, no-secret, no-large-artifact,
  no-uncommitted, format(skip), lint(skip), build, test`).
- Strict release gates vs `main`: green **except** `format`/`lint` — swiftlint/swiftformat not
  installed → strict SKIP→FAIL. **Accepted risk**, identical to the v0.2.1 release precedent.
  Recommend `brew install swiftlint swiftformat` for a clean strict pass.
- Core tests: **167 passing, 0 failures.**
- Security review (`main…dev` code diff): **0 findings.** Subprocess spawn uses an argument
  array with a hardcoded service name (no shell → no command injection); SQLite queries are
  static (no injection); JSON parse is memory-safe Swift. The 3 commits added after the review
  (Bible, redesign spec, `.gitignore`/notes) are docs/chore only — no code — so the review holds.
- Manual QA: user tested the `build/dev` debug build and approved (`/dev-approved`).

## Blockers (must clear before shipping)

1. **Public-repo push gate (#11 secret-history sweep).** `origin` is the PUBLIC repo
   `abhaypadmanabhan/tokei-macos`; dev is 54 commits unpushed. The recorded policy requires a
   full git-history secret sweep + explicit go-ahead before any `git push origin dev`. Not done
   on the orchestrator's initiative. **Action:** run the full-history sweep, then user approves
   the push; then open PR dev→main.
2. **Version bump.** `main` is already `0.2.1`. This RC adds a new provider + P0 fix + UX under
   the SAME version → Sparkle will not offer the update to existing users. **Recommend `0.2.2`**
   (or `0.3.0` given a new provider). No version arg was passed to `/dev-approved`, so
   `MARKETING_VERSION` was left at `0.2.1`. **Action:** decide the version; bump
   `AIUsageDashboard/project.yml MARKETING_VERSION`.

## Remaining steps once blockers clear (release loop, per prior v0.2.1)

1. Bump `MARKETING_VERSION` → decided version.
2. Full-history secret sweep → `git push origin dev` → `gh pr create --base main --head dev`.
3. Merge PR (user approval): `git checkout main && git merge --no-ff dev` → tag `vX.Y.Z`.
4. Release archive + notarize (Developer ID "Abhay Padmanabhan (A47GG9FV27)", hardened runtime,
   Sparkle account `tokei`): `AIUsageDashboard/scripts/release.sh` → `cp dist/appcast.xml docs/`
   → `gh release create vX.Y.Z dist/Tokei-X.Y.Z-arm64.dmg` → push main.
5. **Confirm the keychain fix is dialog-free in the notarized build** (the one unverified case).
6. Update website (§below) + this repo's release notes.

## Notarization / signing status

Not run yet (gated behind version + push). Signing IS configured (Developer ID + hardened
runtime in `project.yml` Release; notary profile `voxi-notary`; Sparkle key on keychain account
`tokei`). The archive/notarize is step 4 above, after the version is set.

## Website (#10)

A Next.js site exists at `website/` (now gitignored — tracked separately). When shipping,
update: version string, **add opencode to the supported-providers list**, changelog highlight
(keychain no-prompt fix + UX overhaul), and the DMG download link to the new tag.

## Rollback

Each WP merged to `dev` as one `--no-ff` commit → `git revert -m 1 <merge-sha>` per package.
`main` (v0.2.1) is the safety net; nothing has reached it. Worktrees + `patch/2026-07-09/*`
branches retained until ship, then pruned.
