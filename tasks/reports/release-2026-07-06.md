# Tokei — Release Candidate 2026-07-06 (dev → main)

Prepared by `/dev-approved`. **Not merged to `main`, not tagged.** Awaiting final approval.

- **Branch:** `dev` @ `0a630dc` · **Base:** `main` @ `0bead8f` · 18 commits ahead.
- **Product:** Tokei (`ai.padzy.tokei`) — local-first macOS AI usage dashboard.
- **Audit trail:** `tasks/patch-bibles/2026-07-06.md` (Patch Bible + per-agent completion notes).
- **Manual QA:** completed by Abhay on the dev build; two findings raised and fixed
  (Codex cost family-fallback, in-app Settings) before this doc.

## What changed
See `CHANGELOG.md` → *Unreleased (dev)*. Headlines:
- Codex estimated USD cost (family-fallback pricing, `.estimated`).
- Cursor local `state.vscdb` read layer (read-only, secret-row–excluded, honest fallback).
- In-app Settings pane (sidebar entry); separate macOS Settings dialog removed.
- Padzy "aitracker" design-system compliance pass; real empty/loading/error states.

## Work packages (all merged to dev, `--no-ff`)
| WP | Merge | Agent |
|----|-------|-------|
| Codex cost estimate | `4cd0462` | Claude Sonnet |
| Cursor connector (local) | `54f104e` | Codex |
| UI Padzy compliance | `34f1f2b` | Claude Opus |
| Fix: Codex family pricing + reachable Settings | `1fed19c` | Claude (QA fix) |
| Feat: in-app Settings pane | `2466b6a` | Claude (QA fix) |
| Gate fix + /simplify cleanups | `4302b80`, `0a630dc` | Claude |

## Gate results (this run)
- `run-all.sh full` (BASE_REF=main): **ALL GREEN** — preflight, no-secret, no-large-artifact,
  no-uncommitted, format(skip), lint(skip), build, **test 69/69**.
- `run-all.sh release` (STRICT_GATES=1): **build + test + secrets + artifacts + uncommitted PASS**;
  **lint + format FAIL only because swiftlint/swiftformat are not installed** (STRICT converts
  absent-tool SKIP → FAIL). Environment gap, not a code defect — see Follow-ups.
- **Security review** (`main...dev`): no findings. No network/process/exfil added; Cursor SQL
  excludes `secret://`/`cursorauth/`/`accesstoken`/`refreshtoken` rows and extracts only integer
  token components; no secrets in the diff. `no-secret` gate hardened to not self-match.
- **/simplify:** 4-angle review; applied resolveRate dead-branch removal, `didSet` invariant
  centralization, padding alignment, `static let` version, and the `no-secret` narrow-exclusion.
  Deferred: shared MenuBarButton/SurfaceCard/SidebarRow componentization (out-of-scope refactor).
- **Dependency audit:** no external Swift packages added; only `UserNotifications.framework`
  (SDK) + system `SQLite3`. Clean.
- **Greptile:** not configured on this repo — relied on security-review + /simplify.

## Build / archive artifacts
- Debug dev build (manual QA): `AIUsageDashboard/build/dev/Build/Products/Debug/Tokei.app`.
- Release archive: `AIUsageDashboard/build/Tokei.xcarchive` — **adhoc-signed, TeamIdentifier not
  set. TEST ARTIFACT ONLY, not distributable.** Notarization skipped.

## Signing / notarization — NOT configured
`project.yml`: `DEVELOPMENT_TEAM: ""`, `CODE_SIGN_IDENTITY: "-"` (adhoc), `ENABLE_HARDENED_RUNTIME: NO`.
To ship a distributable build (issue #8): set a real `DEVELOPMENT_TEAM` + Developer ID identity,
re-enable hardened runtime, export with an options plist, `xcrun notarytool submit --wait`,
`xcrun stapler staple`, then verify `codesign --verify --deep --strict` + `spctl -a -vvv`.

## Known issues
Carried from CHANGELOG: Cursor token metrics unavailable where only code-line stats exist;
Antigravity skeleton; Codex whole-aggregate single-model pricing; UI error state not yet
reachable live; menu-bar label total non-mono.

## Follow-ups before a public ship (open issues)
- **#8 Distribution** — Developer ID signing + hardened runtime + notarized DMG.
- **#9 Auto-update** — Sparkle 2 + signed appcast.
- **#10 Website** — see `tasks/reports/website-todo-2026-07-06.md`.
- **#11 Legal** — LICENSE + monetization decision (Abhay) + redact token fragments.
- **Strict gate:** install swiftlint + swiftformat, add `.swiftlint.yml` / `.swiftformat`
  config, fix violations in a dedicated pass so `run-all.sh release` is fully green.
- **Version bump:** `MARKETING_VERSION` 0.1.0 → **0.2.0** at merge (feature release).

## Rollback
- Per package: `git revert -m 1 <merge-sha>` (each package is one `--no-ff` merge; shas above).
- Whole candidate: `dev` is disposable; `main` @ `0bead8f` is the safety net. Nothing reaches
  `main` without an explicit merge.
