# Patch Bible — 2026-07-19

> Single source of truth for the day's parallel engineering. Generated by
> `/morning-patch`. Consumed by each agent and by `/agents-done`. Do not delete;
> this is the audit trail. Filename: `tasks/patch-bibles/2026-07-19.md`.

- **Base branch:** `dev` @ cb22fb9
- **Integration target:** `dev`
- **Release target:** `main` (never merged automatically)
- **Gate mode at merge:** `full` (release gates run at `/dev-approved`)
- **Product:** Tokei (`ai.padzy.tokei`) — local-first macOS AI usage dashboard
- **Baseline:** tests GREEN (`AIUsageDashboardCore` passes); tree clean;
  lint/format SKIP (swiftlint/swiftformat not installed); dev == main tip,
  ahead 0 / behind 0.

Source of this wave: repo scan 2026-07-19 + open-issue triage. Key finding: the
value engine (`Core/Pricing`) and utilization spine (`Core/Utilization`) landed
in 0.4.0/0.5.0 as fully-tested Core layers with **zero UI consumers** — the
"am I using the tokens I pay for" surface (#23, the product thesis) is the gap.
Track B (Maxxer social/flex cards, #24/#27) stays **parked at the design gate**.

**Stale-open issues verified DONE in code this scan** (close at `/agents-done`,
do not re-implement): #32 (QuotaSeriesStore + sampling via SyncEngine), #35
(Cursor CooldownStore resilience), #38/#40 (MenuBarDisplayMode: todayTokens /
tightestPercent / iconOnly shipped), #45 (SQLiteSidecarCopy + CooldownStore
extracted, commit fe50bde), #26 Gemini half (GeminiProvider shipped 51b8aa3).

---

## 1. Selected issues (prioritized)

Priority score = user-impact × release-value ÷ risk, tie-broken by dependency
order then parallelization potential.

| # | Issue / source | Why it matters | Impact | Risk | Depends on | Agent | Worktree branch |
|---|----------------|----------------|--------|------|-----------|-------|-----------------|
| 1 | #23 (core half) — Maxxer value engine: plan costs, value multiple, scorecard + tiers | The product's thesis ("are you using the tokens you pay for") has tested Core math but NO consumable contract. This WP turns `Core/Pricing` into a scorecard the UI can render. | H | M | — (contract frozen in §4) | Codex | `patch/2026-07-19/maxxer-value-core` |
| 2 | #23 (UI half) + #41 — Value pane, plan-cost settings, lifetime stat + menu-bar lifetime mode | The visible payoff: value-multiple headline + Maxxer tier, per-provider value rows, plan-cost entry, lifetime totals (#41) incl. a new menu-bar `lifetime` display mode. Display-tier hero surface. | H | M | WP-1 contract (shim until merge) | Claude Code (Opus) + padzy-os | `patch/2026-07-19/maxxer-value-ui` |
| 3 | #48 + #49 — QuotaSeriesStore per-series retention; Cursor cooldown scoped per cookie | #48: global 20k cap lets a fast series evict a slow series' history — must be fixed before history/lifetime views consume the series. #49: one account's 429 cooldown fast-fails another account's requests. Both filed from Greptile review of PR #47. | M | L | — | Kimi K2.7 | `patch/2026-07-19/series-retention-cooldown` |

**Selection rationale:** the wave is the #23 vertical (core + UI, file-disjoint
by the UI lock) plus one bounded infra package. WP-1/WP-3 touch disjoint Core
dirs; WP-2 is `UI/`-only. Deferred (see §1b): #12 onboarding (big UI package —
would collide with WP-2's `UI/` scope; next wave's UI slot), #18 crash
reporting, #42 per-agent breakdown (P3), #25 remainder (heatmap already
shipped; streak/achievement layer waits for value surface), #24/#27 (parked).

### 1b. Scored candidates (not selected)

| Issue | Score reasoning | Verdict |
|-------|-----------------|---------|
| #12 onboarding | H impact ÷ M risk, but UI-scope collision with WP-2 | next wave |
| #18 crash reporting | M×M÷M; needs channel decision | next wave |
| #42 per-agent breakdown | P3 "later" per issue | deferred |
| #25 streaks/achievements | heatmap shipped; rest builds ON the value surface | after #23 |
| #24 flex card / #27 leaderboard | Track B parked at design gate | parked |
| #10/#16/#17/#19 | ship-infra batch, no parallelization win today | backlog |

## 2. Merge order & rationale

1. `patch/2026-07-19/series-retention-cooldown` — lowest risk, behavior-narrow
   infra; keeps `dev` green while bigger merges land.
2. `patch/2026-07-19/maxxer-value-core` — additive Core contract; no UI change.
3. `patch/2026-07-19/maxxer-value-ui` — consumes WP-1. **Integration step:
   delete `UI/Value/MaxxerContractShim.swift` at merge** (its types are the
   frozen §4 contract; WP-1's real implementations replace it 1:1).

## 3. Per-issue work packages

### WP-1 · Maxxer value engine (core contract) — Codex
- **Worktree:** `../tokei-worktrees/2026-07-19-maxxer-value-core` (branch `patch/2026-07-19/maxxer-value-core`)
- **Scope (in):** NEW dir `AIUsageDashboardApp/Core/Maxxer/` only, plus tests in
  the Core test target. Read-only use of `Core/Pricing`, `Core/Models`,
  `Core/Analytics`.
- **Scope (out):** everything else. Do NOT touch `UI/`, `Core/Sync/`,
  `Core/Storage/` (no new JSON-store fields — plan costs live in UserDefaults),
  `ModelCodableExtensions.swift`, `project.yml`.
- **Files likely involved:** `Core/Maxxer/MaxxerPlanCostStore.swift`,
  `Core/Maxxer/MaxxerValueEngine.swift`, `Core/Maxxer/MaxxerScorecard.swift`,
  `Tests/.../MaxxerValueEngineTests.swift`.
- **Acceptance criteria:**
  - [ ] Types + signatures match §4 "Maxxer contract" EXACTLY (UI is built
        against them verbatim).
  - [ ] `MaxxerValueEngine.scorecard(snapshots:planCosts:now:)` is pure/static:
        month-to-date API-equivalent USD per provider via `PricingEngine`
        (reuse its existing API — internals free, contract frozen), value
        multiple = apiEquivalentUSD ÷ planMonthlyUSD (nil when either side is
        unknown), totals across providers, tier from total multiple.
  - [ ] `MaxxerPlanCostStore` persists per-provider monthly plan USD in
        UserDefaults (`maxxer.planCost.<providerID>`); unset = nil, never 0.
  - [ ] Confidence propagates from pricing (`.estimated` at best for
        API-equivalent values; `.unavailable` → excluded from totals, listed
        with nil multiple).
  - [ ] Providers with unpriced tokens set `hasUnpricedTokens` handling per
        existing `APIEquivalentCost` semantics — never silently inflate.
- **Test requirements:**
  - [ ] Unit tests: multiple known-price providers → correct per-provider +
        total multiples; unconfigured plan → nil multiple, excluded from
        totalPlanUSD; all-unavailable → empty-safe scorecard; tier boundary
        tests (0.25 / 1 / 2 / 5×).
  - [ ] Existing tests stay green.
- **Known risks:** PricingEngine API mismatch with the frozen entry-point →
  adapt INTERNALLY; the §4 contract does not name PricingEngine on purpose.
- **Rollback:** revert the single `--no-ff` merge commit; branch retained.

### WP-2 · Value surface + lifetime (UI) — Claude Code (Opus) · padzy-os · LOCKED UI package
- **Worktree:** `../tokei-worktrees/2026-07-19-maxxer-value-ui` (branch `patch/2026-07-19/maxxer-value-ui`)
- **Scope (in):** `AIUsageDashboardApp/UI/` only: NEW `UI/Value/` (pane +
  `MaxxerContractShim.swift`), `UI/Overview/OverviewView.swift`,
  `UI/Dashboard/DashboardView.swift` (mount points), `UI/Settings/SettingsView.swift`
  (plan-cost entry), `UI/MenuBar/MenuBarDisplayMode.swift` + `MenuBarLabel.swift`
  (new `lifetime` case), `UI/Components/` additive helpers. Snapshot-derived
  lifetime data comes from the EXISTING public `UsageAnalytics` /
  `ProviderSnapshot.lifetimeUsage` API — no Core edits needed or allowed.
- **Scope (out):** all of `Core/`, `App/`, `project.yml`. Do not modify the
  existing three display modes' behavior (defaults stay).
- **Acceptance criteria:**
  - [ ] `02 / VALUE`-style pane: headline total value multiple ("3.4× plan
        value"), Maxxer tier chip, per-provider rows (plan $, API-equiv $,
        multiple, confidence badge). Mono numerics, ink for $ values (accent =
        state only).
  - [ ] Real empty state when NO plan cost configured → inline CTA to the
        Settings plan-cost section; real loading + error/`unavailable` states.
  - [ ] Settings: "Plan costs" group — per-provider monthly USD field writing
        `MaxxerPlanCostStore`; blank = unset (nil), no zero-coercion.
  - [ ] Lifetime (#41): headline "X tokens all-time" stat with confidence badge
        (per-provider lifetime where parsed; providers with nil lifetime fall
        back to summed daily totals, badged accordingly — mirror
        `UsageAnalytics` line 247 semantics).
  - [ ] Menu bar: new `lifetime` display mode case (label copy + help text in
        the mode enum's style); one aggregate value, fixed compact width,
        monospaced digits; existing modes untouched; mode picker in Settings
        picks it up automatically if driven by `CaseIterable`.
  - [ ] Respects `prefers-reduced-motion`; menu-bar label stays a
        non-`TimelineView` render (see `tasks/lessons.md` — TimelineView breaks
        MenuBarExtra label).
- **Shim protocol (until WP-1 merges):** create
  `UI/Value/MaxxerContractShim.swift` containing the §4 contract types
  VERBATIM plus a stub `scorecard` returning deterministic preview data, marked
  `// SHIM — deleted at integration when WP-1 core lands (Bible §2 step 3)`.
  Never diverge from §4; never add members to shim types.
- **Test requirements:**
  - [ ] Existing Core tests stay green (you change no Core code).
  - [ ] Add UI-side unit tests only where logic exists (e.g. lifetime fallback
        formatting helper) — no snapshot-test scaffolding.
- **Design / UX constraints:** §5. This is a display-tier hero surface — run
  `padzy-os` (aitracker theme), Functional tier; Dense for the per-provider
  value table + menu bar.
- **Known risks:** shim/core drift → mitigated by §4 verbatim freeze + merge
  order; menu-bar regression → manual visual verify REQUIRED before completion
  note (lesson 1490260: build-pass ≠ visual-verify).
- **Rollback:** revert the single `--no-ff` merge commit; branch retained.

### WP-3 · Series retention + cooldown scoping — Kimi K2.7
- **Worktree:** `../tokei-worktrees/2026-07-19-series-retention-cooldown` (branch `patch/2026-07-19/series-retention-cooldown`)
- **Scope (in):** `Core/Storage/QuotaSeriesStore.swift`,
  `Core/Network/CooldownStore.swift`, `Core/Network/CursorUsageClient.swift`,
  plus their tests.
- **Scope (out):** everything else — especially `Core/Maxxer/` (WP-1),
  `Core/Pricing/`, `UI/`, cursor auth/cookie extraction logic (shipped +
  verified; you only change how the cooldown FILE is keyed).
- **Acceptance criteria:**
  - [ ] #48: retention enforced per series key `(providerID, windowType,
        bucketKey)` — cap per series (pick ~2 000, constant, documented) with
        the global 20k as backstop; oldest-first eviction within a series; a
        high-frequency series can no longer evict another series' rows.
  - [ ] #49: Cursor cooldown persisted per cookie identity (stable hash of the
        session cookie — never the raw cookie in the filename or contents);
        a 429 under cookie A does not gate requests under cookie B.
  - [ ] Migration: existing `cursor-usage-cooldown.json` (global) is honored
        for its remaining duration or cleanly ignored+removed — no crash, no
        stuck permanent cooldown.
- **Test requirements:**
  - [ ] #48: two interleaved series, one high-frequency → slow series' rows
        survive; per-series cap enforced; round-trip encode/decode preserved.
  - [ ] #49: cooldown written under cookie A; request under cookie B proceeds;
        request under cookie A still gated until expiry.
  - [ ] Existing tests stay green.
- **Known risks:** silent data-shape change in the series store → keep the
  on-disk schema identical (retention is an in-memory policy at append time).
- **Rollback:** revert the single `--no-ff` merge commit; branch retained.

## 4. Frozen contracts (all agents, non-negotiable)

- `UsageProvider` protocol — do not change signatures.
- `UsageStore` public API — additive only.
- `ProviderSnapshot` fields — **append-only**; any new stored field MUST be added
  to `Core/Storage/ModelCodableExtensions.swift` in the SAME commit (manual Codable).
  (No WP in this wave may add stored fields at all.)
- Existing tests stay green — never delete or weaken them.
- Test fixtures = Swift string literals in `Tests/Fixtures/Fixtures.swift` (no resource files).
- No secrets outside Keychain. Raw provider responses = debug-only. Cookie
  hashes must be non-reversible (SHA-256 prefix ok).
- No UI → provider direct calls (UI talks to the view model/sync engine only;
  pure functions over snapshot data, e.g. `UsageAnalytics`/`MaxxerValueEngine`,
  are allowed as today).

### Maxxer contract (WP-1 implements, WP-2 shims verbatim — FROZEN)

```swift
public struct MaxxerProviderValue: Sendable, Equatable {
    public let providerID: String
    public let apiEquivalentUSD: Double?   // month-to-date, nil if unpriceable
    public let planMonthlyUSD: Double?     // nil = user has not configured
    public let valueMultiple: Double?      // apiEquivalentUSD / planMonthlyUSD; nil unless both known
    public let confidence: MetricConfidence
    public let hasUnpricedTokens: Bool
}

public enum MaxxerTier: String, CaseIterable, Sendable {
    case idle        // total multiple < 0.25
    case warming     // 0.25 ..< 1
    case breakEven   // 1 ..< 2
    case maxxing     // 2 ..< 5
    case goblinMode  // >= 5
}

public struct MaxxerScorecard: Sendable, Equatable {
    public let providers: [MaxxerProviderValue]
    public let totalAPIEquivalentUSD: Double?
    public let totalPlanUSD: Double?
    public let totalValueMultiple: Double?
    public let tier: MaxxerTier?           // nil when no multiple computable
}

public final class MaxxerPlanCostStore {
    public init(defaults: UserDefaults = .standard)
    public func monthlyUSD(for providerID: String) -> Double?
    public func setMonthlyUSD(_ amount: Double?, for providerID: String)
}

public enum MaxxerValueEngine {
    public static func scorecard(
        snapshots: [ProviderSnapshot],
        planCosts: MaxxerPlanCostStore,
        now: Date
    ) -> MaxxerScorecard
}
```

## 5. Design / UX constraints (Padzy OS "aitracker")

- ground `#131316` · surface `#1D1D22` · ink `#ECECF1` · muted `#6E6E78` · ONE accent `#FF3B70`.
- Accent = state/action ONLY (never decoration; cost values use ink, not accent).
- Mono for all numeric/data. No shadows, no gradients, no rounded card grids, radius ≤ 4px.
- Numbered mono kickers (`01 / OVERVIEW`). Exposed 1px hairline structure.
  2px accent tick on active state.
- Premium macOS feel, clean hierarchy. Every surface needs real empty / loading / error states.
- UI is LOCKED to a dedicated Claude Code UI agent scoped to `AIUsageDashboardApp/UI/`; it
  runs the `padzy-os` skill (aitracker theme). UI is never baked into a core package and
  never assigned to a non-Claude agent. Tokens above are the reference/guard if a non-UI
  agent incidentally touches a view — external agents never "run the skill."

## 6. Gates (created / verified this run)

preflight ✅ · worktree-sanity (per worktree below) · lint SKIP (swiftlint absent) ·
format SKIP (swiftformat absent) · build · test ✅ baseline green · no-secret ·
no-large-artifact · no-uncommitted. Fast set runs per-commit (pre-commit hook,
installed per worktree); full set runs at merge (`/agents-done`); release/strict
set at `/dev-approved`.

## 7. Global rollback plan

- Each work package merges to `dev` as a single `--no-ff` commit → revert is one `git revert -m 1 <sha>`.
- `dev` is disposable; `main` is the safety net. Nothing reaches `main` without `/dev-approved`.
- Worktrees and `patch/*` branches are retained until the day's patch ships, then pruned.
  NOTE: 32 stale worktrees from 07-06…07-12 remain (all merged) — prune at `/dev-approved`.

## 8. Completion log (agents append; `/agents-done` reads)

<!-- Each agent appends on finish:
### WP-N — {{agent}} — {{time}}
- Branch/commits: <sha …>
- Done: …
- Left undone / stubbed: …
- Tests run + result: …
- Files touched: …
- Risks / watch-outs: …
-->

### WP-2 — Claude Code (Opus) · padzy-os — 2026-07-19 23:15

- **Branch:** `patch/2026-07-19/maxxer-value-ui` (base `cb22fb9`) — not merged, not pushed, no PR.
- **Commits (5):**
  - `6dc1085` feat(value): lifetime aggregation + value formatting in MaxxerMath (#41)
  - `6f0a7c9` feat(menubar): all-time tokens display mode (#41)
  - `1e42018` feat(value): Maxxer contract shim (§4 verbatim) — **DELETE AT INTEGRATION**
  - `8fb6bd8` feat(settings): Plan costs group — per-agent monthly USD (#23)
  - `dc34009` feat(value): Value pane — plan-value multiple, tier, lifetime (#23, #41)
    — carries the machine-readable integration trailers below.
- **Orchestrator hooks** (`git log cb22fb9..HEAD --format='%b' | grep -E '^(Action-Required-At-Merge|Needs-Decision):'`):
  - `Action-Required-At-Merge: delete UI/Value/MaxxerContractShim.swift (Bible §2 step 3)`
  - `Needs-Decision: MaxxerValueEngine totalAPIEquivalentUSD scope vs unpriced plans`

**Done (all WP-2 acceptance criteria met):**
- Value pane: headline total multiple + Maxxer tier chip, dense per-agent table
  (plan $, API-equiv $, multiple, confidence badge). Mono numerics; $ values in
  ink; accent spent only on the tier chip's active tick and the empty-state CTA.
- All four states real: error (retry), loading, no-agents, no-plan-costs-set
  (inline CTA routing to the Settings plan-cost group). Unknowns render `—`,
  never `$0` / `0×`.
- Settings "Plan costs": per-provider monthly USD field writing
  `MaxxerPlanCostStore`. Blank clears the key (nil), no zero-coercion;
  unparseable input reverts to last stored value; commit on blur/submit/disappear.
- Lifetime (#41): "X TOKENS ALL-TIME" + confidence badge; nil-`lifetimeUsage`
  providers fall back to summed `dailyTotals` and the note says the total is a
  floor. Mirrors `UsageAnalytics.swift:247`, with one deliberate difference —
  a provider with neither source contributes *nothing* rather than `0`.
- Menu bar: 4th `lifetime` case, one aggregate value, fixed compact width,
  monospaced digits, non-`TimelineView` label. Existing three modes untouched;
  Settings picker picked it up automatically via `CaseIterable`.
- Mount points: new `AppSection.value` + sidebar row + `rightPane` route;
  Overview gains a compact Value card linking to the pane.
- No animation added to these surfaces, so Reduce Motion has nothing to suppress.

**Stubbed / left undone:**
- `UI/Value/MaxxerContractShim.swift` — **must be deleted at merge** (Bible §2
  step 3). Contains §4 types verbatim + a REAL `MaxxerPlanCostStore`
  (UserDefaults, key `maxxer.planCost.<providerID>`, verified end-to-end) and a
  STUB `MaxxerValueEngine.scorecard` that prices tokens at one flat blended
  $/token rate instead of per-model `PricingEngine`. All its dollar figures are
  `.estimated`. WP-1 replaces it 1:1; the UI call site is written against the
  frozen signature and does not change.
- Keyboard `.onMoveCommand` arrow chain still runs Overview ↔ providers only;
  the Value row is mouse-reachable but not in that chain. Left alone
  deliberately — changing nav semantics was outside this WP.

**Tests run + result:**
- `xcodegen generate` → ok.
- `xcodebuild -scheme AIUsageDashboardCore -destination 'platform=macOS' test`
  → **273 tests, 0 failures. TEST SUCCEEDED.** (was 255 at baseline; +18 new).
- New `Tests/MaxxerTests/MaxxerLifetimeTests.swift` (18 tests): lifetime source
  preference, daily-totals fallback, mixed-source confidence degradation,
  no-source providers excluded not zeroed, hidden-provider exclusion, empty →
  nil, USD/multiple formatting incl. non-finite and true `×` sign.
- `xcodebuild -scheme AIUsageDashboardApp build` → **BUILD SUCCEEDED** (the Core
  scheme does *not* compile the app target, so this was run separately — all the
  view code lives there).
- **Manual visual verify (required, done):** built, ran the real app, confirmed
  the status item renders in all four modes — `533.6M` (todayTokens) / `46%`
  (tightestPercent) / `2.8B` (lifetime) / mark-only (iconOnly). Also verified in
  the running app: Value pane empty + loaded states, tier chip (GOBLIN MODE),
  per-agent table, lifetime card, Settings plan-cost field round-trip (typed
  `200` → field reformatted to `200.00`, `maxxer.planCost.claude_code = 200`
  persisted), and the 4-segment menu-bar picker in Settings.
  - **First verification attempt was wrong and was caught:** I launched a
    `Tokei.app` from a *different* worktree's DerivedData, where `lifetime` is an
    unknown rawValue and silently fell back to `.todayTokens` — rendering today's
    total and looking like a working lifetime mode. Re-verified against
    `AIUsageDashboard-dfqpbtrhxqjophbvguutbfwtkyif` (this worktree) after
    confirming the new strings were present in `Tokei.debug.dylib`.
  - Test-only prefs I wrote were restored afterwards: `menuBarDisplayMode` back
    to `tightestPercent`, `maxxer.planCost.claude_code` deleted.

**Files touched (11 — all under `UI/` + one new test file; no `Core/`, no
`App/`, no `project.yml`):**
`UI/Value/ValueView.swift` (new), `UI/Value/MaxxerContractShim.swift` (new),
`UI/MenuBar/MaxxerMath.swift`, `UI/MenuBar/MenuBarDisplayMode.swift`,
`UI/MenuBar/MenuBarLabel.swift`, `UI/Dashboard/AppSection.swift`,
`UI/Dashboard/DashboardView.swift`, `UI/Overview/OverviewView.swift`,
`UI/Settings/SettingsView.swift`, `UI/Components/ConfidenceBadge.swift`,
`Tests/MaxxerTests/MaxxerLifetimeTests.swift` (new).

**Risks / watch-outs:**
1. **[For WP-1 / `/agents-done` — semantic, please read]** The total value
   multiple is inflated whenever some agents have usage but no plan cost. Per
   Bible §4 + WP-1 AC, an unconfigured plan is excluded from `totalPlanUSD` but
   its usage still counts toward `totalAPIEquivalentUSD` — so with one $200 plan
   priced and four agents active, the real app showed **78.5× "plan value"**
   ($15,699 ÷ $200). I did **not** diverge the shim from the spec WP-1 is being
   built to; instead the pane now names the unpriced agents and says the total
   weighs their spend against only the priced plans. Worth deciding at
   integration whether the *engine* should restrict `totalAPIEquivalentUSD` to
   providers with a known plan cost. If it does, the UI needs no change.
2. **Shim deletion is mandatory.** If `MaxxerContractShim.swift` survives the
   merge it will collide with WP-1's real types (duplicate declarations) — the
   build fails loudly rather than silently, which is the good failure mode.
3. `MaxxerMath.swift` now also carries lifetime + formatting logic. That file is
   the app's only `UI/` source compiled into `AIUsageDashboardCoreTests`, and
   `project.yml` was out of scope, so it was the only place testable UI logic
   could live. If WP-3 or a later WP edits `MaxxerMath.swift`, expect a conflict
   in the appended sections only (existing pace/tightest funcs untouched).
4. Plan costs are USD-only and locale-independent by design (they're USD figures
   from the pricing tables, and grouping separators would break the mono column
   alignment and the field's own read-back). No multi-currency story yet.
5. The Settings plan-cost field is the first `TextField` in the codebase; it is
   covered by manual verification only, not automated tests (no UI test target).

### WP-3 — Kimi K2.7 — 2026-07-19 22:42 PDT
- Branch/commits: `patch/2026-07-19/series-retention-cooldown` @ `89462b1` (tip), `0f3f11a`
- Done:
  - #48: `QuotaSeriesStore` now enforces per-series retention on `(providerID, windowType, bucketKey)` with `defaultPerSeriesRetentionLimit = 2_000`; global `20_000` remains the backstop. Oldest-first eviction within a series; high-frequency series can no longer evict another series' rows. On-disk schema unchanged (version 1 JSON).
  - #49: `CursorUsageClientImpl` persists cooldowns per cookie using `CursorSession.identityHash` (SHA-256 truncated to 128 bits / 32 hex chars). Raw cookie never appears in filename or file contents. A 429 under cookie A does not gate cookie B.
  - Migration: existing global `cursor-usage-cooldown.json` is honored for its remaining duration, then removed; expired legacy files are cleaned up on first cooldown check so nothing becomes permanently stuck.
- Left undone / stubbed: nothing.
- Tests run + result:
  - `cd AIUsageDashboard && xcodegen generate` PASS
  - `xcodebuild -project AIUsageDashboard.xcodeproj -scheme AIUsageDashboardCore -destination 'platform=macOS' test` → **263 tests, 0 failures** (previous baseline 230; added interleaved-series retention tests and per-cookie cooldown isolation/migration tests).
- Files touched:
  - `AIUsageDashboard/AIUsageDashboardApp/Core/Storage/QuotaSeriesStore.swift`
  - `AIUsageDashboard/AIUsageDashboardApp/Core/Network/CursorSession.swift`
  - `AIUsageDashboard/AIUsageDashboardApp/Core/Network/CursorUsageClient.swift`
  - `AIUsageDashboard/AIUsageDashboardApp/Core/Network/CooldownStore.swift`
  - `AIUsageDashboard/Tests/StorageTests/QuotaSeriesStoreTests.swift`
  - `AIUsageDashboard/Tests/ProviderTests/CursorUsageClientTests.swift`
- Risks / watch-outs:
  - Tuple-based series-key grouping was rejected by this Swift toolchain; switched to a stable string composite key. Schema unchanged.
  - Legacy global cooldown is honored indiscriminately during its remaining duration (acceptable one-time migration behavior); once elapsed it is deleted and only per-cookie cooldowns remain.
  - `CooldownStore` gained an internal `remove()` and `encodedCooldown(_:)` helper; these are not public API and do not affect `ClaudeUsageClient` behavior.


### WP-1 — Codex — 2026-07-19 22:41 PDT
- Branch/commits: `patch/2026-07-19/maxxer-value-core` @ `a1e1961` (`feat(maxxer): add value scorecard engine`).
- Done: Implemented the frozen Maxxer contract verbatim; added positive-only per-provider plan-cost persistence under `maxxer.planCost.<providerID>`; added the pure static scorecard engine with PricingEngine-backed provider/API totals, plan totals, value multiples, confidence and unpriced-token propagation, calendar-MTD adjustment from daily totals, and all five exact tier boundaries. Unavailable/unpriced providers remain listed and are excluded from totals.
- Left undone / stubbed: No WP-1 contract work stubbed. Providers whose snapshots do not carry a trustworthy reference pricing model (`antigravity`, `cline`, `opencode`, `gemini`) intentionally remain unpriced instead of receiving guessed values.
- Tests run + result: `cd AIUsageDashboard && xcodegen generate` PASS; `xcodebuild -project AIUsageDashboard.xcodeproj -scheme AIUsageDashboardCore -destination 'platform=macOS' test` PASS — 265 tests, 0 failures. New focused tests were observed RED before implementation and PASS afterward (7 tests). Pre-commit no-secret/no-large-artifact PASS; format SKIP because `swiftformat` is not installed.
- Files touched: `AIUsageDashboardApp/Core/Maxxer/MaxxerPlanCostStore.swift`, `AIUsageDashboardApp/Core/Maxxer/MaxxerScorecard.swift`, `AIUsageDashboardApp/Core/Maxxer/MaxxerValueEngine.swift`, `Tests/MaxxerTests/MaxxerValueEngineTests.swift`.
- Risks / watch-outs: `ProviderSnapshot` has no model slug and its detailed `monthUsage` is rolling-month data, so Claude/Codex/Cursor use explicit reference models and calendar-MTD pricing scales the detailed rolling token mix by observed MTD daily-token coverage. Values are therefore `.estimated`; coverage is capped/flagged and never extrapolated upward. Pricing-seed drift remains inherited from `Core/Pricing`.

### WP-4 — Claude Code (Opus) · padzy-os — 2026-07-20 00:40

IA consolidation + density pass, fixing the 2026-07-19 design-level reject
(`tasks/reports/reject-2026-07-19.md`). UI-locked package.

- **Branch:** `patch/2026-07-19/ui-ia-consolidation` (base `e882c79`) — not merged,
  not pushed, no PR.
- **Commits (8):**
  - `5b331d1` feat(ui): chip-stat resolution + shared today-merge in MaxxerMath
  - `b7ba760` refactor(ui): AppSection gains DashboardTab and drill-in predicates
  - `41ece39` feat(ui): dashboard tab bar and provider chip strip
  - `1d41628` fix(ui): hidden agents stop feeding dashboard aggregates
  - `d69c95c` refactor(ui): drop the sidebar for tabs, chips and a drill-in
  - `0c6e587` feat(ui): Overview density pass — six cards to four
  - `e8ce4ab` refactor(ui): Value header stops repeating the tab pill
  - `4fd56ba` docs(ui): retire sidebar wording now that there is no sidebar

**Done (all WP-4 acceptance criteria met):**
- **Sidebar deleted.** All ten former destinations are reachable without it:
  Overview + Value = tab pills; each provider = a chip → drill-in; Add Agent =
  the strip's trailing `+`; Settings = the gear (also ⌘,); Connections = a
  drill-in from Settings and from the Limits rows' `Connect live quota →`.
- **Tabs double as the KPI row** (StackAI tabs-in-content × Vercel
  stat-strip-as-tabs): `01 / OVERVIEW` carries today's total, `02 / VALUE`
  carries the plan-value multiple, 2px accent tick on active. This is what pays
  for deleting the Overview "Value" summary card and the aggregate header line
  with **no information regression**.
- **Provider chip strip** (LangSmith): glyph + name + one live number from the
  new `MaxxerMath.chipStat` (today tokens → tightest window % → plan label).
  Deliberately a different metric from the Limits rows, so the two surfaces sit
  at different altitudes instead of printing the same list twice.
- **Drill-in over the pane**, not away from it: the tab pane stays mounted, the
  chip keeps its accent tick, back bar shows `‹ BACK` + a `TAB / LEAF`
  breadcrumb, Esc works. Reduce Motion path: `navigate(to:)` skips
  `withAnimation` and the transition resolves to `.identity`.
- **Density pass**: Overview 6 cards → 4 (Today · trend|donut · patterns|heatmap
  · Limits). Hero lost its area spark (it plotted the same series the full-size
  trend card plots) and gained the all-time tile. Shared time-range control
  (Posh) lives in the top bar and is shown only where it governs data —
  Overview and provider detail, not Value/Settings/Connections.
- **Keyboard nav rewritten, not deleted**: left/right moves along the run you
  are in (tab pills, or chips once drilled in), down enters the chip strip from
  a tab, up leaves any drill-in for its owning tab. Verified live.
- **Real states on the new surfaces**: chip strip → labelled `+ ADD AGENT` when
  nothing is visible (never a blank rail), `···` while the first sync runs,
  inline `!! SYNC FAILED — RETRY` when a sync failed before any snapshot landed;
  drill-in inherits the existing error → empty → loading → loaded precedence.

**Bugs found and fixed during visual verification (not in the original scope):**
1. Hidden agents kept feeding the Overview hero and the tab pill — with all
   seven agents hidden the pane said NO AGENTS LINKED YET while the pill read
   `21.6M`. Fixed via `ProviderVisibility.visible()`, now the single filter for
   every token aggregate. (Pre-existing in the hero; the pill made it visible.)
2. Merging today's usage folded in providers that measured nothing, so the
   merged confidence was always `.unavailable` — the hero badge read UNAVAILABLE
   over a real `LOCAL PARSED` total. Absent is not evidence against the agents
   that did report.
3. `StatCard` silently dropped a `deltaCaption` given without a `delta`, so
   Best day / Least active never showed their dates.

**Left undone / deliberately skipped:**
- **Vercel "stat tiles select the chart" was NOT applied to the hero.**
  `DashboardViewModel` has no per-day per-metric split (`overviewTrend` is total
  tokens only), so tile-as-selector would be a control that cannot change the
  chart. Applied at tab level instead, where the data is real. If a per-metric
  daily series ever lands in Core, the hero tiles are the natural home for it.
- `PadzyRadius.card = 12` / `.control = 8` kept. Bible §5 says radius ≤ 4px, but
  `PadzyTheme.swift` documents these as an explicit user override from the
  2026-07-09 redesign and the whole app is built on them; matching the shipped
  surface beat matching the older line. Flag if that override has lapsed.
- Reduce Motion is code-verified, not runtime-verified — toggling the system
  accessibility setting on the user's machine was out of proportion.
- `App/AIUsageDashboardApp.swift` has a now-stale comment ("dashboard right pane
  via the sidebar SETTINGS entry"). `App/` is out of WP-4 scope, so it was left
  alone — one-line comment fix for whoever next touches that file.

**Tests run + result:**
- `xcodegen generate` → ok.
- `xcodebuild -scheme AIUsageDashboardCore -destination 'platform=macOS' test`
  → **302 tests, 0 failures. TEST SUCCEEDED** (baseline 286; +16 new).
- New `Tests/MaxxerTests/MaxxerChipStatTests.swift` (16 tests): chip-stat
  precedence, per-provider window isolation, genuine-zero handling, syncing vs
  nothing, and the merged-today confidence regressions above.
- `xcodebuild -scheme AIUsageDashboardApp build` → **BUILD SUCCEEDED**.
- **Manual visual verify (required, done)** — built the App scheme, launched the
  real app from THIS worktree's DerivedData
  (`AIUsageDashboard-dfmeddobpfrxvxeqtepfotxljyha`, checked against the running
  dev build's path first so the WP-2 wrong-build trap could not repeat), and
  screenshotted: OVERVIEW tab, VALUE tab, chip → drill-in (mouse AND arrow keys),
  BACK (button + returning to the correct owning tab), gear → Settings, Settings
  → Connections, `+` → Add Agent sheet, empty state with all agents hidden, and
  the window squeezed to its 640×480 minimum (nothing clipped; status strip
  reflowed; chip strip scrolls). Three of the four fixes above came out of that
  pass, not the build.
  - Environment restored afterwards: the seven `provider_hidden_*` prefs were
    written back to their exact prior values, my build was quit, and the user's
    `AI_tracker/AIUsageDashboard/build/dev` Tokei was relaunched.

**Files touched (13 — all under `UI/` + one new test file; no `Core/`, no
`App/`, no `project.yml`, no frozen contracts):**
`UI/Dashboard/DashboardTabBar.swift` (new), `UI/Dashboard/ProviderChipStrip.swift`
(new), `UI/Dashboard/DashboardView.swift`, `UI/Dashboard/AppSection.swift`,
`UI/Overview/OverviewView.swift`, `UI/Value/ValueView.swift`,
`UI/Charts/Cards.swift`, `UI/Components/ProviderVisibility.swift`,
`UI/Components/ProviderCard.swift` (deleted), `UI/MenuBar/MaxxerMath.swift`,
`UI/MenuBar/MenuBarLabel.swift`, `UI/Settings/SettingsView.swift`,
`UI/AddAgent/AddAgentSheet.swift`, `Tests/MaxxerTests/MaxxerChipStatTests.swift`
(new).

**Risks / watch-outs:**
1. `MaxxerMath.swift` grew again (chip stat + merged today). It remains the app's
   only `UI/` source compiled into `AIUsageDashboardCoreTests`, so it is still
   the only place testable UI logic can live without touching `project.yml`.
   Expect conflicts only in the appended sections.
2. `ProviderVisibility.visible()` changes what the Overview hero counts. Intended
   — but `viewModel.providerSplit` (the donut) comes from Core and may still
   include hidden agents. Worth a look in a Core-scoped WP; out of scope here.
3. The tab pills recompute `MaxxerValueEngine.scorecard` on each render, as the
   old Overview Value card did. The Value pane's data path is untouched. If the
   scorecard ever stops being cheap, memoise at the view-model layer.
4. `SidebarProviderRow`/`ProviderCard` are gone. Anything that assumed a
   provider row is 230pt wide or that hiding a provider only affects a sidebar
   should be re-read against the chip strip.
5. Two `.keyboardShortcut` additions: gear takes ⌘, (no prior owner — no
   `Settings` scene exists) and the back button takes Esc. Verified the Add
   Agent sheet still closes on Esc while presented.

### WP-5 — Claude Code (Opus) · padzy-os + frontend-design + staff-engineer — 2026-07-21

Visual redesign to the "Tokei Dashboard" mockup + wire every surface to real
data. Resumes the P0–P3 checkpoint (`203eed4`); this session did **P4–P8**.

- **Branch:** `patch/2026-07-19/ui-ia-consolidation` (base `203eed4`) — not merged, not pushed, no PR.
- **Commits (7):**
  - `94d3ef8` feat(ui): P4 — rebuild Value to the mockup (hero + insight + hairline rows)
  - `2d2c933` feat(ui): P5 — rebuild Agents tab as per-agent management cards
  - `7253d9c` feat(ui): P6 — unify provider drill-in (full + plan-only)
  - `79ec8e4` feat(ui): P7 — rebuild menu-bar popover to the mockup
  - `f9418b6` chore(ui): P8a — delete dead files, orphaned palette ramps, unused params
  - `94181cf` chore(ui): P8b — remove dead capabilityPane + orphaned helpers
  - `09fcfaa` refactor(ui): P8c — /simplify pass (safe localized wins)

**Done:**
- **P4 Value** — 84pt mono hero multiple, inline tier chip (real `MaxxerTier`,
  semantic good/warn colour, never accent), "$api from $plan across N agents"
  sentence, one ◆ accent insight line, clean hairline-row grid (tinted glyph ·
  name | `$plan → $api` with dotted underline when estimated | multiple). Rows
  drill; a priced-but-uncosted row jumps to Settings. #41 lifetime folded onto
  the excluded footnote. `onSelectProvider` wired at the DashboardView call site.
- **P5 Agents** — per-agent management cards, all REAL bindings: Show →
  `ProviderVisibility`; Watching+Rescan honest status (Rescan = refresh, no fake
  pause); LIVE QUOTA off/connecting/live via `.padzy` toggle on the 3
  connectable providers only (reuses `ProviderOverviewRow.connectableProviders/
  liveEnabledKey`), others read "LOCAL LOGS ONLY". Cursor's outbound-request
  disclosure kept verbatim; footer privacy note.
- **P6 Drill-in** — `ProviderDetailView` rebuilt as ONE unified surface for both
  full-metrics and plan-only providers (mockup L273-407): tinted header + route
  chip + plan label + confidence · meta grid · synthesized ◆ insight · circular
  gauge (tightest window) + pace verdict · KPI stats · plan-only PLAN & CREDITS
  section · quota windows with linear-burn pace markers · agent-tinted 30d
  history · today's token split. `DashboardView` now routes plan-only through it
  (capabilityPane retired); `LineTrendChart` gained an optional `tint`. Custom
  `FlowLayout` + width preference give the 2-col ≥720pt / 1-col / 640×480 reflow.
- **P7 Menu bar** — popover rebuilt: TOKENS·TODAY hero + Δ · Plan value (scorecard
  multiple + tier) · Tightest quota (dot + % + who) · top-3 agents · Open Tokei /
  Quit ⌘Q. `dismissPopover` / openWindow / auto-sync mechanics preserved.
  `MenuBarLabel` + `MaxxerMath` untouched (Round-2 contract).
- **P8 cleanup** — deleted `ProviderChipStrip`, `QuotaWindowRow`, `AreaTrendChart`,
  the whole dead `capabilityPane` + ~16 orphaned helpers, `openConnections`,
  `PadzyChartPalette.heatmapRamp/donutRamp`, and unused `thisWeek`/`heatmap`
  inputs (net ~900 lines deleted). Then a 4-angle `/simplify` review.

**Tests run + result:**
- Full gate: `xcodebuild -scheme AIUsageDashboardCore … test` → **302 tests, 0
  failures. TEST SUCCEEDED.** `xcodebuild -scheme AIUsageDashboardApp build` →
  **BUILD SUCCEEDED.** Each surface was also built-green before its commit.
- Core untouched all session — the 302 count is unchanged from baseline.

**Left undone / follow-ups (`/simplify` deferred — all safe to skip now):**
- Extract a shared `QuotaFillBar` + reset-countdown from
  `ProviderOverviewRow.fillBar` (drill-in copies the pace-notch/countdown; the
  fix touches shipped P3 code → deferred to avoid unverifiable visual drift).
- Move per-provider static facts (live-quota disclosure text, `connectableProviders`,
  `liveEnabledKey`, enable-online label) into `ProviderMetadata` so a provider is
  configured in one place instead of 4 switches across 3 files.
- Shared `.chipBorder` / `HairlinePill` (chip idiom in ~5 sites) and a
  `PadzyPrimaryButton` style (accent-fill button in 3 sites).
- Efficiency: `ValueView` reads `scorecard`/`rows` ~14×/~5× per render, and
  `DashboardView.providerDetailPane` rebuilds the full multi-provider scorecard
  once per second (countdown-timer re-render) to extract one provider — cache the
  scorecard per snapshots-revision (VM or `@State`) and thread it down. Applied
  only the localized menu-bar hoist this pass.

**Out of scope (need Core/feature work — NOT WP-5):** range set Today/Week/Month/
All (Core `UsageRange`), a functional accent-override store, a real `mark_gemini`
asset (gemini still falls back to the `sparkle` glyph).

**Risks / watch-outs:**
- **Visual verification is NOT done** — this session's environment has no macOS
  Screen Recording / Accessibility permission, so `screencapture` is black and the
  window can't be driven. Every surface is build-green and data-wired but has NOT
  been looked at. Launch this worktree's `Tokei.app` (DerivedData
  `AIUsageDashboard-dfmeddobpfrxvxeqtepfotxljyha`) and eyeball P4–P8 + the
  640×480 reflow before merge.
- P6 folded the drill's this-week/heatmap cards away (per the mockup) — if those
  are wanted back they'd return as new sections, not a revert.
- `capabilityPane` deletion changed how plan-only providers render (now the
  unified drill's PLAN & CREDITS path). Behaviourally equivalent, but visually new.
