# Patch Bible — 2026-07-09 (Round 3 · Visual Redesign)

> Single source of truth for the day's parallel engineering. Generated by `/morning-patch`.
> Consumed by each agent and by `/agents-done`. Do not delete — audit trail.
> Filename: `tasks/patch-bibles/2026-07-09-visual-redesign.md`.
> Round 1 = `2026-07-09.md` (shipped v0.3.0). Round 2 = `2026-07-09-token-maxxer.md` (merged to dev).
> This Round 3 executes the **approved** design spec:
> `docs/superpowers/specs/2026-07-09-visual-redesign-design.md` (already brainstormed with the user,
> driven by the same 5 inspiration mockups). Read that spec — it is the design authority; this Bible
> is the execution/orchestration layer over it, extended to also cover Phase 2 (Settings) and Phase 3
> (Menu-bar) which the user requested this round.

- **Base branch:** `dev` @ `9bfb778` (== main `dd6e0a8` + Round-2 token-maxxer merges). Clean, tests green.
- **Integration target:** `dev` · **Release target:** `main` (never auto-merged) · **Gate at merge:** `full`
- **Product:** Tokei (`ai.padzy.tokei`) — local-first macOS AI usage dashboard.
- **User decision this round:** execute via `/morning-patch`; scope = **everything (Phase 1a → 3)**.

**Theme override (per the design spec §6/§preamble):** this redesign INTENTIONALLY relaxes the strict
`aitracker` §5 constraints (radius ≤4px, one-accent-only, no gradients). The user explicitly chose the
richer look: rounded surface cards (radius 8/12), a **data-only** categorical chart palette, and ONE
sanctioned chart area-gradient. Pink `#FF3B70` stays the single *action/chrome* accent. See §5 here +
spec §2.

---

## 1. Selected work packages (prioritized)

Only **2 packages** — the canonical Tokei split (Core vs UI), fully disjoint file scopes → true
parallelism from the same base. UI is LOCKED to one dedicated Claude Code UI agent (standing rule);
splitting a design *system* across agents would fracture the visual language, so one UI agent owns the
whole look and lands it in reviewable commits (foundation → screens).

| # | Package | Scope | Agent | Depends on | Worktree branch |
|---|---------|-------|-------|-----------|-----------------|
| WP-1 | **Core analytics + hourly buckets + VM exposure** (spec §4 + §5) | `Core/Analytics/`, `Core/Parsing/*` (hourly bucketing), `Core/Models/ProviderSnapshot.swift` + `Core/Storage/ModelCodableExtensions.swift` (append-only `hourlyTotals`), `Core/Sync/DashboardViewModel.swift` (derived props — §4 frozen contract below), `Tests/` | **Codex** | — | `patch/2026-07-09/redesign-core-analytics` |
| WP-2 | **Full visual redesign UI** (spec §2/§3/§6 + Phase 2 Settings + Phase 3 Menu-bar) | `AIUsageDashboardApp/UI/**`, `AIUsageDashboardApp/Resources/**`, `project.yml` (new UI/Charts + assets sources only) | **Claude Code (Opus) + padzy-os** | WP-1 *only* for the Overview/Detail data-wiring step (see Waves) | `patch/2026-07-09/redesign-ui` |

## 2. Merge order, waves & rationale

The chart/component library, Settings, and Menu-bar are **Core-independent** (components take primitive
value types — spec §3 — and Settings/Menu-bar read existing snapshot/toggle state). Only the
Overview + Provider-Detail *analytics wiring* needs WP-1's new VM props. So:

- **Wave 1 (parallel, both start now):**
  - WP-1 builds all Core analytics + hourly + VM props + tests.
  - WP-2 builds design tokens, the entire `UI/Charts/` component library (preview-driven with sample
    value types), brand assets, and the Settings + Menu-bar redesigns — **all compiling independently**
    (no WP-1 symbols referenced yet). It also lays out Overview + Provider Detail using the components
    fed by **local preview/sample data**.
- **Merge WP-1 → dev first.**
- **Wave 2 (WP-2 continues, same worktree, rebased on updated dev):** swap the Overview + Provider-Detail
  sample feeds for the real frozen VM props (§4). This is the ONLY step that depends on WP-1. Commit,
  then merge WP-2 → dev.

Rationale: maximizes parallel wall-clock, keeps each side compiling/testing independently, and confines
the cross-package dependency to one small, well-specified wiring step against a frozen contract.

## 3. Per-package work

### WP-1 · Core analytics + hourly + VM exposure — Codex
- **Worktree:** `../tokei-worktrees/2026-07-09-redesign-core-analytics` (branch `patch/2026-07-09/redesign-core-analytics`)
- **Scope (in):**
  - `Core/Analytics/UsageAnalytics.swift` (NEW) — pure, synchronous, fully unit-tested derivations over
    existing `ProviderSnapshot` fields (`dailyTotals`, `todayUsage`, `week/month/lifetimeUsage`,
    `quotaWindows`). Functions per spec §4: `streak`, `bestDay`, `leastActiveDay`, `dailyAverage`,
    `peakDay`, `delta`, `providerSplit`, `rolling`. All return `nil`/empty honestly on insufficient history.
  - **Hourly (Phase 1b, spec §5):** add append-only `ProviderSnapshot.hourlyTotals: [Date: Int]?`
    (hour-truncated keys) + register it in `Core/Storage/ModelCodableExtensions.swift` **in the same commit**
    (frozen-contract rule §6). Each `LocalLogProvider` parser buckets records by hour alongside existing
    daily bucketing; plan-only providers (Antigravity) leave it nil. Store only the trailing window needed
    for the heatmap (≤14 days) to bound size. Add `UsageAnalytics.heatmapMatrix(hourlyTotals:) -> [[Int?]]`
    (7×24) and `peakHour(hourlyTotals:) -> (hour: Int, tokens: Int)?`.
  - **VM exposure** — add the §4 frozen derived properties/methods to `DashboardViewModel` (the ONLY
    Core file the UI reads for analytics). Views never call `UsageAnalytics` directly.
  - `UsageRange` enum (`sevenDay/thirtyDay/…`) + `DashboardViewModel.range` (get/set) driving the ranged props.
- **Scope (out):** ALL of `UI/**` and `Resources/**` (WP-2). Do not restyle anything. Do not change
  `QuotaWindow`/`TokenUsage`/`Utilization` shapes. Do not touch `Core/Network/*`.
- **Acceptance criteria:**
  - [ ] `UsageAnalytics` implemented per spec §4/§5 with full unit coverage (spec §8: streak gaps/today-boundary/all-zero/single-day; best/least ties+empty; delta zero-previous→nil; peak day; provider split excludes hidden providers; rolling windows; heatmap bucketing via a fixed calendar; peak hour).
  - [ ] `hourlyTotals` is append-only + Codable-registered same commit; parsers bucket by hour; size-bounded ≤14d; providers without timestamps omit it.
  - [ ] All §4 VM props exist with the exact names/types below and return real data (empty/nil honestly).
  - [ ] Existing Core tests stay green; no frozen contract broken.
- **Rollback:** revert merge commit; branch retained.

### WP-2 · Full visual redesign UI — Claude Code (Opus) + padzy-os
- **Worktree:** `../tokei-worktrees/2026-07-09-redesign-ui` (branch `patch/2026-07-09/redesign-ui`)
- **Run `padzy-os`** (aitracker theme, richer-look override §5). Land in reviewable commits:
  **(a) tokens + component library + assets → (b) Overview + Provider Detail (sample data) → (c) Settings
  → (d) Menu-bar → (e) Wave-2 wiring to real VM props.**
- **Scope (in):** `AIUsageDashboardApp/UI/**`, `AIUsageDashboardApp/Resources/**`, and `project.yml`
  ONLY to add new UI/Charts + asset sources (no target/scheme restructuring).
- **Scope (out):** ALL of `Core/**` (WP-1). Read `DashboardViewModel` published/derived props **read-only**;
  never edit `DashboardViewModel` or any Core file. Do not invent analytics in the View — consume the §4
  VM props (Wave 2). Preserve the Round-2 `MaxxerMath` pace/tightest/route logic (`UI/MenuBar/MaxxerMath.swift`)
  — **restyle** the menu-bar popover around it, don't delete the logic.
- **(a) Design tokens + component library** (spec §2/§3):
  - Extend `PadzyTheme` (do NOT fork): add `PadzyRadius` (card 12, control 8, pill 999) and
    `PadzyChartPalette` (input `#4C86FF` · output `#3DBE8B` · cacheRead `#A46BFF` · cacheWrite `#E8912D` ·
    deltaUp `#3DBE8B` · deltaDown `#FF4D4D` · heatmap ramp surface→`#FF3B70` 5 stops · donut ramp
    `#FF3B70→#7A1D35`). These hues appear ONLY inside charts/sparklines/deltas — never on buttons/nav/ticks.
  - `UI/Charts/` (NEW), each standalone + `#Preview`-driven + plain value-type inputs:
    `LineTrendChart([(Date,Int)])`, `AreaTrendChart`, `ProviderDonut([(ProviderID,Int)])`,
    `ActivityHeatmap([[Int?]])` (hand-rolled Canvas/grid; honest empty state when nil),
    `CircularGauge(Utilization)`, `MetricSparkline(tint:)` (refactor existing inline `Sparkline` here),
    `StatCard`/`SectionCard` rounded shells.
  - `Resources/Assets.xcassets/ProviderBrands/` per-provider marks + `ProviderBrandMark` view; fall back to
    existing monochrome `ProviderMark` if an asset is missing (never blank). Keep `ProviderMark` for dense/menu-bar.
- **(b) Overview** (mockup image 4) + **Provider Detail** (image 1): rebuild `UI/Overview/*`,
  `UI/ProviderDetail/*`, and the `UI/Dashboard/DashboardView.swift` shell to the mockups — rounded cards,
  hero total + INPUT/OUTPUT/CACHE tiles, token-usage-over-time line, provider donut, LIMITS bars +
  session gauge, THIS WEEK stats, activity heatmap, streak/best-day. Range selector ("Last 7 Days") sets
  `viewModel.range`. Per-metric hero sparklines are OUT of scope (data absent) — render a single
  total-tokens hero sparkline; heatmap/peak-hour show the empty state until WP-1's hourly lands (then real).
- **(c) Settings** (mockup image 2): 6-card grid — **Agents** (per-provider enable toggle + status +
  drag-to-reorder), **Data Sources** (per-provider path + format chip + Watching status + Rescan + "Add
  custom source"), **Limits & Alerts** (monthly limit, threshold OFF/90/80/70/CUSTOM, notify via
  Banner/Email/Both), **Appearance** (Dark/OLED/Light theme preview cards + accent color swatches),
  **Notifications** (daily summary / limit approaching / limit exceeded / sync issues / new features
  toggles), **Advanced** (data management, logs, keyboard shortcuts, reset). Config preset + Export config
  header. Wire toggles to existing settings state where it exists; render honest disabled/"coming soon"
  affordances for controls without a backing store yet (do NOT fake persistence).
- **(d) Menu-bar** (mockup image 5): restyle the popover — TOTAL TODAY hero + Δ vs yesterday + mini
  area sparkline, per-provider rows (brand mark · thin utilization bar · today value), footer
  "Last sync · OPEN DASHBOARD". Keep the compact status-bar label (Round-2 `MenuBarLabel`/display modes)
  and `MaxxerMath` logic; this is a visual pass on `UI/MenuBar/MenuBarView.swift` + the popover.
- **(e) Wave 2:** replace Overview/Detail sample feeds with the real §4 VM props. Compile + test on rebased dev.
- **Acceptance criteria:**
  - [ ] Overview + Provider Detail visually match the mockups (rounded cards, charts, heatmap, stats), all
        fed by REAL data via VM props (post-wiring); honest empty states where data is absent.
  - [ ] Settings matches image 2 (6-card grid, appearance, alerts); no faked persistence.
  - [ ] Menu-bar popover matches image 5; MaxxerMath logic preserved.
  - [ ] Chart palette confined to charts/sparklines/deltas; one accent for chrome/action (grep-guard §5).
  - [ ] App scheme builds; existing Core tests stay green.
- **Rollback:** revert merge commit; branch retained.

## 4. Frozen contract — `DashboardViewModel` analytics surface (WP-1 provides, WP-2 consumes)

WP-1 adds exactly these to `DashboardViewModel` (names/types are the contract; WP-2's Wave-2 wiring
targets them verbatim). All are read-only derived (computed from `snapshots` + `range` via `UsageAnalytics`):

```
// Ranged overview (all visible providers)
var range: UsageRange { get set }                         // drives everything below; default .sevenDay
var overviewTrend: [(date: Date, tokens: Int)]            // token-usage-over-time line
var providerSplit: [(provider: ProviderID, tokens: Int)]  // donut source (ranged, hidden excluded)
var overviewDelta: Double?                                // "↑18% vs yesterday" (nil if no baseline)
var streak: (current: Int, longest: Int)
var bestDay: (date: Date, tokens: Int)?
var leastActiveDay: (date: Date, tokens: Int)?
var dailyAverage: Int?

// Per provider (selected detail)
func trend(for id: ProviderID) -> [(date: Date, tokens: Int)]
func thisWeek(for id: ProviderID) -> (peakDayWeekday: Int, peakDayTokens: Int, dailyAverage: Int, delta: Double?)?
func heatmap(for id: ProviderID) -> [[Int?]]?             // 7×24, nil until hourly lands
func peakHour(for id: ProviderID) -> (hour: Int, tokens: Int)?
```

`UsageRange` (Core enum): `.sevenDay`, `.thirtyDay`, `.ninetyDay` (+ `.today` if trivial). Ranges beyond
available history clamp and the UI discloses ("only N days of data").

## 5. Design constraints (padzy-os · aitracker + richer-look override)

- Base tokens: ground `#131316` · surface `#1D1D22` · ink `#ECECF1` · muted `#6E6E78` · accent `#FF3B70`.
- **Override (this redesign only):** rounded surface cards (`PadzyRadius` card 12 / control 8), ONE
  sanctioned pink→transparent **area gradient under line charts** (charts only, never on cards/chrome),
  and the **data-only** `PadzyChartPalette` categorical hues. Everything else keeps the discipline: mono
  for all numerics, 1px hairlines, premium macOS hierarchy, real empty/loading/error on every surface.
- **One-accent rule preserved:** `#FF3B70` is the only chrome/action accent (buttons, nav active, ticks,
  primary CTA). Metric hues live ONLY inside chart/sparkline/delta components. Review grep-guards against
  `PadzyChartPalette` leaking into buttons/nav.
- UI LOCKED to the Claude Code UI agent (WP-2); Codex (WP-1) never touches `UI/**` or `Resources/**`.

## 6. Frozen contracts (all agents)

- `UsageProvider`/`QuotaProvider` protocols — signatures unchanged.
- `ProviderSnapshot` — **append-only**; `hourlyTotals` added WITH its `ModelCodableExtensions.swift`
  entry in the same commit (manual Codable).
- `QuotaWindow`/`TokenUsage`/`Utilization` shapes — frozen (read-only for both).
- `DashboardViewModel` — WP-1 owns edits (adds §4 surface); WP-2 reads only, never edits it or any Core file.
- Existing tests stay green; never delete/weaken. Fixtures = Swift string literals.
- No secrets outside Keychain. No UI → provider/network direct calls (UI ↔ view model only).

## 7. Gates

preflight (PASS) · worktree-sanity (per worktree) · lint/format (SKIP — tools absent) · build · test.
Per-commit hook: no-secret · no-large-artifact · format. At-merge (`/agents-done`): build + test full set.
Note: brand assets are small vector/PNG — keep each < 5MB (no-large-artifact gate).

## 8. Global rollback

Each package merges to `dev` as one `--no-ff` commit → `git revert -m 1 <sha>`. `dev` disposable; `main`
is the safety net; nothing reaches `main` without `/dev-approved`. Worktrees/branches retained until ship.

## 9. Completion log (agents append; `/agents-done` reads)

<!-- ### WP-N — <agent> — <time>
- Branch/commits: … | Done: … | Left/stubbed: … | Tests+result: … | Files: … | Risks: … -->

### WP-1 — Codex — 2026-07-09 18:41 PT
- Branch/commits: `patch/2026-07-09/redesign-core-analytics` @ `a078e5a` (`feat: add core usage analytics`)
- Done: added pure `UsageAnalytics`; append-only `ProviderSnapshot.hourlyTotals` + manual Codable; 14-day hourly bucketing through timestamped parsers/providers; frozen `DashboardViewModel` analytics surface + `UsageRange` cases.
- Left/stubbed: none.
- Tests+result: `cd AIUsageDashboard && xcodegen generate && xcodebuild -project AIUsageDashboard.xcodeproj -scheme AIUsageDashboardCore -destination 'platform=macOS' test` → PASS, 214 tests.
- Files: `Core/Analytics`, `Core/Models`, `Core/Parsing`, `Core/Providers`, `Core/Storage`, `Core/Sync`, `Tests`.
- Risks: VM hidden-provider filtering mirrors the existing `provider_hidden_<rawValue>` user-default key from UI without importing UI.

### WP-2 (Wave 1, steps a–d) — Claude Code UI agent (Opus) — 2026-07-09 18:43 PDT
- **Branch/commits:** `patch/2026-07-09/redesign-ui` — `f63be01` (a: PadzyRadius + PadzyChartPalette + UI/Charts/{LineTrendChart, AreaTrendChart, ProviderDonut, ActivityHeatmap, CircularGauge, MetricSparkline, Cards(StatCard/SectionCard/DeltaLabel)} + ProviderBrands asset slots + ProviderBrandMark) → `dd5a9de` (b: Overview mockup-4 + Provider Detail mockup-1 rebuild; DashboardView routes full-metrics providers to the new detail, plan-only/empty/error panes untouched) → `828767c` (c: Settings 6-card grid, mockup 2) → `6256692` (d: menu-bar popover, mockup 5).
- **Done:** All Wave-1 scope. Real data everywhere it already exists: hero TODAY totals/metric tiles (merged `todayUsage`), LIMITS bars + session `CircularGauge` (`quotaWindows`), detail usage-trend line (`snapshot.dailyTotals`, capped 30d), provider quota rows (utilization spine + MaxxerMath pace/route preserved), Settings toggles (visibility, quota alerts, menu-bar mode, Rescan, Sparkle update), popover TOTAL TODAY + Δ + 7d sparkline + per-provider bars (`dailyTotals`/utilization). Analytics widgets (overview trend/donut/streak/patterns, detail hero-delta/THIS WEEK) ride `OverviewAnalyticsFeed`/`DetailAnalyticsFeed` — value structs mirroring §4 field-for-field, every sample-fed card visibly tagged `SAMPLE`. Heatmap + peak-hour render honest empty states (1b gate). Store-less Settings controls render inert `SOON` chips — zero faked persistence.
- **Left/stubbed for Wave 2 (step e — awaiting WP-1 merge to dev + rebase):** swap `.sample`/local aggregations for the §4 VM props verbatim (`overviewTrend/providerSplit/overviewDelta/streak/bestDay/leastActiveDay/dailyAverage`, `trend(for:)/thisWeek(for:)/heatmap(for:)/peakHour(for:)`, `range` ← Overview's local 7D/30D/90D selector), delete `SampleChip`s and the two `NOTE(Wave 2)` aggregations in `MenuBarView`. ProviderBrands imagesets ship as empty drop-in slots → brand-tinted `ProviderMark` chip fallback renders (never blank); real art can land later without code changes.
- **Tests+result:** `xcodegen generate` ✓ · App scheme Debug build ✓ (at each of the 4 commits) · Core scheme test ✓ 196/196 on this branch · palette grep-guard ✓ (`PadzyChartPalette` only in `UI/Charts/*` + `PadzyTheme.swift`; chrome accent stays `#FF3B70`). #Previews cover empty/partial/full per component + blank-canvas/narrow-640 per surface.
- **Files:** `UI/Charts/*` (new), `UI/Components/{PadzyTheme, AnalyticsFeed(new), ProviderBrandMark(new), ProviderCard, TokeiMark}`, `UI/Overview/*`, `UI/ProviderDetail/ProviderDetailView`, `UI/Dashboard/DashboardView`, `UI/Settings/SettingsView`, `UI/MenuBar/MenuBarView` (MaxxerMath/MenuBarLabel untouched), `Resources/Assets.xcassets/ProviderBrands/*`. `project.yml` needed NO edit (path-based sources already include UI/Charts + assets).
- **Risks:** Wave-2 seam concentrated in `AnalyticsFeed.swift` construction sites + 2 marked popover helpers; sample numbers visible in dev builds until then (mitigated by SAMPLE chips). Settings Appearance accent-swatch previews use raw metric hexes as inert disabled previews (not `PadzyChartPalette` symbols, not actions) — flag if the grep-guard should also ban raw hex.

### WP-2 (Wave 2, step e) — Claude Code UI agent (Opus) — 2026-07-09 18:52 PDT
- **Branch/commits:** `patch/2026-07-09/redesign-ui` — `1f429ce` (merge dev @ WP-1, clean, no conflicts) → `1c6f00a` (Wave-2 wiring). Branch is 6 commits ahead of dev, worktree clean, ready for `/agents-done`.
- **Done:** every sample feed replaced with the frozen §4 `DashboardViewModel` surface VERBATIM — Overview reads `overviewTrend/providerSplit/overviewDelta/streak/bestDay/leastActiveDay/dailyAverage` and binds the 7D/30D/90D selector to `viewModel.range`; `DashboardView` passes `trend(for:)/thisWeek(for:)/heatmap(for:)/peakHour(for:)` into `ProviderDetailView` as plain values; menu-bar hero Δ + sparkline read `overviewDelta`/`overviewTrend`. Heatmap + peak-hour render REAL `hourlyTotals`; empty state kept only when `heatmap(for:)` is nil. `OverviewAnalyticsFeed`/`DetailAnalyticsFeed`/`SampleChip` deleted (`AnalyticsFeed.swift` → `AnalyticsFormat.swift`, pure formatting). No Core file touched.
- **Judgment calls (review):** (1) §4 exposes no per-provider "vs yesterday", so the Detail hero's placeholder delta was dropped — the real comparison lives in THIS WEEK's vs-last-7-days stat. (2) `overviewDelta` compares current vs PREVIOUS window of the same length, so delta captions are range-aware (`vs prev 7 days`, …) instead of the mockup's literal "vs yesterday" (true only for `.today`). (3) Overview's heatmap = element-wise union (sum; nil only where no provider reports) of visible providers' §4 matrices — §4 only defines per-provider heatmaps.
- **Tests+result:** `xcodegen generate` ✓ · App scheme Debug build ✓ · Core scheme test ✓ **214/214** (incl. WP-1's analytics + hourly suites) · palette grep-guard ✓ (`PadzyChartPalette` confined to `UI/Charts/*` + `PadzyTheme.swift`).
- **Files:** `UI/Overview/OverviewView`, `UI/ProviderDetail/ProviderDetailView`, `UI/Dashboard/DashboardView`, `UI/MenuBar/MenuBarView`, `UI/Components/AnalyticsFeed.swift`(deleted)/`AnalyticsFormat.swift`(new).
- **Risks:** menu-bar sparkline/Δ now follow `viewModel.range` (shared published state with the dashboard selector) — at 90D the popover mini-trend plots 90 points; visually fine, flag if the popover should pin `.sevenDay`. ProviderBrands art slots still empty (tinted-chip fallback renders).

### UI-polish round — Claude Code UI agent (Opus) — 2026-07-09 20:35 PT
- **Branch/commits:** `patch/2026-07-09/ui-polish` (worktree `../tokei-worktrees/2026-07-09-ui-polish`, based on the antigravity-detail-redesign merge) — `80871d5` (monochrome marks) → `c227efe` (plan-only pane re-redo) → `78130fa` (dynamic menu-bar icon). Worktree clean; NOT merged/pushed per instructions.
- **#2 Monochrome provider logos (`80871d5`):** `ProviderBrandMark` repointed to ONE monochrome treatment — the `ProviderMark` template glyph (ink) on a neutral hairline chip (`ink.opacity(0.06)` fill, muted stroke). Brand-hue map, `colorMultiply` tint, and the color-asset lookup removed; all 6 call sites (Dashboard, OverviewRow, ProviderDetail, Settings, ProviderCard, MenuBar popover) and sizes untouched. Disabled contexts keep muted `ProviderMark`. `ProviderBrands` empty asset slots left in place (unused, harmless).
- **#1 Plan-only detail pane (`c227efe`):** no reference image was pasted; per Abhay's instruction it now matches the full-detail (mockup-1) language: Plan hero card with display-34 plan name + `StatCard` tiles (Credits used `X / Y`, Accepted lines) + credits `CircularGauge`; quota rows restyled from 1px hairline to the detail LIMITS idiom (threshold-colored 6px rounded fill via `ProviderOverviewRow.thresholdColor`, RESETS countdown, `!!` critical, `ConfidenceBadge`) with mono group kickers for Antigravity's model groups and a trailing tightest-window gauge; Token-usage card gets the `NOT MEASURED LOCALLY` headline — still never a fake 0 hero. Added `#Preview`s: Antigravity full quota groups + off/empty states.
- **#3 Dynamic menu-bar icon (`78130fa`):** new `UI/MenuBar/TokeiStatusIcon.swift`; `TokeiMark` refactored to expose `barPaths(in:)` (combined shape unchanged). Ladder from `MaxxerMath.tightestWindow(viewModel.utilization).usedPercent`: idle(nil/0) · low<40 · med<70 · high<90 · nearLimit<100 · limitReached≥100; bars fill in quarters (any burn ≥1 bar), template-mono through medium, accent-colored from high, small flame lick on the tallest bar at nearLimit/limitReached (verified via rendered contact sheet on light+dark strips — first flame draft read as a spike, shrunk to 3.4×4pt). Circular accent spinner (8 cached frames, 0.15s timer, static under Reduce Motion) trails the value while `isLoading`. Fixed 17×14 canvas + per-state `NSImage` cache so the item never shifts width; `MenuBarLabel` uses it in all 3 display modes. `MaxxerMath` untouched.
- **Tests+result:** per commit — `xcodegen generate` ✓ · App Debug build ✓ · Core scheme test ✓ (214) · palette grep-guard ✓ (`PadzyChartPalette` still only `UI/Charts/*` + `PadzyTheme.swift`; status-icon accent is state, sanctioned per task brief).
- **Risks:** nearLimit vs limitReached render identically (both 4 bars + flame) — % text disambiguates; flag if a distinct ≥100 treatment is wanted. Menu-bar spinner timer publishes every 0.15s even when idle (guarded no-op; negligible). Icon idle state = all bars at 0.3 alpha — quiet by design, flag if too faint.
