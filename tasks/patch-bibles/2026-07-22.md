# Patch Bible — 2026-07-22

> Single source of truth for the day's parallel engineering. Generated by
> `/morning-patch`. Consumed by each agent and by `/agents-done`. Do not delete;
> this is the audit trail. Filename: `tasks/patch-bibles/YYYY-MM-DD.md`.

- **Base branch:** `dev` @ `58cf736`
- **Integration target:** `dev`
- **Release target:** `main` (never merged automatically)
- **Gate mode at merge:** `full` (release gates run at `/dev-approved`)
- **Product:** Tokei (`ai.padzy.tokei`) — local-first macOS AI usage dashboard

**Today's driver (from user):** a P0 "app overheats the system" issue + a front-end
design update, both high priority; then clear a few backlog issues.

**Prep already landed on `dev` this run (verified, pre-worktree):**
- `000f393` — `fix(claude)`: Claude parser incremental per-file cache (the Claude
  half of the P0 overheat, 306 ParserTests green). Uncommitted-but-verified work
  from a prior session; committed so WP-1 branches from a base that already has the
  pattern it must mirror.
- `58cf736` — `docs(patch)`: WP-1 spec (`tasks/todo.md`) + WP-2 reference
  (`tasks/website-craft-research.md`).

---

## 1. Selected issues (prioritized)

Priority score = user-impact × release-value ÷ risk, tie-broken by dependency
order then parallelization potential.

| # | Issue / source | Why it matters | Impact | Risk | Depends on | Agent | Worktree branch |
|---|----------------|----------------|--------|------|-----------|-------|-----------------|
| WP-1 | **#55** Codex parser full re-parse every sync (P0 overheat, Codex half) | Codex re-parses ~93MB logs each sync ≈ 11% CPU → sustained heat. Second half of the P0. | **H** | **M** | prep `000f393` (mirrors Claude cache) | **Codex** | `patch/2026-07-22/codex-parser-cache` |
| WP-2 | **#10** Website: version + Awwwards rebuild (front-end update) | User's on-disk website edits were git-ignored/unversioned; land under VC then push the narrative/texture rebuild. | **H** | **M** | website baseline `bc9fd31` | **Claude Code (Opus) + padzy-os** | `rebuild/awwwards` (separate `website/` repo) |
| WP-3 | **#49** Cursor cooldown file not scoped to cookie/endpoint | A 429 on one cookie fast-fails requests on a different cookie until expiry. Bounded correctness fix. | **M** | **L** | — | **Codex** | `patch/2026-07-22/cursor-cooldown-scope` |
| WP-4 | **#51** Plan-cost presets + best-effort plan auto-detection | Cuts manual per-provider $ entry; presets picker + badged detection. Core + Settings UI feature. Also owns the **Gemini empty-state UI hint** from #54 (UI is Claude-locked). | **M** | **M** | — | **Claude Code (Sonnet) + padzy-os** | `patch/2026-07-22/plan-cost-presets` |
| WP-5 | **#54** Gemini connector: document one-time CLI sign-in | Gemini shows "not signed in" when `~/.gemini/oauth_creds.json` absent (correct, undocumented). Docs-only. | **L** | **L** | — | **Kimi K2.7** | `patch/2026-07-22/gemini-signin-docs` |

Not selected today (deferred): #48 QuotaSeriesStore per-series retention, remaining
token-maxxer/gamification epics (#20/#23/#24/#25), #36/#37 pace/route chips. Reason:
keep review tractable and file-scopes disjoint from the P0 parser work.

## 2. Merge order & rationale

`/agents-done` merges the Swift worktrees into `dev`. WP-2 is a **separate repo**
(`website/`) and never merges into `dev` — it ships via its own git + Vercel.

1. `patch/2026-07-22/codex-parser-cache` (WP-1) — P0; land first, it's the day's headline.
2. `patch/2026-07-22/cursor-cooldown-scope` (WP-3) — independent Core/Network; sole owner of `CursorUsageClient.swift`.
3. `patch/2026-07-22/gemini-signin-docs` (WP-5) — docs-only; zero code collision.
4. `patch/2026-07-22/plan-cost-presets` (WP-4) — last: may *read* Cursor's plan-label API that WP-3 owns; merging after WP-3 avoids a base skew. No shared edited files.

WP-2 (`rebuild/awwwards`) — merges to website `master` on its own cadence; independent of the above.

## 3. Per-issue work packages

### WP-1 · Codex parser incremental cache (#55) — Codex
- **Worktree:** `../tokei-worktrees/2026-07-22-codex-parser-cache`  (branch `patch/2026-07-22/codex-parser-cache`)
- **Scope (in):** `AIUsageDashboardApp/Core/Parsing/CodexJSONLParser.swift`, `Tests/ParserTests/CodexJSONLParserTests.swift` (new). May add a `CodexJSONLParser+Streaming.swift` if a resumable `parseFile(startingAtByte:)` is cleaner as an extension.
- **Scope (out):** everything else. **Do NOT touch** `ClaudeJSONLParser*.swift` (done), `UsageWindows.swift` (frozen this run — additive only if unavoidable, see §4), any UI, any other provider.
- **Files likely involved:** `Core/Parsing/CodexJSONLParser.swift` (588 lines), new tests. Reference implementation to mirror: `Core/Parsing/ClaudeJSONLParser.swift` + `ClaudeJSONLParser+Streaming.swift` (already on `dev` @ `000f393`).
- **Spec:** `tasks/todo.md` (full plan) — resumable `parseFile` via `FileHandle`+byte offset; `fileCache: [String: FileCacheEntry]` on the actor; per-file `FileAggregate` (dailyUsage, hourlyTotals, delta sum, per-session latest cumulative total, latest rate-limits); unchanged file (mtime+size) → reuse; grown → resume+merge; rotated/truncated/first-sync → full parse; combine step replays cached aggregates. `detectLatestModel` stays untouched (separate read-only pass).
- **The two "latest-wins" fields are why Codex ≠ Claude:** `finalTotalsBySession` (latest cumulative per `sessionID ?? path`, timestamp-compared) and `latestRateLimits` (single global latest). Cache a per-file "latest so far" for each and compare across files at combine time — not a pure additive sum.
- **Acceptance criteria:**
  - [ ] Steady-state sync (no file growth) does **zero** full re-reads: unchanged files served from cache (mtime+size match).
  - [ ] Grown file resumes from cached byte offset; output byte-identical to a full re-parse of the same final file (assert equal `AggregateUsage`).
  - [ ] Rotated/truncated file (size < cached offset) falls back to full parse, replaces cache entry.
  - [ ] `deltaReportedTotalTokens`, `finalReportedTotalTokens` (per-session latest summed), and the global latest rate-limit snapshot all match the pre-change full-parse values on the same inputs.
  - [ ] Measured: post-first-sync Codex CPU contribution drops from ~11% toward ~0% on steady-state syncs (note the before/after Activity Monitor sample in §8).
- **Test requirements:**
  - [ ] `CodexJSONLParserTests` incremental-append test mirroring `testIncrementalParseResumesAppendedLogs` (append lines between two parses → equals full parse).
  - [ ] Rotation/truncation test (shrink file → full re-parse path).
  - [ ] Multi-file "latest wins" test: latest per-session total and latest rate-limit picked correctly across ≥2 cached files.
  - [ ] Full `AIUsageDashboardCore` suite stays green (currently 306).
- **Design / UX constraints:** none (Core only).
- **Known risks:** "latest-wins" fields miscombined after a resume (double-count or stale) → mitigation: cache the per-file latest, re-derive the global latest at combine time, cover with the multi-file test. FileHandle offset drift on partial last line → mitigation: mirror the Claude offset handling (record `offsetInFile` after the last complete line).
- **Rollback:** revert merge commit (single `--no-ff`); branch retained.

### WP-2 · Website — version + Awwwards rebuild (#10) — Claude Code (Opus) + padzy-os
- **Repo:** `website/` — now its own git repo. Baseline `bc9fd31` on `master`; **work on branch `rebuild/awwwards`** (already created). This is NOT a Swift worktree and never merges to `dev`.
- **Scope (in):** `website/` only — `app/`, `components/`, `lib/`, `public/`, config. Next.js App Router + Tailwind v4 + the already-installed Lenis + GSAP + SplitText.
- **Scope (out):** the Swift app, `AIUsageDashboard/`, `tasks/`, any `../tokei-worktrees/*`. Never un-ignore `website/` in the parent repo's `.gitignore` (it stays tracked independently, per #10).
- **Reference:** `tasks/website-craft-research.md` (teardown of experientiallabs.ai + mireye.com; the stack; a ranked technique catalog). Near-monochrome, one accent, mono details, oversized display type, heavy whitespace, motion + narrative not decoration — "one narrative pass + one texture pass away."
- **Acceptance criteria:**
  - [ ] `pnpm build` (or `npm run build`) succeeds clean; no console errors on the built page.
  - [ ] The rebuild lands as small, reviewable commits on `rebuild/awwwards` (baseline `master` stays untouched as the safe point).
  - [ ] `prefers-reduced-motion` respected — Lenis/GSAP scroll + reveals disabled/settled for reduced-motion and touch.
  - [ ] Keeps Tokei brand DNA: one accent `#FF3B70` on key words only, mono UI labels, editorial framing; no generic-SaaS gradient-card look.
  - [ ] Website Tailwind `source()` scoping preserved — must keep `source(none)` + scoped `@source` or dev 500s on repo-root ANSI logs (known trap).
- **Test requirements:** build-green + a local `pnpm dev` visual pass (desktop + a 375px mobile check). No unit-test harness on the site.
- **Design / UX constraints:** **run `padzy-os` (aitracker) + `frontend-design`.** Padzy taste is the whole point here.
- **Known risks:** unversioned edits could still be lost before first push → mitigation: baseline already committed (`bc9fd31`); commit early and often. Motion perf regressions → animate transform/opacity/clip-path only; Lenis desktop-only via `matchMedia`.
- **Rollback:** `git reset --hard master` in `website/` returns to the safe baseline.

### WP-3 · Cursor cooldown per-cookie scoping (#49) — Codex
- **Worktree:** `../tokei-worktrees/2026-07-22-cursor-cooldown-scope`  (branch `patch/2026-07-22/cursor-cooldown-scope`)
- **Scope (in):** `AIUsageDashboardApp/Core/Network/CursorUsageClient.swift` (the persisted cooldown around line 72) + a focused unit test. **Sole owner** of this file this run.
- **Scope (out):** everything else. Don't touch `CursorSession.swift` public API, other providers, UI.
- **Problem:** `cursor-usage-cooldown.json` is a single global file checked before every request; a 429 on one account/cookie fast-fails a request on a different cookie until it expires.
- **Acceptance criteria:**
  - [ ] Cooldown persistence keyed per cookie/endpoint (e.g. a stable hash of the cookie/account) so one cookie's 429 never blocks a different cookie.
  - [ ] Backward compatible: a pre-existing global cooldown file is either migrated or ignored gracefully (no crash, no stale global block after upgrade).
  - [ ] Raw cookie value never written to disk or logs (hash only).
- **Test requirements:**
  - [ ] Unit test: cookie A in cooldown → cookie B still allowed; cookie A blocked until expiry.
  - [ ] Malformed/legacy cooldown file → treated as no cooldown, no crash.
  - [ ] Core suite stays green.
- **Design / UX constraints:** none (Core only).
- **Known risks:** cookie-hash instability across launches → mitigation: hash the durable account/cookie identifier already used for the ln filenames (see `CursorSession` per-account file scoping), not a volatile session token.
- **Rollback:** revert merge commit; branch retained.

### WP-4 · Plan-cost presets + auto-detection (#51) — Claude Code (Sonnet) + padzy-os
- **Worktree:** `../tokei-worktrees/2026-07-22-plan-cost-presets`  (branch `patch/2026-07-22/plan-cost-presets`)
- **Scope (in):** `AIUsageDashboardApp/Core/Maxxer/MaxxerPlanCostStore.swift`, `Core/Maxxer/MaxxerValueEngine.swift` (read/consume only if needed), `AIUsageDashboardApp/UI/Settings/SettingsDrawer.swift` (plan-cost section), a small new `PlanPreset`/detection helper under `Core/Maxxer/`, plus the **Gemini provider empty-state UI hint** (from #54) wherever provider empty states render in `UI/`.
- **Scope (out):** `CursorUsageClient.swift` (WP-3 owns it — you may **read** its existing public plan-label API but must not edit it), parsing, other providers' clients, README/docs (WP-5 owns docs).
- **Feature (from #51):**
  - Presets: a picker next to the existing USD field offering known per-provider plans (Claude Max $100 / Pro $20, Codex $20, Cursor $20, Cline $10, Antigravity $5, …). Selecting a preset fills the price; user can always override.
  - Best-effort detection (badged "detected", never silently trusted, never overwrites a user-entered value): start with **Cursor** — `usage-summary` already returns a plan label ("Pro (active)") → map label → preset price. Others (Claude/Gemini/Antigravity/Cline/Codex) are research stubs — wire a clean detection seam, only implement what's cheap and local; leave unknowns `nil` (never `$0`).
  - Multi-account note: Tokei models a single `claude_code` provider — multi-account plan cost stays an open modeling question; do the single-plan case, leave a TODO, don't invent a schema.
- **Acceptance criteria:**
  - [ ] Preset picker renders in the Settings plan-cost section next to the USD field; picking a preset prefills the price; manual override always wins and persists.
  - [ ] Detection never overwrites a user-entered value; detected values are visually badged; unknown plan → `nil`, never `$0`.
  - [ ] Gemini provider empty state shows the one-time sign-in hint ("run `gemini` and sign in to enable Gemini quota").
  - [ ] `MaxxerPlanCostStore` public API stays additive (frozen-contract §4); existing value-engine math unchanged for already-set costs.
- **Test requirements:**
  - [ ] Preset→price mapping unit test; detection-never-overwrites-user-value test; unknown→nil (not 0) test.
  - [ ] Core suite stays green; App scheme builds.
- **Design / UX constraints:** **run `padzy-os` (aitracker) + `frontend-design`.** Settings is a 380px right drawer (existing `DrawerScaffold`). Reuse `PadzyTheme`/`PadzyToggle`/`SectionLabel`; mono for prices; accent = state only; badges subtle; real empty/loading states. Respect `accessibilityReduceMotion`.
- **Known risks:** detection reading a provider client could tempt an edit into WP-3's file → mitigation: read-only via existing API; if the API is insufficient, note it in §8 and stub detection rather than editing `CursorUsageClient.swift`.
- **Rollback:** revert merge commit; branch retained.

### WP-5 · Gemini sign-in docs (#54) — Kimi K2.7
- **Worktree:** `../tokei-worktrees/2026-07-22-gemini-signin-docs`  (branch `patch/2026-07-22/gemini-signin-docs`)
- **Scope (in):** `README.md` + `AIUsageDashboard/docs/` only (docs). Document, don't code.
- **Scope (out):** all Swift source (the Gemini UI empty-state hint is WP-4's, not yours), UI, tests.
- **Content:**
  - README + docs note: Gemini quota requires a one-time CLI sign-in — run `gemini` and sign in; the connector reads `~/.gemini/oauth_creds.json`. Absent file → "not signed in" is correct behaviour.
  - Document the recovery path: an expired Gemini token **cannot** auto-refresh because the gemini-cli OAuth **client secret is intentionally not bundled** (no-secret gate) — the fix is to re-run `gemini` to refresh. State this as the expected, supported recovery.
- **Acceptance criteria:**
  - [ ] README has a clear "Enable Gemini" step + the refresh-recovery note.
  - [ ] Wording matches the real error strings in `Core/Network/GeminiUsageClient.swift` (read them; don't paraphrase loosely).
  - [ ] No code/tests changed; docs-only diff.
- **Test requirements:** none (docs). Pre-commit gates must still pass.
- **Design / UX constraints:** none.
- **Known risks:** doc drifts from real behaviour → mitigation: quote the actual client error messages.
- **Rollback:** revert merge commit; branch retained.

## 4. Frozen contracts (all agents, non-negotiable)

- `UsageProvider` protocol — do not change signatures.
- `UsageStore` public API — additive only.
- `ProviderSnapshot` fields — **append-only**; any new stored field MUST be added
  to `Core/Storage/ModelCodableExtensions.swift` in the SAME commit (manual Codable).
- **`UsageWindows.swift` is frozen this run** — WP-1 must reuse the existing
  `accumulate(_:timestamp:dailyTotal:includeInLifetime:)` API added at `000f393`;
  do not change its signature (Claude parser depends on it).
- **`MaxxerPlanCostStore` public API — additive only** (WP-4).
- **`CursorUsageClient.swift` — sole editor is WP-3.** WP-4 may read its public API only.
- Existing tests stay green — never delete or weaken them (currently 306).
- Test fixtures = Swift string literals in `Tests/Fixtures/Fixtures.swift` (no resource files).
- No secrets outside Keychain. Raw provider responses = debug-only. Cursor cookie → hash, never persisted/logged raw.
- No UI → provider direct calls (UI talks to the view model/sync engine only).

## 5. Design / UX constraints (Padzy OS "aitracker")

- ground `#131316` · surface `#1D1D22` · ink `#ECECF1` · muted `#6E6E78` · ONE accent `#FF3B70`.
- Accent = state/action ONLY (never decoration; cost values use ink, not accent).
- Mono for all numeric/data. No shadows, no gradients, no rounded card grids, radius ≤ 4px.
- Premium macOS feel, clean hierarchy. Every surface needs real empty / loading / error states.
- UI is LOCKED to Claude Code UI agents scoped to `AIUsageDashboardApp/UI/` (WP-4) — they run
  the `padzy-os` skill (aitracker theme). UI is never baked into a core package and never
  assigned to a non-Claude agent. WP-2 (website) also runs `padzy-os`, applied to the Next.js
  surface (padzy-os covers web UI). External agents (Codex/Kimi) never "run the skill" and
  hold no UI scope this run.
- App design system already in place — reuse, don't re-derive: `PadzyTheme` (dark ramp,
  `accent #FF3B70`, `good/warn/danger`, `quotaColor`), `PadzyToggle`, `SectionLabel`,
  `DrawerScaffold` (380px right drawer), `Font.mono`/`Font.sans`, `PadzyMotion` gated on
  `accessibilityReduceMotion`.

## 6. Gates (created / verified this run)

- **Preflight:** PASS on `dev`.
- **Worktree-sanity:** PASS on all 4 Swift worktrees.
- **Test:** PASS — `AIUsageDashboardCore` (306 tests) on `dev` @ `58cf736` (includes the Claude cache fix).
- **Lint / format:** SKIP cleanly (swiftlint / swiftformat not installed).
- **no-secret · no-large-artifact:** PASS on both prep commits + the website baseline (pre-commit hook).
- Fast set (no-secret, no-large-artifact, format) runs per-commit via the pre-commit hook
  installed in each worktree; full build+test runs at merge (`/agents-done`); release/strict
  set at `/dev-approved`.

## 7. Global rollback plan

- Each Swift work package merges to `dev` as a single `--no-ff` commit → revert is one `git revert -m 1 <sha>`.
- Prep commits `000f393` / `58cf736` are already on `dev`; if the Claude cache regresses, `git revert 000f393` (self-contained).
- Website: `website/` is its own repo; `git reset --hard master` (baseline `bc9fd31`) restores the safe snapshot; nothing about the site touches the app repo.
- `dev` is disposable; `main` is the safety net. Nothing reaches `main` without `/dev-approved`.
- Worktrees and `patch/*` branches are retained until the day's patch ships, then pruned.

## 8. Completion log (agents append; `/agents-done` reads)

<!-- Each agent appends on finish:
### WP-N — {{agent}} — {{time}}
- Branch/commits: <sha …>
- Done: …
- Left undone / stubbed: …
- Tests run + result: …
- Files touched: …
- Risks / watch-outs: …
-->

### WP-2 · Website Awwwards rebuild (#10) — Claude Code (Opus) + padzy-os — 2026-07-22

- **Repo/branch:** `website/` (separate repo). Baseline `master @ bc9fd31` left
  untouched as the safe point. Work on `rebuild/awwwards`. **Not pushed / not
  deployed** (awaiting user go-ahead).
- **Commits (4, small & reviewable):**
  - `4538ad4` style(texture): strip decorative accent glow, let structure carry
  - `1dfab3d` style(chart): flat monochrome area fill, drop the accent gradient
  - `2197ecb` feat(narrative): editorial corner frame + accent on key words
  - `1590086` fix(hero): keep the corner frame from overflowing on mobile
- **Done — a narrative pass + a texture pass on the existing Next.js/Tailwind/
  Lenis/GSAP/SplitText site (not a rewrite):**
  - *Texture discipline (the big one).* Accent was spilled as decoration all
    over the page, which breaks the core invariant (one accent, never
    decoration). Removed the hero aurora, the three repeated section glows
    (value/coverage/privacy), the twin payoff blooms (→ one restrained bloom),
    the accent-halo shadow on the app window, and the chart's accent gradient
    area fill (→ flat 4% ink). Replaced the live-dot's animated `box-shadow`
    pulse (also a motion-rule violation) with a hairline ring that expands on
    transform/opacity only — the one sanctioned "live state" loop. Film grain
    kept. Net effect: near-monochrome carried by hairlines + whitespace + type,
    so the finale's single accent bloom finally *means* something.
  - *Narrative.* Editorial corner framing on the hero (experientiallabs lesson):
    mono labels anchor the bottom corners (local-first credit · scroll · build
    stat). Scarce accent on key words (mireye lesson): one accent word in the
    hero ("tokens") and the finale ("counting") — never a whole line. Pulled the
    decorative accent off the four feature eyebrows and the marquee separators so
    accent now only marks a key word, genuine live/active state, or the one CTA.
- **Build:** `pnpm build` clean on all four commits (Next 16.2.10 + Turbopack,
  TypeScript passes, 8/8 static pages). Zero console errors on the built page
  (checked live via the browser console reader).
- **Visual pass:** desktop (1440) walked end-to-end live — hero, app-reveal,
  pinned horizontal features, value ladder, coverage, privacy, finale — all
  clean, disciplined accent, no glow. Mobile: the environment's window would not
  render below 1440 (`resize_window` reported success but `innerWidth` stayed
  1440), so 375px was verified by construction instead: no horizontal overflow at
  1440, all sections are single-column stacks below their breakpoints, and the
  hero corner-frame labels + heading words were **measured against the loaded
  faces** to fit a 375px viewport (frame cols 81/60px ≤ 155px; hero words
  176/226px ≤ 327px). The corner-frame overflow that this surfaced was fixed in
  `1590086`.
- **Left undone / stubbed (future passes, deliberately not attempted — kept this
  to a focused narrative+texture pass, not a rewrite):** the research doc's
  larger phases beyond what already exists — preloader→hero *continuous* handoff
  (loader currently slides up as a separate beat), DrawSVG line-draw connectors
  in "how it works", clip-path panel wipes. Three components are unused in the
  tree (`scramble-text`, `live-gauge`, `count-up`) — left in place (out of
  touch-scope; harmless, don't affect the build).
- **Constraints honored:** Tailwind `source(none)` + scoped `@source` untouched
  (no repo-root ANSI 500s). Animate transform/opacity/clip-path only. Lenis
  desktop-only via matchMedia; every GSAP tween gated on
  `prefers-reduced-motion: no-preference`; reduced-motion + the new live-ring
  settle to final/static state. Parent `.gitignore` not touched. Nothing outside
  `website/` edited except this Bible note.
- **Risks / watch-outs:** SplitText wraps the accent `<span>` in the hero/finale
  headings — verified it renders pink live and, because RevealHeading is
  SSR-visible, the accent word shows even with JS/motion off. `resize_window` is
  unreliable in this environment (silently clamps to 1440) — a future 375px
  *pixel* check needs a real device or DevTools emulation.
- **Rollback:** `git reset --hard master` in `website/` restores `bc9fd31`.

### WP-3 — Codex — 2026-07-22 20:22 PT
- Branch/commits: `patch/2026-07-22/cursor-cooldown-scope` @ `4b2a182` (on top of per-cookie scoping already on `dev` via `89462b1` / #49)
- Done: Cooldown files keyed by SHA-256 of durable WorkOS `userId` (not full cookie/JWT) via `CursorSession.identityHash`; cookie-A cooldown never gates cookie-B; legacy global `cursor-usage-cooldown.json` still honored then removed; malformed per-cookie/legacy files → no cooldown, no crash; raw cookie never on disk/logs (hash only).
- Left undone / stubbed: none for WP-3. Did not change `CursorSession` public API.
- Tests run + result: `xcodegen generate` + `xcodebuild … -scheme AIUsageDashboardCore … test` → **308 tests, 0 failures** (baseline was 306; +2 new client tests).
- Files touched: `AIUsageDashboardApp/Core/Network/CursorUsageClient.swift`, `Tests/ProviderTests/CursorUsageClientTests.swift`
- Risks / watch-outs: users who already have per-cookie cooldown files hashed from the *full* cookie (post-`89462b1`) will get a new filename after this commit (old files orphan harmlessly until TTL/manual cleanup); active legacy global file still briefly gates all cookies until it expires (intentional one-time migration).

### WP-1 — Codex — 2026-07-22 20:35 PT
- Branch/commits: `patch/2026-07-22/codex-parser-cache` @ `99b38d8`
- Done: added a per-file Codex aggregate cache with unchanged-file reuse, byte-offset append resume, and full replacement for truncation/rotation. Cached aggregates retain additive delta usage plus per-file session-final and rate-limit latest-wins state, which is re-derived globally at combine time. Resume validates inode and the trailing 4 KiB before the cached offset; incomplete trailing JSON remains pending for the next append. Added coverage for unchanged files, incremental appends, incomplete records, truncation, atomic and same-inode larger rotations, interior blank lines, and multi-file latest-wins parity.
- Left undone / stubbed: no code is stubbed. A post-change Activity Monitor sample was not available in the test runner; manual QA must capture it.
- Tests run + result: `xcodegen generate`; focused `CodexJSONLParserTests` → **20 tests, 0 failures**; `xcodebuild -project AIUsageDashboard.xcodeproj -scheme AIUsageDashboardCore -destination 'platform=macOS' test` → **314 tests, 0 failures**. Pre-commit no-secret and no-large-artifact gates passed; format gate skipped because `swiftformat` is not installed.
- Files touched: `AIUsageDashboardApp/Core/Parsing/CodexJSONLParser.swift`, new `AIUsageDashboardApp/Core/Parsing/CodexJSONLParser+Streaming.swift`, `Tests/ParserTests/CodexJSONLParserTests.swift`
- Risks / watch-outs: the cache is intentionally in-memory, so the first sync after app launch still performs a full parse. Incomplete-JSON detection relies on Foundation parser diagnostics and is regression-tested. The 4 KiB continuity tail makes same-inode rewrite detection probabilistic rather than cryptographic, while avoiding a full reread on growth.
- CPU (Activity Monitor): before **~11%** Codex parser contribution (reported issue baseline); after **not sampled in this agent environment**. The steady-state path is regression-tested to perform no content read when mtime, size, and inode match; manual QA should record the after sample before release.
