# Patch Bible — 2026-07-12

> Single source of truth for the day's parallel engineering. Generated by
> `/morning-patch`. Consumed by each agent and by `/agents-done`. Do not delete;
> this is the audit trail. Filename: `tasks/patch-bibles/2026-07-12.md`.

- **Base branch:** `dev` @ 3f460b9
- **Integration target:** `dev`
- **Release target:** `main` (never merged automatically)
- **Gate mode at merge:** `full` (release gates run at `/dev-approved`)
- **Product:** Tokei (`ai.padzy.tokei`) — local-first macOS AI usage dashboard
- **Baseline:** tests GREEN (`AIUsageDashboardCore` passes); tree clean; lint/format SKIP (swiftlint/swiftformat not installed).

Source of this wave: fast repo scan (2026-07-12) + competitive read of
github.com/mm7894215/TokenTracker. Track B (Maxxer social/flex-card layer) is
**parked at the design gate** — not in this wave.

---

## 1. Selected issues (prioritized)

Priority score = user-impact × release-value ÷ risk, tie-broken by dependency
order then parallelization potential.

| # | Issue / source | Why it matters | Impact | Risk | Depends on | Agent | Worktree branch |
|---|----------------|----------------|--------|------|-----------|-------|-----------------|
| 1 | Scan #1 — menu-bar spinner timer battery bug | Always-on menu-bar app wakes `.main` ~6.6×/sec for its whole lifetime even when idle → battery drain. Verified. | H | L | — | Claude Code (Sonnet) | `patch/2026-07-12/menubar-spinner-battery` |
| 2 | Steal — LiteLLM live pricing service | Pricing is a frozen 2026-07-06 static snapshot (~15 models). The value-engine / token-maxxer numbers the whole product sells depend on it. Live table + cache + seed fallback prices 100s of models and stays current. | H | M | — | Codex | `patch/2026-07-12/litellm-pricing` |
| 3 | #26 (Gemini half) — Gemini connector | Biggest coverage gap vs TokenTracker; exact endpoints now known (de-risked). New provider, self-contained. | M | M | — | Cursor | `patch/2026-07-12/gemini-connector` |
| 4 | #45 + scan #2/#3/#6 — SQLite WAL helper + CooldownStore dedup | Closes filed tech-debt; de-dups DB-copy loop across 3 parsers and Retry-After/cooldown logic across 2 clients; stops silent cooldown-write loss (repeat 429s). | M | L | — | Kimi K2.7 | `patch/2026-07-12/sqlite-cooldown-dedup` |

**Selection rationale:** all four are unblocked and mutually file-disjoint, so
they parallelize with zero coordination. Copilot connector, DashboardView split,
appcast Intel/arm64, and Info.plist version literals were considered and
**deferred** (see §9) to keep the wave ≤4 and review tractable.

## 2. Merge order & rationale

All four are independent (disjoint file scopes); relative order is free. Recommended
integration order is low-risk-first so `dev` stays green as work lands:

1. `patch/2026-07-12/menubar-spinner-battery` — trivial, isolated UI file.
2. `patch/2026-07-12/sqlite-cooldown-dedup` — behavior-preserving refactor, no new surface.
3. `patch/2026-07-12/litellm-pricing` — new service, additive.
4. `patch/2026-07-12/gemini-connector` — new provider, additive.

## 3. Per-issue work packages

### WP-1 · Menu-bar spinner battery fix — Claude Code (Sonnet)
- **Worktree:** `../tokei-worktrees/2026-07-12-menubar-spinner-battery` (branch `patch/2026-07-12/menubar-spinner-battery`)
- **Scope (in):** `AIUsageDashboardApp/UI/MenuBar/MenuBarLabel.swift` ONLY.
- **Scope (out):** everything else. Do not touch the view model, other UI, or Core.
- **Files likely involved:** `UI/MenuBar/MenuBarLabel.swift`.
- **The bug:** `private let spinnerTimer = Timer.publish(every: 0.15, on: .main, in: .common).autoconnect()` (line 25) fires ~6.6×/sec on the main runloop for the entire app lifetime; `.onReceive` (line 72) only early-returns when `!isLoading` — the timer still wakes `.main`. On an always-running menu-bar item this is continuous battery drain.
- **Fix intent:** the spinner tick must exist ONLY while `viewModel.isLoading`. Drive `spinnerPhase` from a source that is not subscribed when idle (e.g. gate/recreate the publisher on `isLoading`, or a `TimelineView(.animation)`/`.task` loop scoped to the loading branch). No perpetual autoconnected timer. Preserve current behavior: animates during sync, static frame under Reduce Motion, honors existing look.
- **Acceptance criteria:**
  - [ ] Zero timer/publisher wakeups when `!viewModel.isLoading` (verifiable from the code: nothing is subscribed/ticking at idle).
  - [ ] Spinner still animates during an active sync; static frame when Reduce Motion is on.
  - [ ] No visual regression to the menu-bar label (mark + value + spacing unchanged).
- **Test requirements:**
  - [ ] If phase-advance logic is extractable to a pure function, add a unit test for it. Otherwise state in the completion note why it's view-only and confirm existing tests stay green.
- **Design / UX constraints (UI):** Padzy "aitracker" theme — see §5. Run `padzy-os`.
- **Known risks:** SwiftUI timer lifecycle subtlety → mitigation: keep the change minimal and local; verify the loading→idle→loading transition still animates.
- **Rollback:** revert the merge commit; branch retained.

### WP-2 · LiteLLM live pricing service — Codex
- **Worktree:** `../tokei-worktrees/2026-07-12-litellm-pricing` (branch `patch/2026-07-12/litellm-pricing`)
- **Scope (in):** `AIUsageDashboardApp/Core/Providers/` — new `PricingService.swift`; refactor `CodexPricing.swift` (becomes the bundled seed / curated-override source); wire `CodexProvider.swift` to consume the service. Tests under `Tests/`.
- **Scope (out):** all other providers, all `Core/Network`, `Core/Parsing`, UI, `ProviderRegistry.swift`. Do NOT edit `Core/Network/*UsageClient.swift` (WP-4 owns those).
- **Files likely involved:** `Core/Providers/PricingService.swift` (new), `Core/Providers/CodexPricing.swift`, `Core/Providers/CodexProvider.swift`, `Tests/PricingServiceTests.swift` (new), `Tests/Fixtures/PricingFixtures.swift` (new).
- **What:** replace the frozen static price table with a resilient service following TokenTracker's proven chain:
  1. Fetch `https://raw.githubusercontent.com/BerriAI/litellm/main/model_prices_and_context_window.json` (async, timeout ~15s, never blocks UI).
  2. **24h disk cache** (mtime-based) in the app's caches dir; slim each row to 4 fields: `input_cost_per_token`, `output_cost_per_token`, `cache_read_input_token_cost`, `cache_creation_input_token_cost`.
  3. On network failure → serve stale disk cache; if none → **bundled seed snapshot** (fold the current `CodexPricing` table in as the seed).
  4. Lookup order per model: **curated-overrides → LiteLLM table → negative-cache (zero)**. Unknown model = zero, cached, no repeated lookups.
- **Acceptance criteria:**
  - [ ] Prices resolve for many models (gpt-5*, claude-*, etc.) from the live table, not just the ~15 hardcoded rows.
  - [ ] Offline / fetch-fail path: uses disk cache, then bundled seed — no crash, no hang.
  - [ ] Unknown model → zero cost, cached; no crash.
  - [ ] `CodexProvider` cost output unchanged for models already in the old table (no regression).
- **Test requirements:**
  - [ ] Unit-test the fallback chain: parse a live-JSON fixture slice → cache hit → seed fallback. Inject the clock (no real `Date()`), inject the fetcher (no live network in tests).
  - [ ] Fixtures as Swift string literals in a NEW `Tests/Fixtures/PricingFixtures.swift` (do NOT edit the shared `Fixtures.swift` — see §4).
- **Known risks:** network fetch in-app must be async + timeout + off the UI path; cache path must be per-app-container; bundled seed must ship in the target. Mitigation: seed is a Swift-embedded constant (no external resource file needed).
- **Rollback:** revert the merge commit; branch retained.

### WP-3 · Gemini connector — Cursor
- **Worktree:** `../tokei-worktrees/2026-07-12-gemini-connector` (branch `patch/2026-07-12/gemini-connector`)
- **Scope (in):** new `Core/Network/GeminiUsageClient.swift`, new `Core/Providers/GeminiProvider.swift`; register in `Core/Providers/ProviderRegistry.swift`. Tests under `Tests/`.
- **Scope (out):** every other provider/client/parser, pricing, UI. Do NOT touch `CodexProvider`, `CodexPricing`, `*UsageClient.swift` (Claude/Cursor), or the DB parsers.
- **Files likely involved:** `Core/Network/GeminiUsageClient.swift` (new), `Core/Providers/GeminiProvider.swift` (new), `Core/Providers/ProviderRegistry.swift`, `Tests/GeminiProviderTests.swift` (new), `Tests/Fixtures/GeminiFixtures.swift` (new).
- **What:** new provider surfacing Gemini CLI quota.
  - Read OAuth creds from `~/.gemini/oauth_creds.json` (has `expiry_date`). Read-only; never persist tokens outside the CLI's own file / Keychain.
  - Refresh when expired via `POST https://oauth2.googleapis.com/token` using the gemini-cli OAuth client creds (bundled public fallback client id from the open-source CLI — see risk).
  - Quota via `POST https://cloudcode-pa.googleapis.com/v1internal:loadCodeAssist` then `:retrieveUserQuota`. Surface quota windows (`used_percent`, reset). Confidence `.providerReported`.
- **Acceptance criteria:**
  - [ ] Gemini shows up as a provider; when gemini-cli is authed on the machine, displays live quota %/reset.
  - [ ] gemini-cli absent / not authed → clean empty state (a `ProviderWarning`, never a crash).
  - [ ] Expired token → refresh succeeds and the call retries.
- **Test requirements:**
  - [ ] Parse `loadCodeAssist` / `retrieveUserQuota` response fixtures (string literals in NEW `Tests/Fixtures/GeminiFixtures.swift`).
  - [ ] Token-expiry decision tested with an injected clock (fixed date), not real `Date()`.
- **Known risks:**
  - Bundling the gemini-cli fallback OAuth **client id** — it is a public identifier from an open-source CLI (low risk), but NEVER commit a real client *secret*; the no-secret gate will block it. **[needs-decision surfaced to Abhay: OK to bundle the public client id?]**
  - Undocumented Google internal endpoints can change → isolate behind the client; degrade to empty state on any non-200.
- **Rollback:** revert the merge commit; branch retained.

### WP-4 · SQLite WAL helper + CooldownStore dedup — Kimi K2.7
- **Worktree:** `../tokei-worktrees/2026-07-12-sqlite-cooldown-dedup` (branch `patch/2026-07-12/sqlite-cooldown-dedup`)
- **Scope (in):**
  - `Core/Parsing/CursorStateDBParser.swift`, `Core/Parsing/OpencodeStoreParser.swift`, `Core/Parsing/AntigravityStateDBParser.swift` + new `Core/Parsing/SQLiteSidecarCopy.swift`.
  - `Core/Network/ClaudeUsageClient.swift`, `Core/Network/CursorUsageClient.swift` + new `Core/Network/CooldownStore.swift`.
  - Tests under `Tests/`.
- **Scope (out):** connectors (Gemini), pricing, UI, `ProviderRegistry`, `CodexProvider`, `CodexPricing`, `GeminiUsageClient`, `MenuBarLabel`. Touch ONLY the six files named above (+ the two new helpers + tests).
- **Files likely involved:** the six above + `SQLiteSidecarCopy.swift` (new), `CooldownStore.swift` (new), `Tests/CooldownStoreTests.swift` (new), `Tests/DayBucketingTests.swift` (new).
- **What:**
  1. **WAL helper:** extract the identical `-wal`/`-shm` sidecar-copy-before-read loop (currently duplicated verbatim in the 3 DB parsers at ~`CursorStateDBParser:108`, `OpencodeStoreParser:174`, `AntigravityStateDBParser:76`) into one helper; the 3 parsers call it. Copy semantics identical (copy `-wal`/`-shm` next to the temp main-db copy before opening read-only).
  2. **CooldownStore:** extract the duplicated RFC-1123 `Retry-After` `DateFormatter` + `persistCooldown`/`readCooldown` + backoff constants (`ClaudeUsageClient:528`, `CursorUsageClient:164`) into one shared store; both clients use it.
  3. **Fix scan #6:** `try? persistCooldown(...)` currently swallows write failures (`CursorUsageClient:108`, `ClaudeUsageClient:281`) → a lost backoff = repeat 429s across relaunch. Log on failure instead of silently dropping.
  4. **Add scan #7/#9 test:** a day-bucketing edge test around UTC-instant → `Calendar.current` `startOfDay` (midnight boundary), with an injected fixed date. Test-only; no behavior change to bucketing.
- **Frozen-contract note:** pure refactor — external behavior identical, no signature changes, all existing tests stay green.
- **Acceptance criteria:**
  - [ ] All 3 DB parsers call ONE sidecar-copy helper; no duplicated copy loop remains.
  - [ ] Both network clients use ONE `CooldownStore`; no duplicated Retry-After/cooldown logic remains.
  - [ ] Cooldown write failures are logged, not silently swallowed.
  - [ ] New tests: cooldown persist/read round-trip; day-bucketing midnight-edge. All existing tests green.
- **Test requirements:** add tests in NEW files (`CooldownStoreTests.swift`, `DayBucketingTests.swift`); fixtures as string literals in a NEW fixtures file, not the shared `Fixtures.swift` (§4).
- **Known risks:** refactor must be behavior-preserving; verify WAL copy order and cooldown persistence semantics byte-for-byte unchanged. Mitigation: keep existing tests as the regression net; add round-trip tests.
- **Rollback:** revert the merge commit; branch retained.

## 4. Frozen contracts (all agents, non-negotiable)

- `UsageProvider` protocol — do not change signatures.
- `UsageStore` public API — additive only.
- `ProviderSnapshot` fields — **append-only**; any new stored field MUST be added
  to `Core/Storage/ModelCodableExtensions.swift` in the SAME commit (manual Codable).
- Existing tests stay green — never delete or weaken them.
- **Test fixtures = Swift string literals** (no resource files). To avoid concurrent
  merge conflicts, EACH package adds its fixtures to a NEW per-package file
  (`Tests/Fixtures/<Package>Fixtures.swift`) — **do NOT edit the shared
  `Tests/Fixtures/Fixtures.swift` this wave.**
- **Do NOT hand-edit `project.yml`.** Sources are globbed by directory
  (`path: AIUsageDashboardApp/Core`, `path: AIUsageDashboardApp` excl. `Core`), so
  new `.swift` files are auto-included. Run `cd AIUsageDashboard && xcodegen generate`.
- No secrets outside Keychain. Never commit an OAuth client **secret** (no-secret gate
  blocks it). Raw provider responses = debug-only.
- No UI → provider direct calls (UI talks to the view model/sync engine only).
- File-scope isolation: never edit a file another WP owns (see each WP's scope).

## 5. Design / UX constraints (Padzy OS "aitracker")

- ground `#131316` · surface `#1D1D22` · ink `#ECECF1` · muted `#6E6E78` · ONE accent `#FF3B70`.
- Accent = state/action ONLY (never decoration; cost values use ink, not accent).
- Mono for all numeric/data. No shadows, no gradients, no rounded card grids, radius ≤ 4px.
- Premium macOS feel, clean hierarchy. Every surface needs real empty / loading / error states.
- UI is LOCKED to the dedicated Claude Code UI agent (WP-1) scoped to `AIUsageDashboardApp/UI/`;
  it runs the `padzy-os` skill (aitracker theme). No non-Claude agent is assigned a UI package.
  Non-UI agents (WP-2/3/4) touch no views; the tokens above are a guard only.

## 6. Gates (created / verified this run)

preflight (PASS) · worktree-sanity (per worktree, below) · lint (SKIP) · format (SKIP) ·
build · test (baseline PASS) · no-secret · no-large-artifact · no-uncommitted.
Fast set runs per-commit via the installed pre-commit hook (no-secret, no-large-artifact,
format); full set (build, test) runs at merge (`/agents-done`); release/strict set at
`/dev-approved`.

## 7. Global rollback plan

- Each work package merges to `dev` as a single `--no-ff` commit → revert is one
  `git revert -m 1 <sha>`.
- `dev` is disposable; `main` is the safety net. Nothing reaches `main` without `/dev-approved`.
- Worktrees and `patch/2026-07-12/*` branches are retained until the wave ships, then pruned.

## 9. Deferred this wave (considered, not selected)

- **Copilot connector (#26 other half)** — reads another app's encrypted `auth.db`,
  AES-256-GCM key from macOS Keychain. Security-sensitive + fragile → its own Claude Code
  (Opus) package + `/security-review`, not a rushed 4-pack. Next wave.
- **DashboardView split (987 LOC)** — internal-only, no user-visible value, high merge-risk
  against future card UI. Do when the card work touches that area.
- **Appcast arm64-only vs `minimumSystemVersion=14.0` (Intel gap) + single-`<item>` history** —
  release-infra; handle in `/dev-approved` release hygiene.
- **Info.plist dead version literals (`1.0`/`1`) → `$(MARKETING_VERSION)`** — cosmetic; fold
  into a release-hygiene commit.

## 8. Completion log (agents append; `/agents-done` reads)

<!-- Each agent appends on finish:
### WP-N — {{agent}} — {{time}}
- Branch/commits: <sha …>
- Done: …
- Left undone / stubbed: …
- Tests run + result: …
- Files touched: …
- Risks / watch-outs: …
-->

### WP-1 — Claude Code — 2026-07-12 19:31 PDT
- Branch/commits: `patch/2026-07-12/menubar-spinner-battery` @ `a6fa257`
- Done: Removed the perpetual `spinnerTimer = Timer.publish(every: 0.15, …).autoconnect()` stored property and the `@State spinnerPhase` + `.onReceive` from `MenuBarLabel`. The spinner is now driven by a `TimelineView(.periodic(from:.now, by: 0.15))` scoped INSIDE the `if viewModel.isLoading` branch of `syncSpinner`, so the ticking source exists only while a sync is in flight and unschedules the instant loading ends — nothing is subscribed at idle (acceptance criterion "verifiable from the code" met). Reduce Motion renders a single static `spinnerImage(phase: 0)` with no ticking source. Phase derived from the timeline clock via a pure `spinnerPhase(at:)` = `Int(date.timeIntervalSinceReferenceDate / 0.15)`; `TokeiStatusIcon.spinnerImage` already normalizes the index. Cadence/look/spacing preserved (HStack spacing 5, mark + value + trailing spinner unchanged).
- Left undone / stubbed: nothing.
- Tests run + result: `xcodegen generate` (regen ok); `xcodebuild -scheme AIUsageDashboardCore … test` → **215 tests, 0 failures** (baseline green preserved); `xcodebuild -scheme AIUsageDashboardApp … build` → **BUILD SUCCEEDED** (compiles the changed App-target file + validates `TimelineView(.periodic)` on macOS 14). Pure phase math verified via throwaway Swift script: 8 distinct frames, 15 consecutive +1 advances over 16 ticks, within-bucket stable, +1 per 0.15s bucket — identical to the old `(phase+1) % 8` timer.
- Why no in-suite unit test: `spinnerPhase(at:)` is a pure, testable function, but it lives in the App-target `MenuBarLabel.swift`. The `AIUsageDashboardCoreTests` bundle only compiles `Tests/` + the single bridged UI file `MaxxerMath.swift`; wiring a second UI file in requires editing `project.yml` (frozen contract §4) and moving the function to `MaxxerMath.swift` is out of WP-1 scope. So it stays view-only; verified out-of-band via the script above.
- Files touched: `AIUsageDashboard/AIUsageDashboardApp/UI/MenuBar/MenuBarLabel.swift` only (+24 / −8).
- Risks / watch-outs: TimelineView appears/disappears with `viewModel.isLoading` re-rendering `body` — the pre-existing `if viewModel.isLoading` visibility dependency already relied on that, so no new reactivity assumption. The animated branch redraws every 0.15s during sync (same rate as before, now bounded to the loading window). Starting frame is time-derived (any of 0–7) rather than always 0 — visually indistinguishable on a rotating ring, no regression.

### WP-4 — Kimi K2.7 — 2026-07-12 19:39 PDT
- Branch/commits: `patch/2026-07-12/sqlite-cooldown-dedup` @ `d720f65`
- Done: Pure behavior-preserving dedup + robustness, no signature changes.
  1. **SQLite WAL helper:** extracted the identical `-wal`/`-shm` sidecar-copy-before-read loop out of the 3 DB parsers into one `Core/Parsing/SQLiteSidecarCopy.copyDatabase(from:to:using:)`. All 3 parsers' private `copyDatabase(from:to:)` now delegate to it (a thin one-liner, preserving their two internal call sites each); no duplicated copy loop remains anywhere but the single helper.
  2. **CooldownStore:** extracted the duplicated RFC-1123 `Retry-After` `DateFormatter` + cooldown persist/read + backoff constants (`defaultCooldownInterval`, `maxCooldownInterval`) from `ClaudeUsageClient` + `CursorUsageClient` into one shared `Core/Network/CooldownStore` (`Sendable` struct). Both clients hold a `CooldownStore` and call `isActive(at:)` / `retryAfter(from:)` / `record(duration:)`. On-disk cooldown JSON is byte-identical (same `.iso8601` + `[.prettyPrinted,.sortedKeys]`, same `{ "until": … }` shape), so existing cooldown files keep loading and the client test that asserts the cooldown file is written still passes. Removed now-dead client code: `retryAfter`/`cooldownActive`/`isCooldownActive`/`persistCooldown`/`readCooldown` methods, the `Cooldown`/`CursorUsageCooldown` structs, Cursor's now-unused `cursorUsageEncoder`/`cursorUsageDecoder`, and the moved constants/formatter. `maxAttempts`/`maxRetrySleepInterval` stay client-local (retry-loop control, not cooldown state).
  3. **Fix (scan #6):** `try? persistCooldown(...)` in both clients (which silently dropped a backoff → repeat 429s across relaunch) replaced by `cooldownStore.record(duration:)`, which logs the write failure via `os.Logger(subsystem: "ai.padzy.tokei", category: "CooldownStore")` and returns — never throws/crashes the triggering fetch.
  4. **Test (scan #7/#9):** added a `UsageWindows` day-bucketing midnight-edge test with an injected fixed date + pinned calendar timezone (deterministic on any CI tz).
- Left undone / stubbed: nothing. Introduced the app's first `os.Logger` use (no prior logging facility in Core) — minimal, SDK-only, no new dependency.
- Tests run + result: `xcodegen generate` (regen ok); `xcodebuild -scheme AIUsageDashboardCore … test` → **230 tests, 0 failures** (215 baseline preserved + 15 new: 13 `CooldownStoreTests`, 2 `DayBucketingTests`). Confirmed the 215 baseline green BEFORE editing, then after. One self-inflicted red during dev (a wrong test assertion about `isActive` semantics — production `until > referenceDate` is correct and unchanged) was fixed; final run fully green.
- Files touched: `Core/Network/ClaudeUsageClient.swift`, `Core/Network/CursorUsageClient.swift`, `Core/Network/CooldownStore.swift` (new), `Core/Parsing/CursorStateDBParser.swift`, `Core/Parsing/OpencodeStoreParser.swift`, `Core/Parsing/AntigravityStateDBParser.swift`, `Core/Parsing/SQLiteSidecarCopy.swift` (new), `Tests/ClaudeUsageTests/CooldownStoreTests.swift` (new), `Tests/ParserTests/DayBucketingTests.swift` (new). 9 files, +382 / −144. No `project.yml` hand-edit (globbed; `xcodegen` picks up new files). No shared `Fixtures.swift` edit (fixtures are string literals in the new test files).
- Risks / watch-outs: Behavior-preserving refactor — the regression net is the untouched existing client/parser tests (all still green). The only intended behavior change is cooldown-write failures now log instead of silently dropping. `os.Logger` static is `Sendable`; the relocated `DateFormatter` static matches the pre-existing pattern (warning-level under strict-concurrency `complete`, same as before, non-fatal). Cooldown file format verified byte-compatible so no migration needed across upgrade.

### WP-3 — Cursor — 19:49 PDT
- Branch / commits: `patch/2026-07-12/gemini-connector` @ `3a54ccd` (base `dev` @ `3f460b9`). Two commits: `b0694cd` feat(gemini) — client + provider + registry + `ProviderID.gemini`; `3a54ccd` test(gemini) — tests + fixtures. Committed in-worktree only; NOT merged/pushed/PR'd.
- Done:
  - New `Core/Network/GeminiUsageClient.swift`: reads `~/.gemini/oauth_creds.json` **read-only** (never persists tokens); clock-injectable expiry (`GeminiOAuthCredentials.isExpired(now:skew:)`, `expiry_date` in ms); refresh via `POST https://oauth2.googleapis.com/token`; quota via `POST cloudcode-pa …:loadCodeAssist` → `:retrieveUserQuota`. **Reuses the existing `QuotaProvider` contract** (which was defined but unused) instead of inventing a parallel protocol. Windows carry used%/remaining/reset, confidence `.providerReported`, source `"gemini-cli"`. Decoder tolerant of both per-bucket `remainingFraction` (0–1) and explicit `usedPercent` (0–100). Any non-200/unrecognized → typed `GeminiUsageError`; a 401 mid-flight forces one refresh + retry.
  - New `Core/Providers/GeminiProvider.swift`: `id = .gemini`, file-presence detection; **always** resolves to a clean snapshot — creds absent → `.unauthenticated` + info warning; quota error → empty windows + a mapped `ProviderWarning`. Never throws, never crashes.
  - Registered `GeminiProvider()` in `ProviderRegistry.default()`; added `ProviderID.gemini = "gemini"` (append-only; `ProviderID: Codable` is rawValue-based → decode-safe for old snapshots).
- Left undone / stubbed:
  - **[needs-decision — CLIENT ID]**: bundled the gemini-cli **public** OAuth client id as `GeminiOAuthConfig.publicClientID` (public identifier from the Apache-2.0 CLI, not a secret; no-secret gate PASS). Flagged per §3 for Abhay's OK.
  - **[needs-decision — CLIENT SECRET]**: the refresh grant also needs the CLI's OAuth **secret**, deliberately NOT bundled (frozen contract). `GeminiOAuthConfig.clientSecret == nil`; the refresh path is fully implemented + tested (tests inject a secret) but is inert in shipped builds, so an **expired** token degrades to an info warning ("run `gemini` to refresh"). A still-valid token works end-to-end today. Decision needed: ship a public secret / read it from the CLI's own store / keep the degrade.
  - **No privacy toggle**: unlike Antigravity (`antigravityOnlineQuotaEnabled`), Gemini fetches whenever authed. A toggle needs `connectableProviders` + `ProviderOverviewRow` (UI) — out of WP-3 scope. Follow-up.
- ⚠️ **INTEGRATION-REQUIRED (blocks `/agents-done` full APP build)**: adding `ProviderID.gemini` breaks **3 exhaustive `switch`es in the App/UI target** (no `default`). The **Core** scheme (the WP-3 verify target) is fully green, but `AIUsageDashboardApp` will not compile until a `.gemini` case is added to each. UI is out of WP-3 scope ("touch no UI"), so I did NOT edit them. One-liners for the UI agent / integration:
  - `AIUsageDashboardApp/UI/Charts/ProviderDonut.swift` (~L47): `case .gemini: return "Gemini"`
  - `AIUsageDashboardApp/UI/Components/ProviderMark.swift` (~L28): `case .gemini: return "mark_gemini"` (also add a `mark_gemini` asset under `Assets.xcassets/ProviderMarks/`, else it falls back to the SF Symbol)
  - `AIUsageDashboardApp/UI/Components/ProviderMetadata.swift` (~L66): `case .gemini: return ["~/.gemini/oauth_creds.json"]`
  - (`UI/Overview/ProviderOverviewRow.swift` has a `default:` → already safe.)
- Tests run + result: `cd AIUsageDashboard && xcodegen generate` (ok) → `xcodebuild -scheme AIUsageDashboardCore -destination 'platform=macOS' test` → **233 tests, 0 failures, 0 warnings** (215 baseline preserved + 18 new: 12 `GeminiUsageClientTests`, 6 `GeminiProviderTests`). Verified the app-target break separately with `-scheme AIUsageDashboardApp build` (fails on the switches above — expected, documented). `no-secret` gate PASS on staged diff; pre-commit hook PASS on both commits.
- Files touched: `Core/Models/ProviderID.swift`, `Core/Providers/ProviderRegistry.swift`, `Core/Network/GeminiUsageClient.swift` (new), `Core/Providers/GeminiProvider.swift` (new), `Tests/Fixtures/GeminiFixtures.swift` (new), `Tests/ProviderTests/GeminiUsageClientTests.swift` (new), `Tests/ProviderTests/GeminiProviderTests.swift` (new). No `project.yml` hand-edit (globbed; ran `xcodegen`). Shared `Tests/Fixtures/Fixtures.swift` untouched.
- Risks / watch-outs:
  - **Unverified live quota schema**: no gemini-cli was installed/authed on this machine and the repo's "gemini" research is all Antigravity model-group naming, so the `retrieveUserQuota` shape is a best-effort design modeled on the **verified** 2026-07-06 cloudcode-pa capture (per-bucket `remainingFraction`). If Google's real shape differs, the isolated decoder returns empty → graceful degrade, but live quota % may not populate until confirmed on a real authed Mac. Recommend a one-time capture to lock the contract before the coverage claim ships.
  - Undocumented internal Google endpoints can change; all isolated behind the client, any non-200 → empty state.
  - OAuth field literals kept out of source per the no-secret convention (`GeminiOAuthField` constants; fixtures assembled by a builder, not raw JSON access-token key literals).

### WP-2 — Codex — 2026-07-12 19:52
- Branch / commits: `patch/2026-07-12/litellm-pricing` (base `dev` @ `3f460b9`). Two commits, in-worktree only (NOT merged/pushed/PR'd): `30ec7ce` feat(pricing) — PricingService + CodexProvider wiring; `9a44b56` test(pricing) — fallback-chain tests + fixtures.
- Done:
  - New `Core/Providers/PricingService.swift`: resilient `actor` implementing the chain **fresh <24h disk cache → live LiteLLM fetch (async, 15s timeout, off the UI path) → stale disk cache → bundled seed**. Per-model lookup order **curated overrides → LiteLLM table → negative cache** (unknown model resolves to `nil` once and is remembered — no repeated table scans). Slims each LiteLLM row to the 4 fields (`input_cost_per_token`, `output_cost_per_token`, `cache_read_input_token_cost`, `cache_creation_input_token_cost`) via `LiteLLMRate`, which is also the on-disk cache row shape. Injectable **fetcher** and **clock** (`now:`), plus `cacheURL`/`seed`, so tests need no live network or real `Date()`. mtime-based freshness. `PricingService.shared` = live feed + per-user caches dir + bundled seed.
  - **Key deviation from the WP letter (prior-art reuse):** discovered `Core/Pricing/*` already exists (`PricingTable`, `PricingRate`, `PricingEngine`, `PricingSeed` — a 40-model curated snapshot dated 2026-07-08 that already folds in the CodexPricing rows) plus a **dormant `PricingTableRefreshing` seam** used by nothing but its own tests. Rather than build a parallel price model / refactor `CodexPricing` into a second seed (would duplicate the curated table), I implemented `LiteLLMPricingTableRefresher: PricingTableRefreshing` and drove the whole service through the existing `PricingEngine`/`PricingSeed`. One engine, one curated seed. `CodexPricing.swift` is therefore left **untouched** (still exercised by its own frozen direct tests) instead of refactored — minimal diff, zero regression risk.
  - Wired `CodexProvider.costUsage` to price through `PricingService.shared` (injectable, default `.shared`); kicks the live refresh in a background `Task` (never awaited → snapshot never blocks). Costs for previously-known models are **byte-identical**: `PricingSeed` carries the same OpenAI/Codex rows and `PricingEngine` matches `CodexPricing` arithmetic because Codex tokens always have `cacheCreationTokens: 0`.
- Left undone / stubbed:
  - No app-startup/SyncEngine hook for the refresh (out of scope: WP-2 excludes UI/Sync). The refresh is triggered from `CodexProvider.fetchSnapshot` only — so the live table currently refreshes when Codex syncs. Other providers still price via their own paths (unchanged). Follow-up: a central launch-time `PricingService.shared.refreshIfStale()` and routing the other providers through the service.
  - `CodexPricing.swift` now unused in production (only by its direct tests). Left in place deliberately; a future cleanup could delete it once its tests are folded into the engine tests.
- Tests run + result: `cd AIUsageDashboard && xcodegen generate` (ok) → `xcodebuild -project AIUsageDashboard.xcodeproj -scheme AIUsageDashboardCore -destination 'platform=macOS' test` → **225 tests, 0 failures, 0 warnings** (215 baseline preserved + 10 new `PricingServiceTests`; existing `CodexCostTests`/`PricingEngineTests` unchanged & green). Real-artifact proof (throwaway networked test, then removed): production `PricingService.parseLiteLLM` parsed **2488 rows** from the live `raw.githubusercontent.com/BerriAI/litellm/main/model_prices_and_context_window.json` (2963 keys) and resolved gpt-5/gpt-5-mini/claude-sonnet-4-5/gemini-2.5-pro/deepseek-chat/o3/gpt-4.1 end-to-end — vs ~15 hardcoded before. `no-secret` gate PASS on both commits.
- Files touched: `Core/Providers/PricingService.swift` (new), `Core/Providers/CodexProvider.swift` (wire to service), `Tests/PricingTests/PricingServiceTests.swift` (new), `Tests/Fixtures/PricingFixtures.swift` (new). `CodexPricing.swift` NOT modified. No `project.yml` hand-edit (globbed; ran `xcodegen`). Shared `Tests/Fixtures/Fixtures.swift` untouched.
- Risks / watch-outs:
  - **Background network in the shared instance during tests**: the legacy `CodexCostTests`/`CodexProviderTests` use the default `PricingService.shared`, whose `fetchSnapshot` fires a non-blocking, non-awaited `refreshIfStale()` → one background GET to GitHub raw (and a possible write to `~/Library/Caches/AIUsageDashboard/`). It does **not** affect any assertion (they resolve synchronously from the seed) and does not block; all new tests are fully hermetic (injected fetcher+clock+temp cache). Confirmed green regardless.
  - `PricingSeed` curated rows always override LiteLLM (per the WP order) — verified numbers never regress, but a LiteLLM price change for a seeded model won't show until the seed is bumped. Intended.
  - LiteLLM keys can be provider-prefixed (`vertex_ai/…`); `PricingTable.resolveRate` already strips `provider/` + boundary-aligns, so bare slugs from logs still resolve. Rows with no `input_cost_per_token` (e.g. `sample_spec`, metadata-only) are dropped.
