# Patch Bible — 2026-09-17

> Single source of truth for today's parallel engineering. Generated by the herd
> orchestrator (run `~/.herd/runs/20260917-0209-eb/`, session cc1) from four read-only
> research audits: `tasks/t01.result.md` (stats accuracy, Astra), `t02` (CPU/RAM, Astra),
> `t03` (MCP / routing / accounts, Astra), `t04` (UI clutter, Cursor). Consumed by each
> agent and by the collection step. Do not delete; this is the audit trail.

- **Base branch:** `dev` @ `b447e34`
- **Integration target:** `dev`
- **Release target:** `main` → **0.9.0** (never merged automatically)
- **Gate mode at merge:** `full` (release gates run at `/dev-approved`)
- **Product:** Tokei (`ai.padzy.tokei`) — local-first macOS AI usage dashboard
- **Research results (read the one for your package; paths are absolute):**
  `~/.herd/runs/20260917-0209-eb/tasks/t0{1,2,3,4}.result.md`, scripts and probes under
  `~/.herd/runs/20260917-0209-eb/artifacts/t0N/` (t01 `recompute.py`, t02 `baseline/`,
  `chunkpool/`, `linear/` scratch variants, t03 `/schema-*.json` fixtures, t04 screenshots).

---

## 0. What the user asked for, verbatim priorities

1. Stats are wrong — find and fix. Two Claude accounts must both display correctly, math must add up.
2. Less CPU, less RAM, same job.
3. MCP must let an agent pick the account (Claude default vs account-1 today; multiple
   accounts of any provider tomorrow; other users' layouts differ).
4. Remove redundant UI. Live numbers animate (roll), not snap.
5. Simplify. Then test → security review → code review → notarise → update → website.

## 1. Selected issues (prioritized)

Score = user-impact × release-value ÷ risk, tie-broken by dependency then parallelism.
Only the defects with a reproduction in a result file were selected. IDs are the research IDs.

| # | Issue / source | Why it matters | Impact | Risk | Depends on | Agent | Branch |
|---|---|---|---|---|---|---|---|
| 1 | t01 D1, D14, D2, D3, D4, D10, D7-core, D6-core, D9 (Claude) · t02 F3 | Claude numbers are wrong on this machine: nested subagent logs never discovered (−70 M today, −243 M lifetime for account-1), one session file counted under both accounts (+159 M week/month/lifetime), first-wins dedupe drops output updates, append cache corrupts on partial tail / same-size rewrite, no upper day bound, default account's expired-credential reason dropped before export | H | M | — | Sol | `patch/2026-09-17/claude-accuracy` |
| 2 | t01 D5, D11 · t02 F1, F2 + WAL, F4, F5 · t03 A3 (Codex/Cursor `observedAt`) | Codex counts repeated telemetry (+242 K today, +78 M lifetime); Cursor accepted-lines leak into token charts; Codex stream loop is the memory high-water mark (10.6 s / 199 MiB → 0.5 s / 43 MiB on the largest file); opencode reparses an unchanged DB every refresh and misses committed WAL rows; Codex/Cursor quota windows carry no `observedAt`, bypassing the 30-min routing guard | H | M | — | Sol | `patch/2026-09-17/codex-cursor-opencode` |
| 3 | t03 A7, A8, A10 · t01 D7 (text CLI) | MCP misclassifies JSON-RPC errors (valid batch → ParseError, wrong version accepted, unknown tool reads disk first); text `tokei status` hides accounts entirely; docs promise `routeTo: null` the encoder never emits | M | L | — | Sol | `patch/2026-09-17/cli-mcp-transport` |
| 4 | t04 D1–D25 · user: animated live numbers · t01 D6/D8/D12/D13 (labels) · t02 F6 | ~1,650 lines of duplicate / decorative / dead UI; +1252 % delta pinned to a today headline; menu total ignores "show in totals"; "This week" is 7 rolling days; status strip ticks at 1 Hz while hidden; numbers snap instead of rolling | M | L | — | Cursor auto | `patch/2026-09-17/ui-declutter-motion` |
| 5 | t03 P1 + P2: A1, A2, A4, A5, A6 · t01 D8 semantics | Provider-agnostic `ProviderAccount` (identity key, discovery on refresh, Codex adapter, selector env), one account quota decision shared by provider + recommendation, `accountID`/`headlineAccountID`/`target`/`validUntil`/quota status in the public snapshot (schemaVersion stays 1, additive) | H | M | 1, 2 | Sol | `patch/2026-09-17/accounts-model` — **phase B** |
| 6 | t03 P3 + A9 | MCP tool descriptions and docs 06/08/09 + README for the account-aware schema; spec patch for `herd-budget`/`herd-roster.json` (consumer joins on provider id only) | M | L | 5 | Sol | `patch/2026-09-17/mcp-schema-docs` — **phase C** |

### Deliberately NOT selected

- Changing the token total to exclude cache reads (t01: 98 % of Claude's daily total is cache
  reads; the definition is consistent across every surface and explained in the drill-in —
  changing it silently changes the contract). A short "incl. cache" qualifier is UI copy, WP-4.
- Rewriting parsers or providers. Every fix below is the smallest change the audit named.
- Cross-machine snapshot sharing, App Store sandbox, WidgetKit (BACKLOG).
- Deleting `MaxxerMath.swift` from the UI layer (t04: not a visible element; a later Core pass).

## 2. Merge order & rationale

Phase A (parallel, disjoint scopes) → review → merge in this order → gates → phase B → phase C.

1. `patch/2026-09-17/claude-accuracy` — owns `Core/Models`, `UsageWindows`; everything else reads it.
2. `patch/2026-09-17/codex-cursor-opencode` — independent of 1 except shared test-fixture files (see §4).
3. `patch/2026-09-17/cli-mcp-transport` — `TokeiCLI/**` only.
4. `patch/2026-09-17/ui-declutter-motion` — `UI/**`, `App/**`, `Core/Sync/DashboardViewModel.swift`.
5. (phase B) `patch/2026-09-17/accounts-model` — cut from `dev` after 1–4 merge.
6. (phase C) `patch/2026-09-17/mcp-schema-docs` — cut from `dev` after 5 merges.

## 3. Per-issue work packages

Common to every package: worktree only; `cd AIUsageDashboard && xcodegen generate` before
the first build; the test command is
`xcodebuild -project AIUsageDashboard.xcodeproj -scheme AIUsageDashboardCore -destination 'platform=macOS' test`;
RED → GREEN → REFACTOR per defect (write the failing test first; the research files give the
exact fixture values); small reviewable commits; never `--no-verify`; never push/merge/PR;
append a completion note to §8; write the herd result files; reply with only the result path.
Each acceptance criterion names the number a reviewer will check.

### WP-1 · Claude accuracy — Sol
- **Worktree:** `../tokei-worktrees/2026-09-17-claude-accuracy` (branch `patch/2026-09-17/claude-accuracy`)
- **Scope (in):** `Core/Parsing/ClaudeJSONLParser.swift`, `ClaudeJSONLParser+Streaming.swift`,
  `ClaudeUsageRecord.swift`, `UsageWindows.swift`; `Core/Providers/ClaudeCodeProvider.swift`,
  `ClaudeAccount.swift`, `LocalLogProvider.swift`; `Core/Models/**`;
  `Core/Storage/ModelCodableExtensions.swift`; `Core/Utilities/FileWatcher.swift` (discovery reuse only);
  tests: `Tests/ParserTests/ClaudeJSONLParserTests.swift`, `DayBucketingTests.swift`,
  `RealLogsSmokeTests.swift`, `Tests/ProviderTests/ClaudeLogDiscoveryTests.swift`,
  `ProviderModelTests.swift`, `Tests/ClaudeUsageTests/**`, `Tests/UtilizationTests/**`,
  `Tests/StorageTests/**`, `Tests/Fixtures/Fixtures.swift`.
- **Scope (out):** everything else. Do not touch `Core/Parsing/Codex*`, `Opencode*`, `Cursor*`,
  `Antigravity*`, `Core/Providers/{Codex,Cursor,Opencode,Antigravity,Cline,Gemini,Copilot}Provider.swift`,
  `Core/Agent/**`, `Core/Sync/**`, `Core/Network/**`, `TokeiCLI/**`, `UI/**`, `App/**`, docs.
- **Work (research IDs → t01.result.md §Per-defect detail, t02 §Fix plan):**
  - D1 recursive log discovery (regular-file recursive enumerator in `ClaudeCodeProvider`; watcher reuses it).
  - D14 provider-wide dedupe with per-account allocation (shared claimed-key context across the
    sequential account parses; deterministic ownership; explicit "ambiguous history" warning;
    daily histories, splits, account rows and provider sums stay consistent).
  - D2 reconcile updated usage records (subtract previous / add updated; identical copies still dedupe).
  - D3 retain the unfinished tail across appends (reuse Codex's continuity approach; read it, do not import its file).
  - D4 append only after verified continuity (inode + tail check); same-size rewrite reparses.
  - D10 exclusive next-day upper bound in `UsageWindows` current-period windows.
  - D6-core `monthUsage` = last 30 days (`-29 days`) so it agrees with the 30D chart; keep 7-day
    week math (label change is WP-4's).
  - D9 (Claude) rebuild day buckets from timestamps when the effective calendar/timezone changes
    (parser side: invalidate cache on calendar identity change; expose the hook the view model can call).
  - D7-core `ProviderAccountUsage.quotaStatus` — a small enum
    `AccountQuotaStatus { eligible, expiredCredentials, cooldown, disabled, requestFailed, noQuotaSource, unknown }`
    + optional `quotaStatusDetail: String?` (bounded, no secrets) carried on the internal
    account model and persisted via `ModelCodableExtensions` (append-only). Populate it in
    `ClaudeCodeProvider.fetchAccount` from the existing error paths. **Do not** change
    `AgentSnapshot`/writer (phase B) — just make the data exist.
  - F3 carry file size in `LogSource` discovery metadata; zero redundant size stats on the warm path.
  - Delete-list (t02): unused zero-offset Claude parse wrapper; duplicate zero-usage constructor
    → `UsageWindows.emptyUsage(.localParsed)`; the Claude copy of the calendar-hour helper → the
    one in `UsageWindows`.
- **Acceptance criteria:**
  - [ ] `python3 ~/.herd/runs/20260917-0209-eb/artifacts/t01/recompute.py` recursive-first-wins
        totals per account agree with the app's per-account totals for the same frozen corpus
        copy to within the concurrent-growth delta (state the delta); D14 overlap counted once.
  - [ ] Fixtures from t01: nested + top-level + duplicate nested record → both files found, usage once;
        output 1→25 with input 10 / cache 100 → **135** (not 111, not 246); same message copied
        into two identities + one unique each → unique provider total, disjoint account totals
        summing to it, ambiguity warning present; split record + append → warm = cold = 135, no
        permanent malformed warning; same-size rewrite 100→900 → **900**; tomorrow's record →
        headline 0 today.
  - [ ] Two-account expired+valid fixture: default keeps `tokensToday 0`, empty computable windows,
        `quotaStatus == .expiredCredentials`; account-1 still routable; no credential material in any
        serialized form.
  - [ ] Warm two-account parse of the frozen corpus ≤ 80 ms in t02's harness pattern (baseline ~124 ms);
        unchanged rounds report `full=0, bytesRead=0`; all cache entries retained.
  - [ ] Existing 427+ tests green; new tests named per defect ID in the test name.
- **Known risks:** D14 ownership is the most delicate change — do not implement a scalar
  subtraction on the headline; if a fully consistent allocation cannot be finished, ship the
  provider-unique total + warning and mark the per-account allocation `partial` in the result.
  D9 needs a seam, not a rewrite. → mitigation: RED test first for every item; keep `parse()`
  signatures source-compatible (WP-2 and the app call them).
- **Rollback:** revert merge commit; branch retained.

### WP-2 · Codex / Cursor / opencode accuracy + memory — Sol
- **Worktree:** `../tokei-worktrees/2026-09-17-codex-cursor-opencode` (branch `patch/2026-09-17/codex-cursor-opencode`)
- **Scope (in):** `Core/Parsing/CodexJSONLParser.swift`, `CodexJSONLParser+Streaming.swift`,
  `OpencodeStoreParser.swift`, `SQLiteSidecarCopy.swift`, `CursorStateDBParser.swift`,
  `AntigravityStateDBParser.swift`; `Core/Providers/CodexProvider.swift`, `CursorProvider.swift`,
  `OpencodeProvider.swift`, `AntigravityProvider.swift`; `Core/Network/CursorUsageClient.swift`
  (observedAt stamp only); `Core/Storage/QuotaSeriesStore.swift` (redundant sort only);
  tests: `Tests/ParserTests/{Codex*,Opencode*,Cursor*,Antigravity*}.swift`,
  `Tests/ProviderTests/{Cursor*,Opencode*,Antigravity*,CodexCost*}.swift`,
  `Tests/Fixtures/{CursorFixtures,OpencodeFixtures,AntigravityFixtures}.swift` and a **new**
  `Tests/Fixtures/CodexFixtures.swift` (do not edit `Fixtures.swift` — WP-1 owns it).
- **Scope (out):** everything else, especially `UsageWindows.swift`, `Core/Models/**`,
  `ModelCodableExtensions.swift`, all Claude files, `Core/Agent/**`, `TokeiCLI/**`, `UI/**`.
  `QuotaWindow.observedAt` already exists — use it, do not change the model.
- **Work:**
  - D5 Codex: unchanged cumulative usage adds zero tokens (keep last cumulative signature per
    file/session in the cache, survive incremental reads; rate-limit observations still update;
    explicit handling for cumulative resets/decreases and resumed sessions).
  - F1 Codex stream loop: `autoreleasepool` around chunk processing; track the searched position
    so the unfinished line is not rescanned per 64 KiB; keep CRLF/EOF/incomplete-tail/malformed/
    inode/continuity semantics byte-for-byte (t02 §Verification has seven boundary fixtures).
  - F4 memoize Codex model detection by file identity/mtime/size (including nil).
  - A3 stamp `observedAt` on every emitted Codex quota window from the rate-limit event time, and
    on Cursor windows from the successful response time (never the refresh time for a historic event).
  - D11 Cursor: never put accepted-lines into token `dailyTotals`; assign online totals
    unconditionally (nil when unavailable).
  - F2 + WAL opencode: read WAL-aware (coherent snapshot; drop `immutable=1` main-only reads —
    t02 measured 305,539 tokens omitted), then cache the aggregate keyed by main+WAL identity/
    size/mtime + calendar day; invalidate on any change, WAL-only append, midnight, failure.
  - F5 Cursor/Antigravity: one sidecar copy per refresh, temp cleanup; delete the pass-through
    wrappers; delete the unread `now` in `CursorStateDBParser`; Codex zero-offset wrapper;
    Codex duplicate zero-usage constructor → `UsageWindows.emptyUsage`; `QuotaSeriesStore`
    redundant sort.
- **Acceptance criteria:**
  - [ ] Codex fixture cumulative 100→100→150 / last 100→100→50 → **150**, including when the
        duplicate straddles an append; repeated quota event updates quota, adds 0 tokens.
  - [ ] Largest Codex file (frozen copy in t02 artifacts) cold parse ≤ 1.0 s and ≤ 64 MiB peak RSS
        in an optimized harness (observed 0.504 s / 43.2 MiB); full corpus ≤ 35 s / ≤ 180 MiB;
        every token component, window, daily/hourly series, quota field, warning and offset
        equals the baseline run.
  - [ ] 30 unchanged opencode refreshes → one SQL scan; cache lookup ≤ 5 ms; WAL-only append changes
        the next result; frozen WAL fixture yields the ro-mode total (6,720 rows), not the immutable one.
  - [ ] Every Codex and Cursor quota window carries `observedAt` equal to the event/response time;
        a 2-hour-old Codex rate-limit event is no longer routable under the existing 30-min policy
        (assert via `RouteTargetPolicy` in a test — read-only use of `Core/Agent`).
  - [ ] Cursor offline state with 123 accepted lines → token history unavailable; empty CSV → empty, never 123.
  - [ ] Existing tests green; new tests named per ID.
- **Known risks:** F1 boundary indices → seven fixtures + existing append/truncate/rotation tests
  are the gate. F2 invalidation → never cache an error; clock/calendar keyed.
- **Rollback:** revert merge commit; branch retained.

### WP-3 · CLI / MCP transport + text accounts — Sol
- **Worktree:** `../tokei-worktrees/2026-09-17-cli-mcp-transport` (branch `patch/2026-09-17/cli-mcp-transport`)
- **Scope (in):** `TokeiCLI/**`, `Tests/CLITests/**`, `docs/08-agent-snapshot-schema.md`
  (only the A10 corrections: absent-or-null `routeTo`, shared policy statement, `accounts` +
  `observedAt` in the main example). `Tests/Fixtures/AgentSnapshotFixtures.swift` additive only.
- **Scope (out):** `Core/**`, `UI/**`, `project.yml`, README (phase C), any schema field additions (phase B).
- **Work (t03 A7, A8, A10; t01 D7 text CLI):**
  - JSON-RPC envelope validation separate from JSON syntax: −32700 only for malformed JSON;
    −32600 invalid request (wrong version, bad id type, missing method); −32601 unknown method;
    −32602 unknown tool / bad `arguments` (validated **before** any snapshot read); valid
    notifications get no reply; a notification-shaped message with an id is answered; batch:
    either implement per the MCP revision declared in `initialize` or reject explicitly with
    −32600 — never −32700. Update the existing tests that lock in the wrong classes.
  - Text `tokei status`: list each account under its provider (label, tokens today, windows or
    the reason none) — read what `AgentAccount` already carries; no new schema fields.
  - Keep exactly two tools, newline transport, clean stdout, EOF exit.
- **Acceptance criteria:**
  - [ ] Exchange matrix from t03 §Edge-case exchanges reproduced as tests: wrong version, object id,
        missing method, `{}`, batch, unknown tool, missing name, extra/malformed arguments,
        CRLF, no trailing newline, EOF, stale / missing / malformed snapshot.
  - [ ] Unknown tool with a missing snapshot returns −32602, not a snapshot error.
  - [ ] `tokei status` on the two-account fixture prints both accounts.
  - [ ] All CLI tests green (the standard `AIUsageDashboardCore` scheme runs them).
- **Known risks:** low. Do not "improve" the schema here.
- **Rollback:** revert merge commit; branch retained.

### WP-4 · UI declutter + live-number motion — Cursor auto
- **Worktree:** `../tokei-worktrees/2026-09-17-ui-declutter-motion` (branch `patch/2026-09-17/ui-declutter-motion`)
- **Scope (in):** `AIUsageDashboardApp/UI/**`, `AIUsageDashboardApp/App/**`,
  `Core/Sync/DashboardViewModel.swift` (D13 visibility filtering + D9 calendar-change hook call
  + F6 only), `Core/Analytics/UsageAnalytics.swift` (D12 only if needed), `Tests/ChartsTests/**`,
  `Tests/AnalyticsTests/**`, `Resources/**`.
- **Scope (out):** everything else. UI never calls a provider directly.
- **Work:**
  - t04 delete list D1–D25 and merge list, in the file-scope groups t04's Handback names
    (extract `ProviderOverviewRow` statics before deleting its view; keep `SectionCard`,
    `DeltaLabel`, `ProviderVisibility` enum, `ConfidenceBadge` type, `MultiAccountNotice` for
    `.claudeSetup`). Keep-but-fix items: accent budget (tab tick + one primary button), mono
    kickers ≥ 13.5 px, hero suffix ghosting, Value multiple truncation, no new cards.
  - **Animated live numbers (user request):** every figure that changes on refresh — menu-bar
    total, Overview hero and grid cells, quota percents and bars, per-account rows, drill-in
    metrics, Value multiples — transitions with `.contentTransition(.numericText(value:))`
    (or `.numericText(countsDown:)` where direction matters) under
    `.animation(.easeOut(duration: 0.2–0.25), value:)`; bars/gauges interpolate their fill.
    Gate on `accessibilityReduceMotion` → instant. Nothing longer than 300 ms; no bounce.
  - t01 labels: D6 "This week" → "Last 7 days"; daily-history title uses the selected range;
    D12 the +% delta sits beside the ranged total it was computed from (or is removed from the
    today headline); D13 menu total and any "tightest" pressure figure respect
    `ProviderVisibility` like the Overview does; D8 headline gauges are labelled as the named
    best account; a "tightest account" surface shows the real max (80 in the 80/20 fixture).
  - F6 status strip 1 Hz tick gated by the existing window-visibility state; short "incl. cache"
    qualifier next to the token total where t01 asked.
- **Design constraints (house rules, verbatim — no skill available to you):**
  ground `#0B0B0D` (shipped; do not revert to `#131316`), surface/panel per `PadzyTheme.swift`,
  ink `#ECECF1`, muted `#6E6E78`, ONE accent `#FF3B70` for active/primary only, numbers in ink,
  mono for all numeric data, radius ≤ 4 px for cells, 1 px hairlines, no shadows/gradients,
  no coloured status dots (the word carries state), no badge on every row, type floor 13.5 px mono
  / 15 px otherwise, real empty/loading/error states remain. Motion: 120–200 ms micro,
  200–300 ms panel, ease-out on enter, reduce-motion honoured.
- **Acceptance criteria:**
  - [ ] `xcodebuild … AIUsageDashboardApp … build` green; the five deleted chart/banner files are
        gone from `project.yml`-generated targets (xcodegen picks up the filesystem — verify no
        dangling references); no preview or test references a deleted type.
  - [ ] Screenshots after (same surfaces as `artifacts/t04/*.png`) attached in the result; the
        element count per surface drops per t04's table; no house-rule regressions.
  - [ ] A screen recording or frame sequence showing a number rolling on refresh (⌘R) at ≤ 300 ms;
        with Reduce Motion on, the same refresh snaps.
  - [ ] 80/20 two-account fixture: Overview gauge labelled with the best account, tightest surface = 80;
        hidden-provider fixture: menu total == Overview total.
  - [ ] Existing tests green.
- **Known risks:** deleting views used by previews only — build the app target, not just Core.
  Cursor cannot run the padzy-os skill: the rules above are the whole rulebook; when in doubt, delete.
- **Rollback:** revert merge commit; branch retained.

### WP-5 · Accounts model + account-aware decision (phase B) — Sol
Cut from `dev` after WP-1..4 merge. Spec: t03 §Schema proposal, §One provider-agnostic account
abstraction, Packages 1 + 2 (merged into one owner to avoid the interface hand-off).
Scope: `Core/Providers/**`, `Core/Models/**` (+ new `ProviderAccount.swift`),
`ModelCodableExtensions.swift`, `Core/Agent/**`, `Core/Parsing/CodexJSONLParser.swift`
(per-account rate-limit slice + eviction), all related tests + `AgentSnapshotFixtures.swift`.
Deliver: `ProviderAccount`/`AccountProfile`/`AccountSelector`/`AccountDiscovering`; identity key
`providerID + ':' + SHA256(providerID + NUL + quotaIdentity)` with `:local:` fallback; discovery
on every refresh (A6); Codex adapter (`CODEX_HOME` + registered roots); one account quota
decision (peak-first trust + freshness) used by provider headline and recommendation (A1, A2);
public additive fields `accountID`, `selector`, `quota{status,usedPercent,headroomPercent,
bindingWindowIndex,validUntil}`, provider `headlineAccountID`, `recommendation.target`,
`avoidAccounts`, `validUntil` (A4, A5); `schemaVersion` stays 1; writer exports bounded reason
codes only. Delete `AgentRecommendationEngine.headlineAccount`/`peakPercent`, the provider's
private `headlineAccount`/`hasRoutableReading`/`peakPercent`, and the `export CLAUDE_CONFIG_DIR`
reason text. Acceptance = t03 Package 1 + 2 lists, plus the exact before/after JSON fixtures in
`artifacts/t03/schema-*.json`.

### WP-6 · MCP schema descriptions, docs, consumer (phase C) — Sol
Cut after WP-5. Spec: t03 Package 3 + A9. Scope: `TokeiCLI/**` (tool descriptions, freshness
metadata, expiry invalidation on read), `README.md`, `docs/{06,08,09}.md`, `project.yml`
(helper compiles the shared Foundation-only schema/expiry source), `Tests/CLITests/**`; plus a
**spec file** `tasks/reports/herd-budget-account-aware.md` for the Herdr vault consumer (the
orchestrator applies it outside this repo). Acceptance = t03 Package 3 list.

## 4. Frozen contracts (all agents, non-negotiable)

- `UsageProvider` protocol — do not change signatures.
- `UsageStore` public API — additive only.
- `ProviderSnapshot` fields — **append-only**; any new stored field MUST be added to
  `Core/Storage/ModelCodableExtensions.swift` in the SAME commit (manual Codable). Only WP-1
  (phase A) and WP-5 (phase B) may do this.
- `AgentSnapshot` public schema — **phase A does not add fields.** `schemaVersion` stays 1 through 0.9.0.
- Existing tests stay green — never delete or weaken them. Tests that lock in a wrong behaviour
  (t03 A8 names two) are *corrected* with a comment citing the research ID, not deleted.
- Test fixtures = Swift string literals under `Tests/Fixtures/` (no resource files). `Fixtures.swift`
  is WP-1's; WP-2 uses provider-named fixture files.
- No secrets outside Keychain. Raw provider responses = debug-only. No email / raw UUID / token
  in any serialized or logged form.
- No UI → provider direct calls (UI talks to the view model / sync engine only).
- Parser `parse()` entry points stay source-compatible.
- Numbers reported must not change except where a research ID says the old number was wrong.

## 5. Design / UX constraints (Padzy OS "aitracker")

See WP-4. UI ownership is WP-4 only. Core packages must not touch `UI/**`; if a core change
needs a label change, write it in the result's Handback and WP-4 (or the orchestrator) applies it.

## 6. Gates (created / verified this run)

`bash .claude/gates/preflight.sh` PASS on `dev` @ `b447e34` (uncommitted: five deleted PNGs under
`tasks/brand`, `tasks/handoff` — pre-existing, not ours). Gate scripts present and executable:
preflight · worktree-sanity · lint · format · build · test · no-secret · no-large-artifact ·
no-uncommitted · cli-version-sync · run-all. Per-commit (pre-commit hook in each worktree):
no-secret, no-large-artifact, format. At merge: build, test, lint baseline compare.
Release/strict at `/dev-approved`.

## 7. Global rollback plan

- Each work package merges to `dev` as a single `--no-ff` commit → `git revert -m 1 <sha>`.
- `dev` is disposable; `main` is the safety net. Nothing reaches `main` without `/dev-approved`
  and the user's word.
- Worktrees and `patch/*` branches retained until 0.9.0 ships, then pruned.
- Perf baseline for the before/after: a **fresh** launch of the 0.8.0 build and of the `dev`
  build on the same frozen corpus, ≥ 8 min settle, footprint + RSS + 180 s CPU delta
  (t02 §Fix plan). The live pid 13882 numbers (731 MB RSS at 15 min; 153 MB later) are not a
  controlled baseline.

## 8. Completion log (agents append; auto-read by the orchestrator's collection step)

<!-- Each agent appends on finish:
### WP-N — {{agent}} — {{time}}
- Branch/commits: <sha …>
- Done: …
- Left undone / stubbed: …
- Tests run + result: …
- Files touched: …
- Risks / watch-outs: …
-->

### WP-3 — Codex — 2026-09-16 20:02 PDT
- Branch/commits: `patch/2026-09-17/cli-mcp-transport` / `6278d49`
- Done: A7 JSON syntax vs envelope classification, explicit batch rejection, A8 pre-read tool validation, D7 text account rows, A10 doc corrections, and the frozen exchange-matrix tests.
- Left undone / stubbed: none in WP-3 phase-A scope.
- Tests run + result: 58 selected CLI tests PASS; standalone `tokei` build PASS; frozen-fixture status and 12-frame MCP smoke PASS with parseable stdout and empty stderr.
- Files touched: `TokeiCLI/{MCPServer,StatusFormatting}.swift`, `Tests/CLITests/{MCPServer,MCPServerExchangeMatrix,SnapshotReader,StatusFormatting}Tests.swift`, `Tests/Fixtures/AgentSnapshotFixtures.swift`, `docs/08-agent-snapshot-schema.md`.
- Risks / watch-outs: full Core baseline had 3 pre-existing `ClaudeMultiAccountProviderTests` failures and was interrupted at a live-log smoke test to honor the no-live-provider-state boundary; whole-tree strict lint remains baseline-red, while every changed Swift file is lint-clean.

### WP-2 — Codex — 2026-09-16 20:12 PDT
- Branch/commits: `patch/2026-09-17/codex-cursor-opencode`; `018e951`, `b33ac7a`,
  `c291271`, `e160ae6`, `60fef3e`, `c771de0`, `fc2412a` (plus this log-correction commit).
- Done: D5 cumulative-signature deduplication across appends/resets; F1 linear, pooled Codex
  streaming; F4 positive/nil model cache; A3 Codex/Cursor observation timestamps; D11 Cursor
  line/token separation; F2 WAL-aware opencode snapshots and main+WAL+day cache; F5 one-snapshot
  Cursor/Antigravity path and requested delete-list cleanup.
- Left undone / stubbed: no product work. The current combined parser's full 816-file corpus was
  not freshly rerun because the supplied full manifest points at forbidden live provider state;
  preserved t02 evidence for the same F1 implementation is 26.253 s / 140,410,880 bytes.
- Tests run + result: full `AIUsageDashboardCore` suite, 520 tests / 0 failures; Codex frozen
  largest-file `-O` harness 0.846409 s / 56,999,936 bytes with aggregate digest parity; all seven
  F1 boundary fixtures byte-offset/record/malformed/digest parity; frozen opencode snapshot 6,720
  rows, lifetime 486,313,523, 30 cache lookups max 1.816034 ms; preflight PASS.
- Files touched: scoped Codex, Cursor, opencode, SQLite, Antigravity, quota-series sources and their
  scoped tests; new `Tests/Fixtures/CodexFixtures.swift`; this completion log.
- Risks / watch-outs: one preserved benchmark was initially invoked with its arguments in the
  wrong order, briefly reading the live-path manifest before it was killed; it made no writes and
  its output was overwritten by the frozen-fixture rerun. The SQLite busy/locked fallback is
  intentionally limited to rollback-journal databases without a WAL; WAL sources always require
  the coherent online-backup path.

### WP-2 review fix round — Codex — 2026-09-16 21:06 PDT
- Branch/commits: `patch/2026-09-17/codex-cursor-opencode`; `75d4601`, `c449e12`, plus the
  lint-neutral follow-up that records this final log correction.
- Done: R06-02 now retries SQLite online backup and fails closed instead of raw-copying a locked
  rollback-journal main file, retaining an opencode parser's last coherent cached aggregate;
  R06-03 carries Cursor quota response time independently of later CSV completion; R06-04 ignores
  empty/null/unrecognized cumulative objects so valid deltas remain additive. Named regressions use
  the review fixtures.
- Tests run + result: live-state-safe `AIUsageDashboardCore` suite, 514 tests / 0 failures;
  `git diff --check` clean. Frozen 826-file corpus retained aggregate digest
  `33fe524e9ea928c6bb61bf7cab0615105653279cb378b968d6b69d0bd926f9f7` and offset digest
  `a231148c09cd4a7c95d1a6d43e350b43581328b455a3b81cc5cf5ed985480cb9` in every run.
- Performance: exact 4 MiB production runs were 36.003 s / 175.63 MiB and 40.309 s / 184.55 MiB;
  experimental 64 KiB was 45.905 s / 119.92 MiB, 2 MiB was 35.250 s / 178.39 MiB, and sliced-line
  4 MiB was 50.347 s / 191.58 MiB. Instrumented copies were 30.475 s / 168.75 MiB and
  47.830 s / 203.66 MiB; stream/decode/accumulate dominated at 47.599 s versus 0.167 s metadata
  and 0.005 s apply in the latter run. No experimental parser change outperformed the checked-in
  4 MiB implementation safely, so no speculative performance diff was retained.
- Files touched: scoped Codex/opencode/SQLite/Cursor sources and scoped parser/provider tests; this
  completion log.
- Risks / watch-outs: the exact final-tree corpus did not reproducibly meet both the 35 s / 180 MiB
  gate on the loaded shared host; the orchestrator should re-measure on a quiet host as planned.

### WP-4 — Cursor auto — 2026-09-16 20:15 PT
- Branch/commits: `patch/2026-09-17/ui-declutter-motion` @ `95bd618`
- Done: t04 delete/merge list (D1–D3, D5, D20–D23 views); live numbers via `rollingNumber` / `numericText` ≤300ms; D6/D8/D12/D13 labels; F6 status-strip gate; "incl. cache" on the token total.
- Left undone / stubbed: D9 parser-cache rebuild stays WP-1 (view-model calendar hook + refresh only). Live Reduce Motion snap was not recorded (system setting restored; unit test is the gate). ⌘R frame sequence used the same `rollingNumber` path via the 7D→30D chip because a no-op refresh does not roll.
- Tests run + result: `AIUsageDashboardCore` 517 tests, 0 failures (`** TEST SUCCEEDED **`, 2026-09-16 20:08:21). App Debug build `** BUILD SUCCEEDED **` after the warning fixes.
- Files touched: `UI/**` listed in the commit, `Core/Sync/DashboardViewModel.swift`, `Core/Analytics/UsageAnalytics.swift`, `Tests/AnalyticsTests/WP4LabelMotionTests.swift`.
- Risks / watch-outs: screenshots are the Debug instance reading the live corpus (pid 80023, quit after capture). Do not treat those numbers as a fixture. Menu-bar popover was not captured; the item reads `82%`.

### WP-1 — Codex — 2026-09-16 20:20 PDT
- Branch/commits: `patch/2026-09-17/claude-accuracy`; `951b934`
- Done: D1, D14, D2, D3, D4, D10, D6-core, D9, D7-core, F3, and the Claude delete-list. Provider-wide allocation is deterministic and disjoint; account quota status is append-only Codable state.
- Left undone / stubbed: no 1,515-file post-patch replay because no frozen full Claude corpus was present and live state is forbidden. The available four-file frozen two-account corpus passed at 0.269 ms warm with `full=0`, `bytesRead=0`, and 4/4 cache entries retained.
- Tests run + result: RED per defect in `/tmp/herd-t05/red.log`; final scoped suites 65/65 green; final fixture-only core suite 515/515 green. Frozen recompute raw totals default=250/account-1=260 reconcile to app allocations default=130/account-1=260 via one shared 120-token message; provider total=390, concurrent-growth delta=0. Strict lint remains globally red but improved from 360 baseline violations to 354; formatter skipped because `swiftformat` is not installed.
- Files touched: Claude parser/streaming, usage windows, Claude provider, `LogSource`, `ProviderAccountUsage`, model Codable extensions, and scoped Claude/provider/storage tests.
- Risks / watch-outs: the final suite excluded four live-state smoke suites to honor fixture-only constraints. An initial baseline and an interrupted broad run did execute read-only live smoke tests before that exclusion was complete; no provider state was mutated. Re-run the full-corpus 1,515-file warm benchmark from an authorized frozen copy during integration.

### WP-1 review fixes — Codex — 2026-09-16 21:18 PDT
- Branch/commits: `patch/2026-09-17/claude-accuracy`; implementation `90e11a8`
- Done: closed R05-1 through R05-4 with a generation-keyed reconciled allocation cache, chunk-scoped autorelease, inode-backed cache identity, shared Claude fixture factory, the later-timestamp tie test, and bounded ownership-warning text that names the selected account and reason.
- Benchmarks: reviewer's optimized `Bench.swift` on the frozen 1,722-file corpus measured cold 53.900 s at 120.531 MiB cold peak RSS and warm unchanged parses of 41.194, 40.029, 39.408, 39.062, and 39.283 ms. Start/end load averages were 10.03/13.15/14.65 and 7.15/11.88/14.09. All eight per-account totals matched the review baseline.
- Tests run + result: final Core suite 521 tests, 3 expected non-corpus smoke skips, 0 failures; scoped Claude suites 56/56 green; all 166 adverse byte splits and the mtime-preserving atomic-replacement probe passed; `git diff --check`, secret, and large-artifact checks passed. SwiftLint remains red only on inherited violations and improves the changed-file baseline from 56 to 54; `swiftformat` is unavailable.
- Files touched: `LogSource`, Claude provider and parser/streaming, shared Claude fixtures, Claude parser/discovery/multi-account tests, and this completion log.
- Risks / watch-outs: the final acceptance run is frozen-corpus-only. An extra Core verification attempt incorrectly used `HOME` instead of `CFFIXED_USER_HOME`, so Xcode's test runner read live provider logs read-only; its evidence was discarded, no provider state was written, and the suite was rerun successfully with the test runner fixed to the frozen corpus.

### WP-5 — Codex — 2026-09-16 22:34 PDT
- Branch/commits: `patch/2026-09-17/accounts-model`; implementation `9e06190` plus this completion-log commit.
- Done: A1/A2 shared peak-first trusted/fresh account quota decision, A4 reader-side recommendation expiry, A5 additive account-aware snapshot fields and exact-JSON oracle, A6 refresh-time Claude/Codex discovery with identity folding and metadata caches, and the Codex half of D9 calendar-aware cache rebuilding. Duplicate headline/peak/trust implementations and shell-export recommendation text were removed.
- Left undone / stubbed: none in WP-5 scope. Codex identity uses only recognized nonsecret `auth.json` account metadata; roots without it intentionally use distinct canonical-path local IDs.
- Tests run + result: final live-state-safe `AIUsageDashboardCore` suite 586 tests / 0 failures with the four real-log smoke suites explicitly skipped; focused account-discovery suite 7/7; standalone `tokei` and app Debug builds succeeded; `git diff --check` clean. TDD RED and focused GREEN logs are retained under `/tmp/herd-t09/`.
- Files touched: provider/account models, Claude and Codex discovery/providers, Codex parser cache/calendar handling, agent schema/writer/recommendation logic, model Codable extensions, scoped tests/fixtures, `docs/08-agent-snapshot-schema.md`, and this completion log.
- Risks / watch-outs: no live provider state, network, or paid API was used. `swiftformat` is unavailable. Strict SwiftLint remains red on inherited oversized/complex files and legacy test-style violations; the new discovery test files are lint-clean.

### WP-5 review fixes — Codex — 2026-09-17 12:00 PDT
- Branch/commits: `patch/2026-09-17/accounts-model`; `45d6856`, `29cd9bb`, `c8a4d53` plus this completion-log commit.
- Done: resolved R09-01 through R09-07 and R09-09/R09-10. Codex reads only `tokens.account_id`, normalizes unknown identity before caching, and gates reauthenticated quota on a new observation while retaining the old identity's token baseline. Public binding indices now address emitted windows; account eligibility/expiry covers every computable window and mixed ties. Executable selectors are provider-specific, nonempty, absolute, existing-directory maps revalidated at export/read time. Removed Claude registered/inherited roots now disappear while the synthetic default remains.
- Tests run + result: named R09 regressions were observed red then green. Final clean-environment Core gate with all four real-log smoke suites skipped executed 600 tests with 0 failures in 6.417 seconds; `git diff --check` clean. Logs are under `/tmp/herd-t09b/`.
- Files touched: WP-5 Codex/Claude discovery and provider files, Agent quota/schema/writer/recommendation files, related Core tests including `CodexProviderTests.swift`, and this completion log.
- Risks / watch-outs: reauthentication history is in-memory for the provider lifetime; app restart reconstructs current identity from disk and cannot recover a prior identity boundary that was never persisted. No live provider state, network, paid API, deployment, push, merge, or PR was used.

### WP-5 second review fixes — Codex — 2026-09-17 12:31 PDT
- Branch/commits: `patch/2026-09-17/accounts-model`; implementation `7ee3c0a`, attribution isolation `e240d1f`, plus this completion-log commit.
- Done: R09B-01 now derives account rows from raw per-root dated totals and non-overlapping identity epochs, conserving tokens through A→B→C, shared-root splitting, and midnight rollover. R09-07 uses the refresh that observes an identity change as the quota boundary, so older events remain quarantined until an event at or after the transition. R09-04 treats explicit unavailable applicable windows as incomplete coverage while preserving the binding-window projection.
- Tests run + result: all five named regressions were observed RED before implementation and GREEN afterward. Final live-state-safe Core gate skipped the four real-log smoke suites and executed 605 tests with 0 failures in 6.462 seconds; focused decision/discovery gate executed 25 tests with 0 failures; `git diff --check` clean. Logs are under `/tmp/herd-t09c/`.
- Files touched: `Core/Agent/AccountQuotaDecision.swift`, `Core/Parsing/CodexJSONLParser.swift`, `Core/Providers/{CodexProvider,CodexIdentityAttribution}.swift`, `Tests/AccountDiscoveryTests/CodexAccountDiscoveryTests.swift`, `Tests/AgentTests/AccountQuotaDecisionTests.swift`, and this completion log.
- Risks / watch-outs: identity epochs remain in memory for the provider lifetime; after an app restart, an identity transition that was only observed by the prior process cannot be reconstructed. No live provider state, network, paid API, deployment, push, merge, or PR was used.

### WP-5 third review fix — Codex — 2026-09-17 12:52 PDT
- Branch/commits: `patch/2026-09-17/accounts-model`; implementation `fd2f785` plus this completion-log commit.
- Done: R09C-01 replaces calendar-day-keyed identity baselines with cumulative counters captured at each transition, then partitions the parser's current calendar-projected days at those invariant boundaries. LA→Tokyo→LA retains A's 10 tokens, and a later Tokyo-day event belongs to B without breaking provider/account conservation.
- Tests run + result: the named calendar regression was observed RED with five ownership failures, then GREEN. The focused Codex provider/discovery gate executed 15 tests with 0 failures. The final live-state-safe Core gate skipped the four real-log smoke suites and executed 606 tests with 0 failures in 6.444 seconds; both changed files are strict-SwiftLint clean; `git diff --check` clean. Logs are under `/tmp/herd-t09d/`.
- Files touched: `Core/Providers/CodexIdentityAttribution.swift`, `Tests/ParserTests/CodexProviderTests.swift`, and this completion log.
- Risks / watch-outs: epoch ownership is still process-local, as previously documented. `swiftformat` is unavailable; whole-tree strict SwiftLint remains baseline-red, while both changed Swift files have zero violations. No live provider state, network, paid API, deployment, push, merge, or PR was used.
