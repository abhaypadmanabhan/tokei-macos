# Patch Bible — 2026-07-24

> Single source of truth for the day's parallel engineering. Generated by
> `/morning-patch`. Consumed by each agent and by `/agents-done`. Do not delete;
> this is the audit trail. Filename: `tasks/patch-bibles/YYYY-MM-DD.md`.

- **Base branch:** `dev` @ `3e16ed4`
- **Integration target:** `dev`
- **Release target:** `main` (never merged automatically)
- **Gate mode at merge:** `full` (release gates run at `/dev-approved`)
- **Product:** Tokei (`ai.padzy.tokei`) — local-first macOS AI usage dashboard

**Today's driver:** live dry run of the newly-restructured pipeline (herdr direct
dispatch + auto-collect, built 2026-07-23/24) — this run is dual-purpose: land real
work AND exercise the new machinery. User specifically flagged the MCP-integration
issue (#57) as a required inclusion.

**Important correction made during Step 1/2 (see hindrance log for detail):** of
the candidate issues initially considered, **3 newly-discovered-this-run turned out
to already be shipped but left open on GitHub** (#41, #48, #55) — confirmed against
both prior Patch Bible completion logs and the live source tree before being dropped
from selection. Same pattern as the already-known #49/#51/#54 (3 more, from a prior
session) — 6 confirmed total, plus #1/#2 flagged separately below as *likely* stale
(inferred from the shipped README state, not independently re-verified with the same
blame/source-tree rigor as the other 6). This is a real gap in the pipeline (nothing
currently closes an issue on merge) — see hindrance log, not fixed this run.

---

## 1. Selected issues (prioritized)

Priority score = user-impact × release-value ÷ risk, tie-broken by dependency
order then parallelization potential.

| # | Issue / source | Why it matters | Impact | Risk | Depends on | Agent | Worktree branch |
|---|----------------|----------------|--------|------|-----------|-------|-----------------|
| WP-1 | **#57** Agent access: bundled `tokei` CLI + stdio MCP server | User-mandated this run. Orchestrating agents are blind to quota across tools; Tokei already computes the data, just has no machine-readable surface. Design pre-validated in a same-day brainstorm (full JSON schema in the issue). | **H** | **M** | — | **Claude Code (Opus)** | `patch/2026-07-24/agent-snapshot-mcp` |
| WP-2 | **#26** (Copilot half only) Provider expansion framework | Gemini + OpenCode halves of #26 already shipped (verified: `GeminiProvider.swift`, `OpencodeProvider.swift` exist). Copilot (`auth db + app parser`) is the one remaining first-target connector, and no Copilot files exist in source — confirmed genuinely open. | **M** | **M** | — | **Codex** | `patch/2026-07-24/copilot-connector` |
| WP-3 | **#19** Robustness batch (Core-only slice) | Small audit-sourced fixes: unbounded Cline-log read (memory risk on huge/symlinked files), non-atomic `UsageStore.persist()` (data loss risk on crash mid-write + 644 perms), full paths leaking into warning strings. Bounded, mechanical, no judgment calls. | **M** | **L** | — | **Cline** | `patch/2026-07-24/robustness-core` |

**Deferred out of this WP-3 slice (still open, not dropped, just descoped to keep
scope disjoint from WP-1/WP-2 and off the UI lock):** #19's dock-icon-toggle,
launch-at-login-toggle, and stale-docs items — those touch Settings UI and
`README.md`/`docs/`, better owned by a future UI package. Noted in WP-3 scope-out.

**Confirmed already shipped, excluded (see correction note above):** #41 (lifetime
usage — `ValueView.swift`/`MaxxerMath.swift` etc.), #48 (`QuotaSeriesStore`
per-series retention — `defaultPerSeriesRetentionLimit` already present), #55
(Codex incremental parser cache — commit `99b38d8`, 2026-07-22), #49/#51/#54
(commit `85f512e`, 2026-07-22/23) — **6 confirmed**. Plus #1/#2, flagged separately
below as likely-also-stale (P0 features both already shipped per README, not
re-verified with the same rigor) — **8 total candidates for cleanup.** Recommend
the user bulk-close all eight on GitHub — not done here, out of scope for an
engineering patch run.

**Not selected (genuine backlog, deferred for scope/risk reasons):** #4/#52/#53
(Antigravity connector work — BACKLOG.md flags the data source as "research
stalled," too high-risk for unattended one-shot dispatch this round), #14
(credential/Keychain hardening — security-sensitive, wants deliberate review not
a first pipeline test), #10 (website — separate repo/workflow per
[[tokei-website-redesign]], not this Swift-agent fleet), P0 #1/#2 (stale, see
above), remaining token-maxxer/gamification epics (#20/#23/#24/#25/#27/#36/#37) —
scope discipline, keep this dry run tractable.

## 2. Merge order & rationale

All three are file-disjoint (verified: WP-1 touches only new files, WP-2 touches
only new `Core/Providers/Copilot*` files, WP-3 touches `ClineMessagesParser.swift`
+ `UsageStore.swift` + one string in `ClaudeJSONLParser.swift`) — no dependency
order required between them. Merge smallest/lowest-risk first so a problem in a
bigger package doesn't block landing the safe ones:

1. `patch/2026-07-24/robustness-core` (WP-3) — smallest, most mechanical, lowest risk.
2. `patch/2026-07-24/copilot-connector` (WP-2) — new isolated provider module, additive.
3. `patch/2026-07-24/agent-snapshot-mcp` (WP-1) — largest/newest surface area, land last.

## 3. Per-issue work packages

### WP-1 · Agent-facing quota snapshot + `tokei` CLI/MCP (#57) — Claude Code (Opus)
- **Worktree:** `../tokei-worktrees/2026-07-24-agent-snapshot-mcp` (branch `patch/2026-07-24/agent-snapshot-mcp`)
- **Scope (in):** new `Core/Agent/AgentSnapshotWriter.swift` (or similar — your naming), a new bundled Swift executable target (`tokei` helper — add to `AIUsageDashboard/project.yml` as a new target under `Tokei.app/Contents/Helpers/`), its own source dir (e.g. `AIUsageDashboard/TokeiCLI/`), and `docs/` for the schema doc.
- **Scope (out):** **the Settings "Agent Access" UI section is explicitly OUT OF SCOPE for this dispatch** (UI is Claude-locked to a dedicated UI package per §5 — don't self-assign it just because you're also Claude Code; note it as follow-up in your completion log instead). Do not touch `UsageProvider.swift`, `UsageStore.swift` (WP-3 owns Store this run), any existing provider files, any `AIUsageDashboardApp/UI/` file.
- **Spec:** full design already validated in the issue body — read `gh issue view 57` yourself for the complete JSON schema, the exact 2 MCP tools (`get_usage`, `get_route_recommendation`), the `tokei status [--json]` / `tokei mcp` CLI shape, staleness rules (>10min or app-not-running → `"stale": true`, never silent), and the no-secrets constraint (percentages/counts/timestamps only).
- **Realistic scope for one dispatch:** this is a multi-day-sized feature. Prioritize, in order: (1) `AgentSnapshotWriter` + the versioned JSON schema written atomically after each refresh, (2) `tokei status`/`tokei status --json`, (3) `tokei mcp` stdio server if time allows. Be honest in your completion note about what's stubbed — partial, well-tested progress is expected and fine.
- **Acceptance criteria:**
  - [ ] `agent-snapshot.json` written atomically after every refresh cycle to `~/Library/Application Support/AIUsageDashboard/`; schema documented in `docs/`.
  - [ ] `tokei status --json` returns the snapshot; `tokei status` renders a readable table.
  - [ ] No secrets in the output (audit your own diff for this before committing).
  - [ ] Stale/missing snapshot paths handled (app quit, app never launched) — never silent.
  - [ ] (stretch) `tokei mcp` registers and both tools are callable end-to-end.
- **Test requirements:**
  - [ ] Unit tests for `AgentSnapshotWriter` schema encoding + the staleness calculation.
  - [ ] Core suite stays green.
- **Design / UX constraints:** none — this package is Core + a new CLI target, no SwiftUI.
- **Known risks:** adding a new Xcode/xcodegen target is easy to get subtly wrong (signing, bundling into `Contents/Helpers/`) → mitigation: get the target building and producing correct output before polishing the MCP layer; a working `tokei status` beats a half-built MCP server. Scope creep into the Settings UI → mitigation: explicit scope-out above.
- **Rollback:** revert merge commit; branch retained.

### WP-2 · GitHub Copilot connector (#26, Copilot half only) — Codex
- **Worktree:** `../tokei-worktrees/2026-07-24-copilot-connector` (branch `patch/2026-07-24/copilot-connector`)
- **Scope (in):** new `Core/Providers/CopilotProvider.swift` (conform to `UsageProvider`, mirror `GeminiProvider.swift`/`OpencodeProvider.swift` as your reference pattern), plus whatever new `Core/Network/` or `Core/Parsing/` file(s) the data-reading needs (your call, follow existing naming convention).
- **Scope (out):** everything else. Do NOT modify `UsageProvider.swift`'s protocol signature (frozen, §4), do NOT touch `GeminiProvider.swift`/`OpencodeProvider.swift` (done, working, not your scope). UI is out of scope by default — **update, mid-run exception granted (see §8 completion note):** adding `ProviderID.copilot` broke existing exhaustive `switch` statements over `ProviderID` in 4 UI files; the orchestrator approved minimal, mechanical case-arm additions only (no new views/behavior) so the app would compile — `UI/Charts/ProviderDonut.swift`, `UI/Components/{AgentTint,ProviderMark,ProviderMetadata}.swift`.
- **Reference pattern:** read `Core/Providers/GeminiProvider.swift` and `Core/Providers/OpencodeProvider.swift` first — #26's "provider expansion framework" (the generalized `detect()`/`capabilities()`/`readUsage()`/`readUtilization()` contract) is **already built and already used by both** — you are adding a new conformance, not building new infrastructure.
- **Spec:** Copilot usage data lives in its own auth/session store (issue #26 says "auth db + app parser" — investigate what Copilot actually persists locally on this machine/in general; likely under `~/.config/github-copilot/` or similar, and/or VS Code's own storage). If the real on-disk format can't be determined confidently, say so explicitly in your completion note rather than guessing at a schema — a documented "researched, format TBD, stubbed with `capabilities()` returning unavailable" is an acceptable, honest outcome for this package.
- **Acceptance criteria:**
  - [ ] `CopilotProvider` conforms to `UsageProvider`, registered alongside the other 5 providers.
  - [ ] `detect()` correctly reports installed/not-installed.
  - [ ] If a usable local data source is found: `readUsage()`/`readUtilization()` return real data. If not: return `.unavailable` cleanly (no crash, no fabricated numbers) and document why in your completion note.
  - [ ] Handle the "tool not installed" case with a real empty state (no UI work needed from you — just make sure the Core layer doesn't crash the existing empty-state UI).
- **Test requirements:**
  - [ ] Unit tests for `CopilotProvider` (detect + at least the not-installed/unavailable path; real-data path if the format was determined).
  - [ ] Core suite stays green.
- **Design / UX constraints:** none (Core only).
- **Known risks:** Copilot's local storage format may be undocumented/encrypted (issue itself says "auth db") → mitigation: time-box the research; a clean "unavailable" stub is a legitimate, honest deliverable if the format can't be determined safely — do not guess at parsing an undocumented encrypted store.
- **Rollback:** revert merge commit; branch retained.

### WP-3 · Robustness batch — Core slice only (#19) — Cline
- **Worktree:** `../tokei-worktrees/2026-07-24-robustness-core` (branch `patch/2026-07-24/robustness-core`)
- **Scope (in):** `Core/Parsing/ClineMessagesParser.swift` (line ~31, unbounded `Data(contentsOf:)` read), `Core/Storage/UsageStore.swift` (lines ~38-56, `persist()` — non-atomic write + swallowed errors + file permissions), `Core/Parsing/ClaudeJSONLParser.swift` (line ~44, `error.localizedDescription` leaking full paths into warning strings — strip to filename only).
- **Scope (out):** the dock-icon toggle, launch-at-login toggle, and stale-docs items from #19 — those are UI/docs and explicitly deferred to a future package (see §1). Do not touch any `AIUsageDashboardApp/UI/` file. Do not touch any provider connector file (WP-2's scope).
- **Fixes required:**
  - Cline parser: add a size cap before `Data(contentsOf:)` (or switch to streaming) so a huge/symlinked `*.messages.json` can't balloon memory — pick a sane cap (e.g. document your choice) and skip/log-and-skip files over it rather than crashing.
  - `UsageStore.persist()`: replace delete-then-move with `FileManager.replaceItemAt` (atomic); stop swallowing errors via bare `try?` — surface/log them; create the store file with `0600` permissions (currently `644`).
  - `ClaudeJSONLParser.swift`: warning strings built from `error.localizedDescription` must not embed full filesystem paths — use just the filename.
- **Acceptance criteria:**
  - [ ] A synthetic oversized/symlinked Cline messages file no longer causes unbounded memory growth (test with a size cap in place).
  - [ ] `UsageStore.persist()` uses atomic replace; a killed-mid-write scenario doesn't corrupt the store; new store files are created `0600`.
  - [ ] Claude parser warning strings contain no full paths (filename only) — add/update a test asserting this.
- **Test requirements:**
  - [ ] Unit test: oversized Cline log → capped/skipped, not OOM.
  - [ ] Unit test: `UsageStore.persist()` atomicity/permissions (as testable without a real crash).
  - [ ] Unit test: Claude parser warning string has no path separator / no absolute path.
  - [ ] Core suite stays green.
- **Design / UX constraints:** none (Core only, and note: Cline has no herdr lifecycle tracking — completion for this package is confirmed via the `HERDR_TEST_DONE`-style marker convention, not agent status).
- **Known risks:** changing file permissions on an existing store could break on an upgrade if the file already exists with different perms → mitigation: only enforce `0600` on file *creation*, don't chmod an existing file as a side effect unless that's also explicitly desired (your call, document it either way).
- **Rollback:** revert merge commit; branch retained.

## 4. Frozen contracts (all agents, non-negotiable)

- `UsageProvider` protocol (`Core/Providers/UsageProvider.swift`) — do not change signatures. WP-2 adds a new conformance only.
- `UsageStore` public API — additive only. **WP-3 is the sole editor of `UsageStore.swift` this run** (internal `persist()` changes only, no public signature changes).
- `ProviderSnapshot` fields — **append-only**; any new stored field MUST be added to `Core/Storage/ModelCodableExtensions.swift` in the SAME commit (manual Codable).
- Existing tests stay green — never delete or weaken them.
- Test fixtures = Swift string literals in `Tests/Fixtures/Fixtures.swift` (no resource files).
- No secrets outside Keychain. Raw provider responses = debug-only. **WP-1's snapshot file is a new externally-readable surface — audit it personally for secrets before committing (percentages/counts/timestamps only, per the issue's own design).**
- No UI → provider direct calls (UI talks to the view model/sync engine only).
- No package this run touches `AIUsageDashboardApp/UI/` — if you think you need to, stop and say so in your completion note instead of doing it.

## 5. Design / UX constraints (Padzy OS "aitracker")

No UI package this run — all three work packages are Core/CLI-only by design (see
each WP's scope). Tokens below are reference only, in case any package
incidentally needs to reason about UI-adjacent code paths without editing them:

ground `#131316` · surface `#1D1D22` · ink `#ECECF1` · muted `#6E6E78` · ONE accent `#FF3B70`.
Accent = state/action ONLY. Mono for all numeric data. No shadows, no gradients,
no rounded card grids, radius ≤ 4px. UI is LOCKED to a dedicated Claude Code UI
agent scoped to `AIUsageDashboardApp/UI/`, running `padzy-os` — none of today's
packages qualify or are permitted to touch it.

## 6. Gates (created / verified this run)

- **Preflight:** PASS on `main`, with WARN (uncommitted pipeline-doc changes on
  `main` — not blocking, worktrees come from `dev`'s clean tree, see hindrance log).
- **`dev` branch:** exists @ `3e16ed4` (not created this run).
- **Gate scripts:** all 10 present + executable in `.claude/gates/` (`ls -l` confirmed).
- Fast set (no-secret, no-large-artifact, format) runs per-commit via the
  pre-commit hook installed in each new worktree; full build+test runs at merge
  (Step 9, auto-inline this run); release/strict set at `/dev-approved`.

## 7. Global rollback plan

- Each work package merges to `dev` as a single `--no-ff` commit → revert is one `git revert -m 1 <sha>`.
- `dev` is disposable; `main` is the safety net. Nothing reaches `main` without `/dev-approved`.
- Worktrees and `patch/*` branches are retained until the day's patch ships, then pruned.
- WP-1's new bundled CLI target is net-new (no existing behavior to regress) — if broken, dropping the merge entirely is zero-risk to the rest of the app.

## 8. Completion log (agents append; auto-read by the orchestrator's collection step)

<!-- Each agent appends on finish:
### WP-N — {{agent}} — {{time}}
- Branch/commits: <sha …>
- Done: …
- Left undone / stubbed: …
- Tests run + result: …
- Files touched: …
- Risks / watch-outs: …
-->

### WP-3 — Cline — 2026-07-24 ~01:15 PDT
- Branch/commits: `patch/2026-07-24/robustness-core` @ `3608e81`, `375c24d`, `23dd681`
- Done:
  - `ClineMessagesParser.swift`: added configurable 16 MB size cap (default) before `Data(contentsOf:)`; oversized/symlinked files are skipped with a filename-only warning instead of being loaded.
  - `UsageStore.swift`: rewrote `persist()` to use `FileManager.replaceItemAt` for atomic writes; errors are now logged via `os.Logger` instead of swallowed; new store files are created with `0600` permissions via a placeholder, while existing file permissions are left untouched.
  - `ClaudeJSONLParser.swift`: removed `error.localizedDescription` from the parse-error warning so no full filesystem path can leak; warning now contains only the filename.
  - Added/updated unit tests for all three fixes and confirmed the existing 329 Core tests still pass.
- Left undone / stubbed:
  - Issue #19's dock-icon toggle, launch-at-login toggle, and stale-docs items remain intentionally out of scope per WP-3 scope-out.
  - No AIUsageDashboardApp/UI/ files or provider connectors were modified.
- Tests run + result:
  - `cd AIUsageDashboard && xcodegen generate`
  - `xcodebuild -project AIUsageDashboard.xcodeproj -scheme AIUsageDashboardCore -destination 'platform=macOS' test` — **329 tests, 0 failures**.
- Files touched:
  - `AIUsageDashboard/AIUsageDashboardApp/Core/Parsing/ClineMessagesParser.swift`
  - `AIUsageDashboard/AIUsageDashboardApp/Core/Storage/UsageStore.swift`
  - `AIUsageDashboard/AIUsageDashboardApp/Core/Parsing/ClaudeJSONLParser.swift`
  - `AIUsageDashboard/Tests/ParserTests/ClineMessagesParserTests.swift`
  - `AIUsageDashboard/Tests/StorageTests/UsageStoreTests.swift`
  - `AIUsageDashboard/Tests/ParserTests/ClaudeJSONLParserTests.swift`
- Risks / watch-outs:
  - `UsageStore` uses a 0600 placeholder file for first-time creation; if `replaceItemAt` metadata preservation behavior changes across macOS versions, permissions could deviate. Existing files are never chmod'd, matching the Bible's mitigation.
  - The Cline cap is a hard skip, not streaming; legitimate messages files >16 MB will be ignored with a warning. The default was chosen to be generous for normal Cline sessions while bounding memory.

### WP-2 — Codex — 2026-07-24 01:09 PDT
- Branch/commits: `patch/2026-07-24/copilot-connector` / `9b6055d` (`Add GitHub Copilot provider detection`)
- Done: added `CopilotProvider` as a `UsageProvider`, added `.copilot` to `ProviderID`, registered it in `ProviderRegistry.default()`, and kept the value engine unpriced for Copilot. Detection checks known Copilot CLI/config markers, VS Code/Cursor extension directories, VS Code global storage, common CLI binary paths, and the Copilot for Xcode app. Missing installs return `.notInstalled` and an unauthenticated, fully unavailable snapshot without throwing; detected installs return `.installed` with unknown auth and an explicit unavailable-data warning. Added five provider tests covering marker and versioned-extension detection, not-installed behavior, installed-but-unavailable behavior, and registry inclusion.
- Left undone / stubbed: token usage, cost, and utilization/quota remain deliberately unavailable, and `capabilities` is empty. Research found VS Code `chatSessions` files locally, but the sampled records exposed model/session metadata rather than exact token usage. The VS Code extension obtains quota snapshots from authenticated responses and stores credentials in protected storage. Current GitHub documentation identifies `~/.copilot/session-state/*/events.jsonl` and `session-store.db`, but their usage schema is managed/undocumented and those data stores were absent on this machine. No schema or numeric usage was guessed.
- Tests run + result: from `AIUsageDashboard`, ran `xcodegen generate`; `xcodebuild -project AIUsageDashboard.xcodeproj -scheme AIUsageDashboardCore -destination 'platform=macOS' test` — PASS, 329 tests / 0 failures; `xcodebuild -project AIUsageDashboard.xcodeproj -scheme AIUsageDashboardApp -destination 'platform=macOS' build CODE_SIGNING_ALLOWED=NO` — BUILD SUCCEEDED. Pre-commit: no-secret PASS, no-large-artifact PASS, SwiftFormat SKIP because `swiftformat` is not installed.
- Files touched: `AIUsageDashboardApp/Core/Providers/CopilotProvider.swift`, `AIUsageDashboardApp/Core/Providers/ProviderRegistry.swift`, `AIUsageDashboardApp/Core/Models/ProviderID.swift`, `AIUsageDashboardApp/Core/Maxxer/MaxxerValueEngine.swift`, `Tests/ProviderTests/CopilotProviderTests.swift`, `Tests/ProviderTests/ProviderModelTests.swift`; plus the user-approved mechanical exhaustive-switch arms in `UI/Components/AgentTint.swift` (reused Cursor's neutral tint), `UI/Charts/ProviderDonut.swift` (display name), `UI/Components/ProviderMetadata.swift` (detected paths), and `UI/Components/ProviderMark.swift` (missing asset name + existing code-symbol fallback). No views or UI behavior were added.
- Risks / watch-outs: local Copilot storage is product/version dependent; custom `COPILOT_HOME`, uncommon IDE plugin locations, or a future documented usage schema may require expanding detection/parsing. The connector intentionally prefers unavailable metrics over reading secrets or fabricating estimates.

### WP-1 — Claude Code (Opus) — 2026-07-24 ~01:20 PDT
- Branch/commits: `patch/2026-07-24/agent-snapshot-mcp` @ `819c240` (core snapshot writer + schema), `91195bc` (tokei CLI + stdio MCP + docs)
- Done — **all 5 acceptance criteria met, including the MCP stretch**:
  - **AgentSnapshotWriter** (`Core/Agent/`): maps the snapshots Tokei already collects → a versioned, public `AgentSnapshot` schema and writes `agent-snapshot.json` **atomically** (`Data.write(.atomic)`) to `~/Library/Application Support/AIUsageDashboard/` after **every** refresh. Hooked into `SyncEngine.refreshAll()` (additive, default-valued dependency — backward-compatible init; no other call site changed).
  - **Public schema decoupled from internal stores.** Per-window `%` mapped from `QuotaWindow` (retains `source`); aggregate reuses `UtilizationEngine.aggregate`; recommendation via a new pure `AgentRecommendationEngine`. Internal 5-level `MetricConfidence` collapses to the public `official | local_estimate | unavailable`.
  - **No secrets**: percentages/counts/timestamps only — verified by a unit test (`testEncodedSnapshotContainsNoSecretShapedFields`), a manual grep of the whole new surface, and the no-secret gate. Personally audited the emitted JSON.
  - **`tokei status` / `tokei status --json`**: readable aligned table + raw snapshot. **Staleness never silent** — >10min (or app-not-running, which collapses to old `generatedAt`) → `⚠︎ STALE` banner in the table and `"stale": true` + `ageSeconds` in JSON; still exits 0. **Missing file** (app never launched) → actionable "launch Tokei" error, exit 3.
  - **`tokei mcp`** (stretch, DONE): dependency-free newline-delimited JSON-RPC 2.0 stdio server; `initialize`/`notifications/initialized`/`tools/list`/`tools/call`/`ping` + unknown-method `-32601`. Both tools (`get_usage`, `get_route_recommendation`) driven **end-to-end** and verified (fresh, stale-prefix-warning, missing→isError, unknown-tool→isError).
  - **Bundled** at `Tokei.app/Contents/Helpers/tokei` via a new xcodegen `tool` target + copy-files phase; verified present + runnable in a **clean app build**, and standalone (`otool -L` shows it does NOT link the Core framework/Sparkle).
- Left undone / stubbed:
  - **Settings "Agent Access" UI section — intentionally OUT OF SCOPE** (UI is Claude-locked to a dedicated package per §5). The install one-liners are written up in `docs/08-agent-snapshot-schema.md` ready for that package. **Follow-up for a future UI dispatch.**
  - **`MaxxerMath.routeTarget` (#37) and `AgentRecommendationEngine` are two engines today** because `MaxxerMath` lives under `UI/MenuBar/` and is not compiled into the Core framework (only into the test target). The agent engine re-expresses the same least-filled semantics + the issue's 85% avoid threshold. **Follow-up: when MaxxerMath moves into Core, collapse them into one so the human chip and the agent snapshot are guaranteed identical.**
  - Live end-to-end proof against a *real running app* wasn't done (the installed app predates this feature, so it doesn't write the file yet); proven instead via the writer unit tests + the CLI/MCP run against schema-valid fixtures. Will be live after this merges and a dev build runs.
  - `tokei` CLI version is a hardcoded constant (`0.6.1`) tracking `MARKETING_VERSION`; minor drift risk noted in code.
- Tests run + result:
  - `cd AIUsageDashboard && xcodegen generate`
  - `xcodebuild -project AIUsageDashboard.xcodeproj -scheme AIUsageDashboardCore -destination 'platform=macOS' test` — **344 tests, 0 failures** (329 baseline + 15 new Agent tests; all 3 new Agent suites confirmed executed).
  - `xcodebuild ... -scheme tokei ... build` — BUILD SUCCEEDED; `... -scheme AIUsageDashboardApp ... build` (Debug, clean) — BUILD SUCCEEDED with `Contents/Helpers/tokei` embedded + runnable.
  - Behavioral proof: ran `tokei help / status / status --json / mcp` against fresh, stale, and missing snapshots — all correct.
  - Pre-commit fast gates on both commits: no-secret PASS, no-large-artifact PASS, format SKIP (swiftformat not installed).
- Files touched (new unless noted):
  - `AIUsageDashboardApp/Core/Agent/AgentSnapshot.swift`, `AgentSnapshotWriter.swift`, `AgentRecommendationEngine.swift`
  - `AIUsageDashboardApp/Core/Sync/SyncEngine.swift` *(modified — the only existing Core file, additive hook)*
  - `TokeiCLI/main.swift`, `SnapshotReader.swift`, `StatusCommand.swift`, `StatusFormatting.swift`, `MCPServer.swift`
  - `Tests/AgentTests/AgentSnapshotWriterTests.swift`, `AgentSnapshotStalenessTests.swift`, `AgentRecommendationEngineTests.swift`
  - `project.yml` *(modified — new `tokei` tool target + app embed)*, `docs/08-agent-snapshot-schema.md`
  - Did NOT touch: `UsageStore.swift`, `UsageProvider.swift`, `ModelCodableExtensions.swift`, any provider file, any `AIUsageDashboardApp/UI/` file. File-disjoint from WP-2/WP-3.
- Risks / watch-outs:
  - **Merge note (no textual conflict expected):** WP-2 adds `.copilot` to `ProviderID`. WP-1 iterates `ProviderID.allCases` and uses `.rawValue` with no exhaustive switch on `ProviderID`, so it's forward-compatible — a new provider case flows through the snapshot automatically. WP-1 touches only new files + `SyncEngine.swift` + `project.yml`, none of which WP-2/WP-3 edit.
  - The app-embed copy phase only lands the helper on a **clean** build (an incremental build in a fresh DerivedData dir showed the copy running before `tokei` existed). Release/packaging does a clean build, so this is fine; flagged so `/dev-approved` archive verification confirms `Contents/Helpers/tokei` is present.
  - MCP server is hand-rolled (no MCP Swift SDK) to stay dependency-free/read-only; it implements the common client subset (initialize, tools/list, tools/call, ping, notifications). If a client requires a capability beyond that, extend `MCPServer.handle`.

## 9. Manual test checklist (2026-07-24 — tailored)

- [ ] Core flows: multi-provider dashboard loads; existing providers still show correct data (regression check — nothing in today's diff should change existing behavior).
- [ ] **NEW — GitHub Copilot row**: with Copilot NOT installed, provider shows a clean "not installed" empty state (no crash, no fake numbers). If you have Copilot/a Copilot VS Code extension installed, provider shows "installed" with an honest "usage unavailable" state, not a guessed number.
- [ ] **NEW — `tokei` CLI**: run `Tokei.app/Contents/Helpers/tokei status` and `tokei status --json` from Terminal after launching Tokei once — confirm real data appears, confirm it errors clearly (exit 3) if the app was never launched.
- [ ] **NEW — `tokei mcp`**: register it with Claude Code (`claude mcp add tokei -- <path>/tokei mcp`) or Codex CLI and confirm `get_usage`/`get_route_recommendation` are callable and return sane data end-to-end (this is the one acceptance-criterion item WP-1 marked done-but-not-externally-verified from inside its own dispatch — first real cross-process check).
- [ ] **NEW — staleness**: quit Tokei, wait, then run `tokei status` — confirm a `STALE` warning appears (age-based, no silent stale data).
- [ ] Settings/preferences: unaffected by today's changes — spot check nothing regressed.
- [ ] Data persistence: `usage-store.json` still survives relaunch — confirm the new atomic-write path (`UsageStore.persist()`) didn't change on-disk behavior from a user's perspective.
- [ ] Robustness (WP-3, hard to observe directly): no user-visible change expected — these are crash/leak-prevention fixes (Cline oversized-file cap, atomic store write, path-free warnings). Regression-only: confirm Cline provider still works normally.
- [ ] Performance/permission flows: unaffected by today's changes.
- [ ] Anything named in WP-1/WP-2/WP-3 acceptance criteria above — cross-check each `[ ]` against real behavior, not just the agents' self-reported test runs.
